# 裁决包 · S1/S2 预告项（缺失审计产出）

> **一句话**：对当前已定内容做**缺失审计**（只问"什么还没写"），盘出迟早会咬人的决策；均属 S1/S2，**不阻塞 S-1 冻结**。
> **状态**：⏳ 待裁决 ｜ **裁决人**：用户 ｜ **最后更新**：2026-09-19
> **用法**：每项给 2–3 个选项 + 建议，可一行回复；**不写的按建议执行（标为未定默认）**
> ⚠️ **文件名不带项数** —— 项数会增长，写进文件名就会过期（本文件原名为"预告八项"，2026-09-19 扩至 11 项后改名）
> **当前覆盖**：B-018 ~ B-028，共 **11 项**

---

## 🔴 先说最实在的一笔欠账（不在本节 8 项内，但优先度最高）

**BL-002 · 165 条资料全部未挂"采用状态"** —— 资料是收了，但**一条都没被"采用"过**，等于白收。
⇒ 建议补一轮「资料采用轮」：把支撑 S-1 八个槽位的那批标 ✅，其余保持 ⏳。**采用权在你**（`../80-资料/README.md`）。

---

## B-018 · 演示脚本（= S5 验收标准，也是 SLICE-00 的验收标准）

> **一句话**：演示什么、跑几步，才算"可演示"？—— **不定这条，做完了也不知道算不算完成。**

| 选项 | 内容 | 客观代价 |
|---|---|---|
| **① 最小闭环 7 步**（建议作为**及格线**） | 编程序 → 校验 → 下发 → 虚拟下位机执行 → 3D 实时映射 → 误差上报 → 归正下发 | 与 P-1「闭环不可缺」一一对应 |
| **② ① + 多设备切换** | 再加"切换/同时看多台" | 体现 B-013 的差异点 |
| **③ ② + 报警与日志展示** | 再加报警触发与日志查询 | 演示面更全，工作量更大 |

- **AI 建议**：**① 定为及格线**，②③ 列为加分项（不进 DoD）

---

## B-019 · 坐标系与单位

> **一句话**：3D 场景用什么单位、原点在哪 —— 不定则**所有几何与插补都要返工**。

| 选项 | 内容 | 代价 |
|---|---|---|
| **① mm，原点在工件/工作台一角**（建议） | 与行业参数口径一致（资料里精度都是 mm：±0.01mm、±0.02mm） | 需明确原点到机器零点的偏移 |
| **② mm，原点在机器零点** | 与运动控制口径一致 | 模型摆放要额外处理偏移 |
| **③ 无量纲单位化** | 与渲染解耦 | 与行业数值脱节，不便讲解 |

- **AI 建议**：**①**

---

## B-020 · 设备状态集合（状态机 ④ 的输入）

> **一句话**：层次+表驱动的状态机需要**状态枚举**才能落地。

| 选项 | 状态集 | 代价 |
|---|---|---|
| **① 最小 6 态** | 离线 / 就绪 / 运行 / 暂停 / 急停 / 报警 | 简洁；但"回零中""排胶中"无处安放 |
| **② 8 态**（建议） | ① + **回零中** + **排胶中** | 覆盖 B-011 的运行控制 8 项 |
| **③ 更细** | 再加空跑、单步等 | 与"不做单步/断点"冲突（§8） |

- **AI 建议**：**②**；且**顶层父状态用 ① 那 6 个**（急停放顶层 ⇒ 表达"覆盖一切"），子状态放到"运行"下面

---

## B-021 · 持久化选型

> **一句话**：设备 / 程序 / 工艺 / 记录 四类数据存哪。

| 选项 | 代价 |
|---|---|
| **① SQLite** | 关系清晰、可查询；旧项目用过（有 `demo_seed.sql`） |
| **② JSON 文件** | 最省；但查询与并发写麻烦 |
| **③ 内存 + 退出落盘** | 最快；崩溃丢数据 |
| **④ SQLite + JSON 配置**（建议） | 业务数据进 SQLite，配置进 JSON ⇒ 各用所长 |

- **AI 建议**：**④**
- 行业参考：`../80-资料/03-...md` 方向 4 —— Nordson 用 CSV / FTP 留存点胶日志（合规用途）

---

## B-022 · 单元测试框架（§6 已定"单测纳入"，但框架未选）

> **一句话**：用什么框架写 §6 要求的单测。

| 选项 | 代价 |
|---|---|
| **① GoogleTest**（建议） | 与你已有的 C++ 测试体系（GTest/GMock + CTest + sanitizer + 覆盖率）一致；企业认可度高 |
| **② Qt Test** | 与 Qt 集成自然；但 GMock 能力弱，纯算法测试不如 GTest |
| **③ Catch2** | 单头文件；生态不如 GTest |

- **AI 建议**：**①**

---

## B-023 · Qt / VTK 版本锁定（按原则 E2 必须记录）

> **一句话**：外部依赖要锁版本并留档，否则环境一变就崩。

| 项 | 建议锁定值 | 来源 |
|---|---|---|
| Qt | **6.10.3**（官方 `C:/Qt/6.10.3`） | 实测（2026-09-19） |
| VTK | **9.3**（vcpkg `x64-windows`，**MSVC**） | 实测 |
| 构建 | CMake + Ninja；**MSVC 工具集** | 实测 |
| ⚠️ 坑 | vcpkg 内另含一套 Qt6 ⇒ 必须在 CMake 中**显式指定 `Qt6_DIR`** 指向官方路径，否则被拉回 vcpkg 那套 | 旧项目已踩 |

- **AI 建议**：**锁定 Qt 6.10.3 + VTK 9.3**，并在 S1 建一份「构建与依赖」文件留档

---

## B-024 · 虚拟下位机的时间推进方式

> **一句话**：演示时程序跑完要多久 —— 直接决定演示体验。

| 选项 | 代价 |
|---|---|
| **① 实时 1:1** | 真实；但一个几百点的程序可能要跑几分钟 |
| **② 实时 + 可调倍速**（建议） | 演示时加速、调试时 1:1；少量代码 |
| **③ 步进（手动触发）** | 调试友好；演示不连贯 |

- **AI 建议**：**②**

---

## B-025 · 源码树结构与代码命名规范

> **一句话**：14 个模块落成什么目录、代码怎么命名 —— S1 必答。

| 选项 | 代价 |
|---|---|
| **① 按模块分目录**（`src/模块名/`） | 与 14 模块一一对应，边界清晰 |
| **② 按层分目录**（`src/core`、`src/ui`、`src/comm`） | 分层清晰；但模块边界被拆散 |
| **③ 混合：按层 + 层内按模块**（建议） | 兼顾；需先定层 |

- **AI 建议**：**③**；并配套一份**代码命名规范**（类名 / 函数 / 成员 / 命名空间），**避免各模块各写各的**

---

## B-026 · VTK + Qt 集成可行性验证（🔴 P0 · **前提性风险**）

> **一句话**：VTK 9.3（MSVC）能不能和官方 Qt 6.10.3 跑通，**从未验证过** —— 不成立则整个 3D 主攻方向不成立。

| 选项 | 做法 | 客观代价 |
|---|---|---|
| **① 作为 SLICE-00 的第一个断言**（建议） | 行走骨架的第一步就是"VTK 渲染窗口能在 Qt 窗口里显示出图元" | 零额外工作 —— 它本来就是骨架的必经步骤 |
| **② 单独立一个"技术验证"任务，先于一切** | 提前隔离风险 | 多一个任务；且骨架迟早要做同一件事 |
| **③ 不专门验证，做到哪算哪** | 最省 | 🔴 风险后置 —— 若集成不通，已完成的工作可能作废 |

- **AI 建议**：**①**
- **依据**：旧项目同一位置留有「**Qt Quick 3D 可用性验证未做 ⇒ 3D 主攻存在前提风险**」的未决记录；换成 VTK 后问题同构
- **需验证的具体点**：`QVTKOpenGLNativeWidget` 与 Qt 6.10 的兼容性 · OpenGL 上下文共享 · 事件转发（鼠标旋转/平移/缩放）

## B-027 · git 仓库（🔴 P0）

> **一句话**：`newproject3/` 至今**没有版本控制**。

| 选项 | 做法 | 客观代价 |
|---|---|---|
| **① 建立 git + 冻结打 tag**（建议） | 初始化仓库；每次层冻结打 tag（如 `S-1-frozen`），与变更单体系对齐 | 极少量日常开销 |
| **② 暂不建** | 省事 | 🔴 改坏无法回退；且**迭代过程本身是简历可讲的素材**，不留痕就讲不出 |

- **AI 建议**：**①**
- **依据**：文档 + 代码体量已不小；旧项目同类问题（是否建 git）一直未答

## B-028 · 项目名称（🟡 P1）

> **一句话**：简历需要**项目名**，目前所有文件里都是描述性长句。

| 选项 | 说明 |
|---|---|
| **① 你定**（建议） | AI 可提供候选，**不代决** |
| **② 沿用描述句** | 简历里出现一长串描述，不够利落 |

- **AI 建议**：**①**；参照旧项目的构词法（`GlueTwin` = Glue + Twin）

---

## 汇总（一行回复即可）

| 编号 | 待裁项 | AI 建议 |
|---|---|---|
| B-017b | **线程架构**（你还没答） | **④ 每设备一线程 + SPSC 无锁汇聚** |
| B-018 | 演示脚本（验收标准） | ① 最小闭环 7 步（及格线） |
| B-019 | 坐标系与单位 | ① mm，原点在工件角 |
| B-020 | 设备状态集合 | ② 8 态（顶层 6 态，急停放顶层） |
| B-021 | 持久化 | ④ SQLite + JSON 配置 |
| B-022 | 单测框架 | ① GoogleTest |
| B-023 | Qt / VTK 版本锁定 | Qt 6.10.3 + VTK 9.3（显式指定 `Qt6_DIR`） |
| B-024 | 虚拟下位机时间推进 | ② 实时 + 可调倍速 |
| B-025 | 源码树与命名 | ③ 按层 + 层内按模块，配套命名规范 |
| **B-026** | **VTK + Qt 集成验证** | 🔴 ① 并入 **SLICE-00 第一个断言** |
| **B-027** | **git 仓库** | 🔴 ① 建立，冻结打 tag |
| **B-028** | **项目名称** | 🟡 **你定**（AI 给候选，不代决） |
| — | **BL-002** 165 条资料挂采用状态 | **建议补一轮资料采用**（不在本包，优先度最高） |

> ⚠️ **建议 ≠ 决定**。一行即可：`线程=④ B018=① B019=① B020=② B021=④ B022=① B023=锁定 B024=② B025=③`
