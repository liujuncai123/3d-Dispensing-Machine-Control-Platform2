# 待裁 · 精度分级到底是 **3 档** 还是 **4 档**？

> **一句话**：`AS-005`（假设台账）写「**4 档**（含 L0）」，`REQ-3D-010`（S0 需求基线）写「**3 档**…**无更低档**（用户裁：**不要 L0**）」—— **两处都是 2026-09-20 已裁，结论不同**。
> **状态**：🔴 **待用户裁决** ｜ **提出**：2026-09-21 ｜ **性质**：**两处已裁结论互相矛盾**（不是新需求）
> **发现人**：AI（主 Agent 自查 `AS-005` 与 `REQ-3D-010` 时实测；由回放 Q5 推荐"用 L2 档"牵出）

---

## 一、实测原文（**逐字对照**）

| 出处 | 原文（关键片段） |
|---|---|
| `../40-假设台账/假设台账.md` · `AS-005` | 「~~高低精度差异未定义~~ → **✅ 已裁（2026-09-20）：4 档** —— **L0 极简**(色块/**无胶路**/200ms) · L1 简化(包围盒/画胶路/100ms) · L2 完整(6部件/画胶路/50ms) · L3 分析(L2+预期vs实际对比/50ms)；**各档几何·胶路·刷新均可配置**」 |
| `../10-基线/S0-需求基线.md` · `REQ-3D-010` | 「**渲染精度分级（3 档）**…① L1 简化（**最低档**）：简化包围盒，画胶路，100ms…④ 默认：详情=L2、其余=L1；**无更低档**（用户裁：**不要 L0**）⑤ 每档的几何 / 胶路 / 刷新频率均可配置」 |

**核心冲突**：**L0 存不存在**
- `AS-005`：**存在**（L0 极简，色块、无胶路、200ms）
- `REQ-3D-010`：**不存在**（"最低档"=L1、"无更低档"、"用户裁：不要 L0"）

**顺带的不一致**：`AS-005` 的 L1 = 「包围盒 / 画胶路 / 100ms」与 `REQ-3D-010` 的 L1 一致；L2/L3 也一致 ⇒ **只有 L0 有无这一处冲突**。

---

## 二、为什么必须裁

| # | 理由 |
|---|---|
| 1 | 依 `00-流程/02-文件结构规范.md` **C3**（同类信息只有一个权威源，别处只引用）⇒ 两处都"完整写了一份"，**必然分叉**——本项就是已分叉的实例 |
| 2 | `REQ-3D-010` 是 **S0 已冻条目**（P1，演示用），`AS-005` 是**假设台账**上已裁的一条 ⇒ **谁让步，取决于用户** |
| 3 | 回放（`app/replay`）的渲染档位选择**直接依赖这个口径**（架构师建议 L2）⇒ 不裁清，S2 接口里回放那一栏写不出 |

---

## 三、选项（**只列，不替用户拍**）

| 选项 | 含义 | 连带动作 |
|---|---|---|
| **A · 以 `REQ-3D-010` 为准（3 档，无 L0）** | `AS-005` 台账改为"→ 见 `REQ-3D-010`（3 档）"，删掉自己的档位副本 | 台账只留值 + 引用（同 `CR-008` 的 C3 修法）；**无需开 S0 变更单** |
| **B · 以 `AS-005` 为准（4 档，有 L0）** | `REQ-3D-010` 改为 4 档 | ⚠️ 动 **S0 冻结条目** ⇒ **必须开 CR-S0-00X**，并重跑 S0 闸门 |
| **C · 先不动** | 只登记在此 | 回放渲染档在 S2 前必须再回来裁 |

> 🔑 **一句提示（供参考，不作决定）**：`REQ-3D-010` 的 ④ 写着「**无更低档**（**用户裁：不要 L0**）」—— 这句话把裁决权**明确归于用户**，且时间也是 2026-09-20 ⇒ **看起来 `REQ-3D-010` 是更晚/更明确的表述**；但**这不构成我可以替你定**的理由，仍需你点头。
