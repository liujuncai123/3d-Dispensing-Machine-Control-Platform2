# 裁决包 · 全量待裁与建议方案

> **一句话**：把当前**所有**未决问题集中在此，每项给「选项 + 客观代价 + **AI 建议**」；**建议不是决定**，状态保持 ⏳ 直到你点头。
> **用法**：末尾有汇总表，**一行回复即可**（例：`§4=A §5=B §6=C §7=B §8=确认 P002b=A G1=5 G2=400 轴=A B010=D B011=B B012=A B013=② B014=① B015=A`）
> **最后更新**：2026-09-19 ｜ **裁决人**：用户

---

## 一、S-1 剩余槽位（5 项）

### §4 · 项目定位

| 选项 | 做法 | 客观代价 |
|---|---|---|
| **A** | 主力项目（简历第一项目，投入最大） | 与你已投入的相匹配（已重建整套流程与结构、收集 165 条资料） |
| **B** | 次要项目 | 与既有投入不匹配，等于前期工作浪费 |

- **AI 建议**：**A**
- **依据**：事实层面你已是按主力项目在做

### §5 · 能力目标（哪几项主攻）

| 选项 | 主攻项 | 客观代价 |
|---|---|---|
| **A** | 3D 可视化 + 工业软件架构 + 完整工程化（仿真轻量） | 覆盖面均衡；但"多设备并发"只当普通功能 |
| **B** | 3D 可视化 + 工业软件架构 + **多设备并发管理**（工程化作贯穿要求，仿真轻量） | 与你简历里最硬的技术点（并发）对齐；差异化更强 |
| **C** | 四项全主攻 | 摊薄，单人难以做透 |

- **AI 建议**：**B**
- **依据**：§3 已定多台；而你自述里**并发是最强项**（SPSC 无锁队列 / 内存序等有真代码），"多设备并发 + 3D 孪生"的组合比"工程化"更能讲深

### §6 · 完成判据

| 选项 | 内容 | 客观代价 |
|---|---|---|
| **A** | 只"可演示" | 最快；但面试经不起深挖 |
| **B** | 可演示 + 可讲清 | 中等 |
| **C** | 可演示 + 可讲清 + **单元测试** | 需为核心模块写单测（**只需核心**：轨迹插值、校验规则、虚拟下位机时序、归正、存储） |

- **AI 建议**：**C**
- **依据**：单测只覆盖核心算法与接口，不追求覆盖率；有它才能说"工程化"

### §7 · 仿真边界（做到什么算够）

| 选项 | 内容 | 客观代价 |
|---|---|---|
| **A** | 只做几何 / 运动学（位置、速度），**不含误差** | 最省；但"误差→归正"这段闭环断掉 |
| **B** | + **固定幅度 + 随机扰动**的误差产生与上报 | 多一个小闭环；有算法可讲 |
| **C** | + 物理模型（加减速 / 惯量 / 振动） | 需真实参数，虚拟下位机拿不到 |

- **AI 建议**：**B**
- **依据**：对齐你说的「简单模拟也是一个完整闭环的重要的」

### §8 · 明确不做清单（确认版）

| 类别 | 清单 |
|---|---|
| **不做**（明确排除） | ① 基于物理量的寿命 / 磨损预测 ② 五轴 / 旋转轴 ③ 真实硬件对接 ④ 权限 / 主题 / 多语言 / 自定义布局 ⑤ 多平台（Linux / macOS）/ 安装包制作 |
| **预留**（有接口无实现） | ① 物理仿真 ② 预测性维护 ③ 真实 CCD 视觉 ④ PID / 自适应补偿 ⑤ DXF / G 代码 / PLT 导入 ⑥ OPC UA / SECS/GEM ⑦ 配方库 / 版本 / 审计 ⑧ MES / ERP 对接 |

| 选项 | 代价 |
|---|---|
| **A 确认此清单** | 范围清晰，可直接进 S0 |
| **B 增删后确认** | 需你指出增删项 |

- **AI 建议**：**A**（若同意，请说"§8 确认"）

---

## 二、流程参数（3 项）

### P-002-b · 实现期是否追加"运行结果"证据

| 选项 | 代价 |
|---|---|
| **A 是** | S4 单测与 S5 演示脚本本就要写 ⇒ **搭便车拿到唯一硬证据** |
| **B 否** | 省一点；但设计期之后就没有硬证据了 |

- **AI 建议**：**A**

### P-004-a · G1 单个切片的改动文件数上限

| 选项 | 3 / 5 / 8 |
|---|---|
| 代价 | 3 最稳但切片会很碎；8 快但 AI 一次会话难保多文件一致性 |

- **AI 建议**：**5**

### P-004-b · G2 单个切片的净增代码行数上限

| 选项 | 200 / 400 / 800 |
|---|---|
| 代价 | 200 最稳；800 风险高 |

- **AI 建议**：**400**
- ⚠️ **来源标注 = 推算（非实测）**，SLICE-00 完成后必须回来校准一次

---

## 三、范围与实现（6 项）

### B-001（补）· 轴的定义

| 选项 | 代价 |
|---|---|
| **A X / Y / Z 三轴直线**（无旋转轴） | 运动学最简单，符合"3 轴"字面 |
| **B 含 1 个旋转轴** | 需四轴插补与旋转变换，复杂度陡增；且与"3 轴"字面冲突 |

- **AI 建议**：**A**（此前填的是未定默认，请确认）

### B-010 · 通信协议

| 选项 | 做法 | 客观代价 |
|---|---|---|
| **A** | 真上 Modbus TCP 完整栈 | 最"真实"可讲；工作量大（需实现或引第三方库） |
| **B** | Modbus RTU | 需串口，虚拟环境下不如 TCP 方便 |
| **C** | 自定轻量帧 | 最省；但面试讲不出行业标准 |
| **D** | **对外 Modbus TCP 语义 + 内部抽象层 + 当前用简化实现** | 架构留足、可讲行业标准，实现成本接近 C |

- **AI 建议**：**D**
- **依据**：`80-资料/03` 方向 3 —— 点胶设备普遍支持 Modbus RTU/TCP；HMI 刷新建议 ≤50ms（可作量化参考）

### B-011 · 工程运行细节砍到什么程度

| 选项 | 代价 |
|---|---|
| **A** 只保留运行控制 8 项，工艺与设置全砍 | 最省；但违反你说的"工艺不能没有" |
| **B** 运行控制 8 项 + **工艺参数最小 5 项**（不做配方库） | 符合"简化但不能没有" |
| **C** 保留较完整（含配方库 / 设置树） | 工作量过大 |

- **AI 建议**：**B**

### B-012 · 虚拟下位机是否必须具备

| 选项 | 代价 |
|---|---|
| **A 必备** | 无真实硬件 ⇒ 必须有被控对象；且是 §2 的 T3（误差模拟）的前提 |
| **B 不必备** | 直接喂数据；但闭环断掉，且与 §2 已选 T3 冲突 |

- **AI 建议**：**A**（**逻辑上已被 §2 决定** —— 选了 T3 就必然要虚拟下位机）

### B-013 · 多设备的 3D 呈现方式

| 选项 | 做法 | 客观代价 |
|---|---|---|
| **①** 单场景多实例（网格布局） | 视觉冲击最强（"同时看多台"） | 渲染与状态映射成本 ×N |
| **②** 单实例 + 切换 | 最省 | 丢掉"同时看多台"的观感 |
| **③** 总览缩略 + 详情 | 折中 | 多一套缩略渲染逻辑 |

- **AI 建议**：**② 起步**，**③ 留作预留/Backlog**
- **依据**：符合 P-3（重活只预留）——先跑通，再谈观感

### B-014 · Y 轴归属

| 选项 | 代价 |
|---|---|
| **① 工作台沿 Y 移动**（龙门固定） | 几何更简单（一块板在动）；点胶机常见形态 |
| **② 龙门沿 Y 移动**（工作台固定） | 更像"大行程龙门"；立柱需在导轨上跑 |

- **AI 建议**：**①**（差异很小，只影响几何变换挂在哪个节点）

---

## 四、新增（由你"安全有保证"引出，1 项）

### B-015 · 安全边界（**属 S1，不阻塞立项**）

| 选项 | 内容 | 客观代价 |
|---|---|---|
| **A** | **急停本地优先**（不依赖通信往返）+ **掉线超时即停** + **批量下发二次确认** | 增加少量状态机与超时逻辑；但这是"控制类软件"最该讲的点 |
| **B** | 只做基础（急停经通信下发、掉线仅提示） | 省；但"安全有保证"不成立，且面试容易被追问穿 |

- **AI 建议**：**A**
- **依据**：你 2026-09-19 原话「架构必须优秀而且**安全有保证**」

---

## 五、汇总表（照此一行回复即可）

| 编号 | 待裁项 | **AI 建议** | 你的裁决 |
|---|---|---|---|
| §4 | 项目定位 | **A 主力** | ___ |
| §5 | 能力目标 | **B（含多设备并发）** | ___ |
| §6 | 完成判据 | **C（三项全纳）** | ___ |
| §7 | 仿真边界 | **B（固定+随机误差）** | ___ |
| §8 | 不做 / 预留清单 | **A 确认** | ___ |
| P-002-b | 追加运行结果证据 | **A 是** | ___ |
| P-004-a | G1 文件数上限 | **5** | ___ |
| P-004-b | G2 行数上限 | **400** | ___ |
| B-001(补) | 轴定义 | **A XYZ 直线** | ___ |
| B-010 | 通信协议 | **D（Modbus TCP 语义 + 抽象简化实现）** | ___ |
| B-011 | 工程细节 | **B（8 项控制 + 工艺最小 5 项）** | ___ |
| B-012 | 虚拟下位机 | **A 必备** | ___ |
| B-013 | 多设备 3D 呈现 | **② 起步（③ 预留）** | ___ |
| B-014 | Y 轴归属 | **① 工作台移动** | ___ |
| B-015 | 安全边界 | **A（急停本地优先等）** | ___ |

> ⚠️ **所有建议均非决定**。你回复后我才写入 `../60-决策记录/` 并把状态改为 ✅。
> 若某条你想改，直接写编号 + 你的值即可；**不写的按建议值执行**（会标注为"未定默认"，仍可随时推翻）。
