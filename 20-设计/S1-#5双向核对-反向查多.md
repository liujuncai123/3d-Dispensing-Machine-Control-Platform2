# S1 `#5` 依赖图 · 双向核对 —— 反向（查多）+ 出处真实性

> **本文件装什么**：`#5 依赖图`第三步「双向核对」的反向部分。逐条核验 `S1-依赖边表.txt` 的 **64 条边**，回答两件事：① 边的「出处」是否真的支持这条边（出处真实性，重点防「§17.5 动作错挂」式伪通过）；② 每条边能否指出它落在哪个场景（查多，说不出⇒记「多边」）。
> **被核对象**：`20-设计/S1-依赖边表.txt`（64 条边：A 层 48 / B 层 7 / D 层 9；31 节点）。
> **对照物**：`20-设计/S1-关键场景清单.md`（8 个场景 SC1~SC8）。
> **比对锚点**：`20-设计/S1-架构基线草稿.md`（§四 需求→模块映射 127 条 · §五 数据所有权表 · §七 配置 · §14.2 外部实体 · §17.5 数据流 · §17.2 决策 17 · 决策 1/2）、`20-设计/S1-#5依赖图方法-定稿.md`（§五 禁边 · §七 核对规则）。
> **日期**：2026-09-21 ｜ **审计人**：dep-audit-backward ｜ **立场**：只核验、不改任何既有文件、不 git 提交。
> **口径**：非模块节点（`main`/`qt`/`vtk`/`E1~E7`）依任务指示放宽；模块间边（`infra/*` 等）不享受放宽。反向覆盖率 = 能归到至少一个场景的边数 / 64。

---

## 表一：64 条边逐条判定表

> 列说明：**出处是否支持** = 去读该边引用的原文后，原文是否真能推出此边（含方向）；**归场景** = 该边出现在哪个（些）场景，或「多边（无 SC）」；**判定** = 通过 / 多边 / 出处失真。
> 引用均带源文件路径 + 原文片段（遵守「禁用行号」纪律）。

### A 层（使用期依赖，48 条）

| # | 边 | 出处原文是否支持 | 归到哪个场景 | 判定 |
|---|---|---|---|---|
| A1 | `main -> infra/config` | 支持。`S1-架构基线草稿.md` §七 7.2.1②：「由启动装配点（`main`/`app` 层）把解析结果注入各模块」「`infra/config` 只负责：加载 → 校验」。main 作组合根加载配置。 | SC1（冷启动·装配期） | 通过 |
| A2 | `main -> core/device` | 支持。同上 7.2.1②「装配点注入」；§七 配置项「设备容量上限 \| `core/device`」为读取方，main 注入。 | SC1 | 通过 |
| A3 | `main -> core/execution` | 支持。同上；§七「哪些设置算重要 \| `core/execution` + `app/dialogs`」为读取方。 | SC1 | 通过 |
| A4 | `main -> core/concurrency` | 支持。§七「状态/坐标刷新周期 \| `core/concurrency`」为读取方。 | SC1 | 通过 |
| A5 | `main -> core/alarm` | 支持。§七「误差超阈值 \| `core/alarm`」为读取方。 | SC1 | 通过 |
| A6 | `main -> core/error` | 支持。§七「误差幅度/分布 \| `core/error`」为读取方。 | SC1 | 通过 |
| A7 | `main -> app/scene3d` | 支持。§七「3D 渲染 UI 刷新率 \| `app/scene3d`」「渲染精度分档 \| `app/scene3d`」为读取方。 | SC1 | 通过 |
| A8 | `main -> app/dialogs` | 支持。§七「防误点秒数/确认按钮延时 \| `app/dialogs`」为读取方。 | SC1 | 通过 |
| A9 | `main -> comm/interface` | 支持。§七「掉线判定阈值 \| `comm/interface`」为读取方。 | SC1 | 通过 |
| A10 | `main -> data/log` | 支持。§七「日志保留量 \| `data/log`」为读取方。 | SC1 | 通过 |
| A11 | `app/main_window -> core/device` | 支持。`S1-架构基线草稿.md` §五 数据所有权「设备状态（8 态）」只读者含 `app/*`；`app/main_window` 属 UI 层，读设备状态显示。另 §四 `REQ-DEV-001/002`（设备列表/在线离线）落 `core/device`。 | SC1 | 通过 |
| A12 | `app/main_window -> qt` | 支持。`S1-架构基线草稿.md` §17.2 决策 1「界面框架 **Qt**」（用户定）；`app/main_window` 为主框架窗口。 | SC1 | 通过 |
| A13 | `app/scene3d -> core/device` | 支持。§五「设备状态」只读者含 `app/*`；scene3d 读设备实时坐标做 3D 映射。 | SC3 / SC8 | 通过 |
| A14 | `app/scene3d -> core/mapping` | 支持。§五「坐标→几何映射参数」持有者 `core/mapping`、只读者 `app/scene3d`（只读取结果画）。 | SC3 / SC8 | 通过 |
| A15 | `app/scene3d -> core/program` | 支持。§五「程序/轨迹」只读者含 `app/scene3d`；§四 `REQ-3D-006` 编程轨迹渲染落 `app/scene3d`。 | SC1 / SC3 | 通过 |
| A16 | `app/scene3d -> vtk` | 支持。§17.2 决策 2「3D 渲染 **只用 VTK**」（`P-006` 用户原话）；scene3d 依赖 VTK 渲染。 | SC3 / SC8 | 通过 |
| A17 | `app/scene3d -> qt` | 支持。决策 1「界面框架 Qt」；scene3d 承载于 Qt 窗口。 | SC3 / SC8 | 通过 |
| A18 | `app/dashboard -> core/device` | 支持。§五「设备状态」只读者含 `app/*`；dashboard 显示设备状态/总览。 | SC5 / SC8 | 通过 |
| A19 | `app/dashboard -> core/alarm` | 支持。§五「报警」只读者含 `app/dashboard`。 | SC5 | 通过 |
| A20 | `app/dashboard -> core/stats` | 支持。§四 `REQ-MON-002`「产量计数 \| `core/stats`」、`REQ-MON-004`「统一设备数据表 \| `app/dashboard`」；dashboard 读 stats 呈总览。 | SC5 | 通过 |
| A21 | `app/dashboard -> data/store` | 支持。§四 `REQ-MON-003`「数据可导出 \| `app/dashboard`」；导出需读 `data/store`。 | SC5（横切） | 通过 |
| A22 | `app/dashboard -> qt` | 支持。决策 1「界面框架 Qt」。 | SC5 | 通过 |
| A23 | `app/dialogs -> core/execution` | 支持。§四 `REQ-RUN-010`「运行前强制确认（作业前点检）\| `app/dialogs`（确认 UI）+ `core/execution`」；dialogs 确认后驱动 execution。 | SC1 / SC2 | 通过 |
| A24 | `app/dialogs -> core/device` | 支持。§四 `REQ-DEV-005`「批量下发需二次确认 \| `core/device`」；dialogs 触发 device 批量下发。 | SC2 | 通过 |
| A25 | `app/dashboard -> comm/interface` | 支持。§四 `REQ-COM-003`「连接状态可见 \| `comm/interface` + `app/dashboard`」；dashboard 读 comm/interface 连状态。 | SC1 / SC7 | 通过 |
| A26 | `app/dialogs -> qt` | 支持。决策 1「界面框架 Qt」。 | SC2 | 通过 |
| A27 | `core/device -> core/statemachine` | 支持。§四 `REQ-STA-001`「状态集合明确 \| `core/statemachine`」；device 持有设备状态（8 态），需引用 statemachine 定义的状态集合。 | SC1 / SC6 | 通过 |
| A28 | `core/execution -> core/device` | 支持。§五「设备清单/参数」只读者含 `core/execution`；execution 读设备参数执行。 | SC3 | 通过 |
| A29 | `core/execution -> core/program` | 支持。§五「程序/轨迹」只读者含 `core/execution`；execution 读程序运行。 | SC3 | 通过 |
| A30 | `core/execution -> core/statemachine` | 支持。§四 `REQ-RUN-004`「急停（本地优先）\| `core/statemachine`（急停迁移）+ `core/execution`」；execution 驱 statemachine 急停迁移。 | SC6 | 通过 |
| A31 | `core/execution -> data/log` | 支持。§五 硬约束②「`data/log` 是唯一写者（其它模块经接口调用）」；execution 经接口写执行日志（`REQ-LOG-006`）。 | SC3（横切） | 通过 |
| A32 | `core/execution -> data/store` | 支持。§四 `REQ-DB-001`「四类数据可持久化 \| `data/store`」；§五「记录」可写者 `core/*`（经接口）。 | SC3 | 通过 |
| A33 | `core/concurrency -> core/device` | 支持。§五「设备状态」可写者 `core/concurrency`（只“投递”不持有）；§5.1 硬约束①「`core/concurrency` 只投递不持有」。concurrency 把汇聚到的状态投递回 device。 | SC3 | 通过 |
| A34 | `core/device -> core/concurrency` | 支持。`S1-架构基线草稿.md` §17.5 数据流：「`core/device`（逐台下发）→ `core/concurrency`（投递）」。第一跳方向 device→concurrency 与边一致。 | SC2 | 通过 |
| A35 | `core/device -> infra/eventbus` | 支持。§17.5 数据流：「`core/device`（★落成唯一事实源）→ `infra/eventbus`」。设备状态变更经事件总线广播。 | SC3 / SC5 | 通过 |
| A36 | `core/concurrency -> comm/interface` | ⚠ **出处短语失真**（边本身在 §17.5，但标注短语不对向）。§17.5 数据流含两跳同两节点：「`core/concurrency`（投递）→ `comm/interface` → E2」（**下发**，方向=本边）与「`comm/interface` → `core/concurrency`（汇聚）」（**汇聚**，方向=反）。边标注「§17.5:状态汇聚」指向的是**汇聚跳（反方向）**；本边（concurrency→comm/interface）对应的是 §17.5 的「下发投递」跳。出处来源含此边，但**标注短语与方向不一致**。 | SC2 / SC3（下发） | **出处失真** |
| A37 | `core/concurrency -> data/log` | 支持。§四 `REQ-LOG-005`「通信日志 \| `data/log`」；concurrency 经接口写通信日志。 | SC2 / SC7 | 通过 |
| A38 | `core/error -> core/device` | 支持。§五「设备状态」只读者含 `core/error`（归正需读实际位置）。 | SC4 | 通过 |
| A39 | `core/error -> core/vision` | 支持。§四 `REQ-VIS-003`「对位/测高结果进入误差链路 \| `core/vision`」；error 用 vision 结果算误差。 | SC4 | 通过 |
| A40 | `core/error -> data/log` | 支持。§四 `REQ-LOG-007`「误差日志（偏差可追溯）\| `data/log`」。 | SC4 | 通过 |
| A41 | `core/error -> data/store` | 支持。§五「记录（含误差日志）」可写者 `core/*`（经接口）；error 存误差记录。 | SC4 | 通过 |
| A42 | `core/alarm -> core/device` | 支持。§五「设备状态」只读者含 `core/alarm`；alarm 读设备状态判定报警。 | SC5 | 通过 |
| A43 | `core/alarm -> core/execution` | 支持。§四 `REQ-ALM-004`「报警可触发停机 \| `core/alarm`」；alarm 驱 execution 停机。 | SC5 / SC6 | 通过 |
| A44 | `core/alarm -> core/error` | 支持。§五「误差/修正量」只读者含 `core/alarm`；alarm 读误差判断超阈值。 | SC5 | 通过 |
| A45 | `core/alarm -> data/log` | 支持。§五「报警」记录经 `data/log`（唯一写者，§5.1 ②）；另 `REQ-ALM-*` 判定落 `core/alarm`、日志落 `data/log`。 | SC5 | 通过 |
| A46 | `core/stats -> core/device` | 支持。§五「设备状态」只读者含 `core/stats`；stats 读设备状态算产量/稼动率。 | SC5（横切） | 通过 |
| A47 | `comm/modbus -> comm/interface` | 支持。§四 `REQ-COM-002`「对外语义为 Modbus TCP \| `comm/modbus`」；§17.5/§17.1 主干④「`comm/modbus` 只是 `comm/interface` 的一个实现」。modbus 依赖抽象层。 | SC1 / SC2 / SC7 | 通过 |
| A48 | `data/store -> infra/lifecycle` | 支持。§六 ARCH-007「中断恢复 \| `infra/lifecycle` + `data/store`（快照/恢复点字段）」；边表头注「依赖倒置：接口定义在 `infra/lifecycle`，`data/store` 实现」。 | SC7（掉线恢复） | 通过 |

### B 层（事件通道，7 条）

| # | 边 | 出处原文是否支持 | 归到哪个场景 | 判定 |
|---|---|---|---|---|
| B1 | `core/device -> app/scene3d` | 支持（事件通道表示）。§17.2 决策 17「模块间通知走 `infra/eventbus`」；§17.5「`core/device` → `infra/eventbus` → `app/scene3d`（画）」。方法 §五 明确「B 层 `core → app` 合法（事件即向上通知）」。场景「状态变更→3D 刷新」存在。 | SC3 / SC8 | 通过 |
| B2 | `core/device -> app/dashboard` | 支持（事件通道）。§17.5「`core/device` → `infra/eventbus` → `app/dashboard`（显示表）」；决策 17。场景「状态变更→总览」存在。 | SC5 / SC8 | 通过 |
| B3 | `core/alarm -> app/dashboard` | 支持（事件通道）。§17.5「`core/alarm`（超阈值告警）→ `data/log`（留痕）」且 `app/dashboard` 呈现报警（§四 `REQ-ALM-001/002` 落 `app/dashboard`）；决策 17。 | SC5 | 通过 |
| B4 | `core/error -> core/alarm` | 支持（事件通道）。§四 `REQ-ERR-006`「误差超阈值→告警+记录 \| `core/error`」；§17.5「`core/device` → `core/error`（算偏差）→ `core/alarm`（超阈值告警）」。 | SC4 / SC5 | 通过 |
| B5 | `core/device -> core/error` | 支持（事件通道）。§17.5 数据流：「`core/device` → `core/error`（算偏差）」。状态变更触发偏差计算。 | SC4 | 通过 |
| B6 | `infra/config -> infra/eventbus` | 出处本身真实（§七 7.2.1④「配置变更通知走 `infra/eventbus`」），但 **8 个运行期场景无一覆盖「运行期改配置」**。见下表二。 | **多边（无 SC）** | 多边 |
| B7 | `core/device -> infra/eventbus` | 出处真实（§七 7.2.1④ 订阅配置变更）；device 订阅配置变更通知。但 **8 个场景无「运行期改配置」**，且设备参数多为构造注入（§五 设备清单/参数「初始化由 `infra/config`」），device 是否需订阅存疑。无场景覆盖。见下表二。 | **多边（无 SC）** | 多边 |

### D 层（协议契约，9 条）

| # | 边 | 出处原文是否支持 | 归到哪个场景 | 判定 |
|---|---|---|---|---|
| D1 | `comm/interface -> apps/virtual-device` | 支持。`S1-架构基线草稿.md` §14.2 E2「虚拟下位机… → 下发程序与指令… 协议 Modbus TCP」。comm/interface 向虚拟下位机下发。 | SC1 / SC2 / SC3 / SC7 | 通过 |
| D2 | `apps/virtual-device -> comm/interface` | 支持。§14.2 E2「← 上报实时坐标/状态/误差/IO」。虚拟下位机经抽象层上报。 | SC3 / SC4 / SC7 | 通过 |
| D3 | `app/main_window -> E1` | 支持。§14.2 E1「操作员… 操作：编程/校验/下发… 图形界面（双向）」。main_window 是操作员图形界面入口。 | SC1 | 通过 |
| D4 | `data/store -> E4` | 支持。§14.2 E4「本机文件系统… 读写：…SQLite 库」。store 落 SQLite。 | SC3（横切） | 通过 |
| D5 | `data/log -> E4` | 支持。§14.2 E4「读写：…日志」；`data/log` 写日志文件。 | 横切（全 SC） | 通过 |
| D6 | `infra/config -> E4` | 支持。§14.2 E4「读：配置 JSON」；`infra/config` 读配置文件。 | SC1（装配期） | 通过 |
| D7 | `data/log -> E5` | 支持。§14.2 E5「系统时钟… 取时间戳（日志/记录/趋势）」；`data/log` 取时间戳。日志在全部场景横切写入，故时间依赖贯穿 SC1~SC8。 | 横切（全 SC） | 通过 |
| D8 | `infra/build -> E6` | 出处真实（§14.2 E6「构建/开发环境… vcpkg/CMake/VS2022… 单向」），但 **构建是开发期活动，8 个运行期行为场景（SC1~SC8）无一覆盖「构建」**。见下表二。 | **多边（无 SC）** | 多边 |
| D9 | `core/alarm -> E7` | 支持。§14.2 E7「报警声光输出… 第一次确认时『关声光』\| `REQ-ALM-002` 判据①」；alarm 触发声光，首次确认关声光。 | SC5 | 通过 |

---

## 表二：多边清单（边表里有、但没有任何场景需要它）

> 口径：严格按任务「每条边都要回答落在哪个场景；说不出的⇒多边」。以下边均为**模块间/真实依赖**，不享受非模块节点放宽；其出处本身真实（非伪造），但 8 个运行期场景 SC1~SC8 均不触发它。归类为 **I（改进/不阻塞）**——它们反映的是「8 场景对照物对 构建期 / 配置变更 两类活动覆盖不足」，而非边本身错误；建议团队二选一：补场景或明文认定这两类活动不进运行期场景对照。

| # | 边 | 为什么没有场景需要它 | 归类 |
|---|---|---|---|
| B6 | `infra/config -> infra/eventbus`（B pub，配置变更通知） | §七 7.2.1④ 规定配置变更经 eventbus 广播，此为真实机制；但 SC1~SC8 皆为运行期端到端行为，**无任一场景在运行期修改配置**（SC1 冷启动是 main 构造注入，非运行期变更广播）。该边只在「运行期改配置」时才被需要，而该活动未被 8 场景之一覆盖。 | I |
| B7 | `core/device -> infra/eventbus`（B sub，订阅配置变更） | 同上，device 订阅配置变更通知；但 8 场景无运行期改配置，且 §五「设备清单/参数」注明「初始化由 `infra/config`」（构造注入、非运行期可变）。device 是否需在运行期订阅配置变更存疑——目前无任何场景证明此订阅被消费。 | I |
| D8 | `infra/build -> E6`（D protocol，构建环境） | §14.2 E6 是真实外部实体（构建/开发环境）；但**构建是开发期活动**，SC1~SC8 是运行期行为场景，无一覆盖「构建」。该边合法但无运行期场景可挂靠。 | I |

> 备注：**D7 `data/log -> E5`（时间戳）** 初看亦无专属场景，但日志在全部场景横切写入（场景清单 §三 明示「日志/统计 横切，SC1~SC8 每一跳都产生日志」），时间戳依赖贯穿所有场景，故**不计入多边**，判通过。

---

## 表三：出处失真清单（出处原文推不出这条边 / 措辞被曲解）

> 口径：重点防任务警示的「§17.5 动作错挂」式伪通过。逐条比对后，**未发现「编造不存在的出处」类失真**（64 条出处均可定位到 §五/§17.5/§7.2.1/§四/§14.2/§六/决策 的真实原文）。发现 **1 条出处短语与边方向不一致**（属于「措辞曲解」子类，C 类不阻塞）。

| # | 边 | 出处原文 | 为什么不支持 / 措辞偏差 | 归类 |
|---|---|---|---|---|
| A36 | `core/concurrency -> comm/interface`（A usage，标注 `§17.5:状态汇聚`） | `S1-架构基线草稿.md` §17.5 数据流含两跳：`core/concurrency`（投递）→ `comm/interface` → E2（**下发**，方向=本边）；E2 → … → `comm/interface` → `core/concurrency`（**汇聚**，方向=反）。 | 边标注「状态汇聚」指向的是**汇聚跳（comm/interface → core/concurrency，反方向）**，并非本边方向。本边（concurrency→comm/interface 下发）确实存在于 §17.5，但**标注短语与方向不一致**——与已知前科「把 §17.5 某节点动作错挂到另一节点」同源。建议改标注为「§17.5:下发投递(第二跳)」以消除歧义。边本身非多余、方向正确，故仅出处短语失真。 | C（不阻塞） |

---

## 收敛小结

1. **64 条判定分布**：**通过 60 条** · **多边 3 条**（B6、B7、D8）· **出处失真 1 条**（A36，仅短语层级，边本身成立）。无任何「出处编造不出来源」类硬失真。
2. **多边 3 条**：均为「出处真实、但 8 个运行期场景不覆盖」——B6/B7（运行期配置变更通知）、D8（构建期依赖）。归类 I，不阻塞；反映对照物对「构建期 / 配置变更」两类活动覆盖缺口，建议补场景或明文豁免。
3. **出处失真 1 条**：A36 的标注短语「状态汇聚」错指 §17.5 的反方向汇聚跳；边与方向本身正确，仅出处措辞需改。C 类不阻塞。
4. **反向覆盖率 = 能归到至少一个场景的边数 / 64 = (64 − 3) / 64 = 61 / 64 ≈ 95.3%**（A36 虽出处短语失真，但边落在 SC2/SC3 下发，计入「能归场景」；3 条多边不计入）。

> 反向查多只报「多边 / 出处失真」，未顺手报漏边（漏边属正向 / 另一负责人范围）。本报告未改动 `S1-依赖边表.txt` 或任何其它文件。

---

## 收尾：check 脚本退出码

```
python 00-流程/check-deps.py      退出码 = 0
  （共 64 条边：A 48 / B 7 / D 9；① A 层无环 ② 无禁边 ③ 端点合法 ④ 出处无行号）

python 00-流程/check-consistency.py  退出码 = 0
  （共 23 项检查，失败 0 项；覆盖 S-1/S0 基线计数/引用/版本/残留）
```
