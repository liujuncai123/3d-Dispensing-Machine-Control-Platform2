# S1 收口评估 · 离冻结还差什么

> **一句话结论**：S1 的 **19 项任务**里 —— **已完成 3 项** · **已产出只差你裁 3 项**（`#4`/`#5`/`#12`）· **未产出 13 项**。
> 而**真正拦住冻结的不是那 13 项**，是：**一个"12 项待设计"的结**（`§十二`，`#8` 的落点）+ **两处状态自相矛盾** + **`P-6` 的硬闸门**。
> **建立日期**：2026-09-21 ｜ **产出人**：AI（主理人自查）｜ **依据**：`./S1-架构基线草稿.md` 逐节实测

---

## 一、19 项任务逐项盘点（**结论表**）

| # | 任务 | 状态 | 落点 / 依据 | 差什么 |
|---|---|---|---|---|
| 1~3 | 分层 · 需求归位 · 模块划分（25） | ✅ **已完成** | `§三` ~ `§十`（8 项产出） | ⚠️ 见 **结-2**（`A3-补` 状态自相矛盾） |
| **4** | **上下文视图** | 🟡 **已产出 · 待裁** | `§十四`（14.1~14.6：产出物 3 件 / 外部实体 7 个 / 预留外部实体 / 边界内不负责 / 3 条硬约束 / ISO 23247 四域对照） | **只差你裁决** |
| **5** | **构建块视图** | 🟡 **产出齐备 · 待认** | 方法定稿 + `S1-依赖边表.txt`(**87 条** A70/B8/D9) + `S1-关键场景清单.md`(9 场景) + **`S1-接口清单.md`（25 模块 / 60 接口）** | **你确认判据达成** → 更新 `§1.0.1` 状态 |
| **6** | 运行时视图 | 🔴 **未产出（有素材）** | 素材：`S1-#5循环第2批-轮11至20-并发与运行时.md` 的 **表三**（模块×线程归属）+ **表四**（跨线程边清单） | 未成文；且素材写的是 **24 模块**（现 **25**） |
| 7 | 部署视图 | 🔴 未产出 | 无 | 从零 |
| **8** | **横切机制方案（12 项）** | 🔴 **未产出（被 `§十二` 挡住）** | `§十二` 就是它的落点 ⇒ **12 项待设计** | **先解 `§十二` 的 12 项** |
| **12** | **解决方案策略** | 🟡 **已产出 · 待裁** | `§十七`（17.1~17.6；`#19`/`#20` **已并入** 17.3/17.4） | **只差你裁决** |
| 13 | 架构决策记录 ADR | 🔴 未产出 | 无 | 从零（但 `§17.2` 的 **17 条决策**可直接转为 ADR 素材） |
| 14 | 性能预算分配 | 🔴 未产出 | 无（`§十二` A7 已定「状态 ≤50ms / UI ≥10Hz」） | 从零（有上游口径） |
| 15 | NFR → 达成手段表 | 🔴 未产出 | `§17.6` 是**骨架**，正文**明确留给 `#15`** | 从零 |
| **9** | **状态机实现结构** | 🔴 **未产出** | ⚠️ **`P-6` 硬要求**（见 结-3） | 从零 |
| 10 | 文件清单 | 🔴 未产出 | 可复用 `S1-接口清单.md` 的文件头清单 | 从零（有素材） |
| 11 | 任务分解 | 🔴 未产出 | 无 | 从零 |
| 16 | 技术债清单 | 🔴 未产出 | 素材：各处 `I` 类发现（含 `§1.0.1` 刚登记的"任务数无单命令可复现"） | **汇总即可** |
| **17** | **关键场景走查** | 🔴 未产出（**对照物已备**） | `S1-关键场景清单.md`（**9 场景**，`#5` 与 `#17` 共用） | 逐场景走查 |
| 18 | 构建组织 | 🔴 **未产出（有素材）** | 素材：`S1-#5循环第2批-轮1至10-工程与构建.md` | 未成文 |
| 19 | 技术选型理由 | ✅ **并入 `#12`** → `§17.3` | — | 随 `#12` 一起裁 |
| 20 | 权衡点清单 | ✅ **并入 `#12`** → `§17.4` | — | 随 `#12` 一起裁 |
| 21 | 操作流程设计 | 🔴 未产出 | 无 | 从零 |

**计数核验**：已完成 **3**（`#1~#3`）+ 已产出待裁 **3**（`#4`/`#5`/`#12`）+ 并入 **2**（`#19`/`#20`）+ 未产出 **13**（`#6`/`#7`/`#8`/`#9`/`#10`/`#11`/`#13`/`#14`/`#15`/`#16`/`#17`/`#18`/`#21`）⇒ 3+3+2+13 = **21** − 2（已并入）= **19** ✅

---

## 二、🔴 拦住冻结的三个"结"（**比那 13 项更要紧**）

### 结-1 · `§十二` 的 **12 项待设计**（`#8` 的落点）

实测 `§12.3 关注点清单最终统计`：

| 类 | 项数 | 已定 | **待设计** | 待确认/不做 |
|---|---|---|---|---|
| A 运行期 | 12 | 5 | **7** | 0 |
| B 数据 | 8 | 4 | **3** | 1 |
| C 开发期 | 7 | 4 | **2** | 1 |
| D 演进 | 5 | 3 | 0 | 2 |
| **合计** | **32** | **16** | **12** | **4** |

⚠️ **其中 `A11`（急停插队）与 `P-5` 安全优先直接相关** ⇒ 属**安全链**，不宜拖到最后。
⇒ `#8 横切机制方案（12 项）` **不能凭空写**，它必须先有这 12 项的设计。

### 结-2 · 两处**状态自相矛盾**

| # | 矛盾 | 实测 |
|---|---|---|
| 1 | `§十一` 的 `A3-补` 写「**A3 的 6 项产出补全 🔴 待做**」，但 `§1.0.1` 说 `#3` 已完成 | 实测：6 项里 **5 项有落点**（`§四` 映射表 / `§五` 所有权表 / `§七` 配置项清单 / `§八` 约束清单 / `§九` 风险模块），**缺「契约分界清单」**（该词全文**只出现在 `A3-补` 那一行**）⇒ **`A3-补` 状态过期**，但**确实还差 1 项** |
| 2 | **`§十五` 章节号空号** | 章节从 `## 十四` **直接跳到** `## 十六` ⇒ 要么补节、要么显式说明为何跳过（否则引用 `§十五` 会指空） |

### 结-3 · `P-6` 的**硬闸门**（不是普通任务）

`00-流程/01-开发流程.md` 的 **`P-6`** 原文：
> 「① **S1 必须产出独立的状态机设计与线程架构设计**，二者**冻结后方可进 S2**」

⇒ **`#9`（状态机实现结构）+ `#6`（运行时视图 = 线程架构）是 `P-6` 的硬闸门**，**不是"可以排后面"的普通任务**。
⇒ 这两个不产出，**S2 无论 S1 其余项做没做完都进不去**。

---

## 三、建议的收口顺序（**三批**）

| 批 | 内容 | 为什么这个顺序 |
|---|---|---|
| **批 1 · 解锁主架构** | 你裁 `#4` / `#5` / `#12` | 三项**产出已有**，只差裁决 ⇒ **S1 的"主架构"（`#4`+`#12`+`#5`）当场立起来**，成本最低、收益最大 |
| **批 2 · 解两个结** | ① `§十二` 的 **12 项待设计** → 产出 `#8`（**`A11` 安全项优先**）② 修 `A3-补` 状态 + 补「契约分界清单」③ 说明 `§十五` 空号 | 不解 `§十二`，`#8` 写不出；状态矛盾会让后续引用踩空 |
| **批 3 · 补 `P-6` 与其余** | **先 `#6` + `#9`**（`P-6` 硬闸门）→ 再 `#7`/`#10`/`#11`/`#13`/`#14`/`#15`/`#16`/`#17`/`#18`/`#21` | `P-6` 是**进 S2 的硬条件**；其余可穿插 |

**可复用素材（别从零写）**：
- `#6` 运行时视图 ← `S1-#5循环第2批-轮11至20-并发与运行时.md` 的表三/表四（**注意模块数要更到 25**）
- `#18` 构建组织 ← `S1-#5循环第2批-轮1至10-工程与构建.md`
- `#17` 关键场景走查 ← `S1-关键场景清单.md`（9 场景已备）
- `#13` ADR ← `§17.2` 的 **17 条决策**
- `#10` 文件清单 ← `S1-接口清单.md` 各模块的文件头
- `#16` 技术债 ← 各处 `I` 类发现汇总

---

## 四、机器核验（收尾，实测）

```
python 00-流程/check-deps.py        ⇒ 退出码 0（87 条边 A70/B8/D9 · 32 节点）
python 00-流程/check-consistency.py ⇒ 退出码 0（23 项检查，失败 0）
```

---

## 五、⚠️ 本文件的诚实声明

- 本文件是**盘点**，**不含任何替用户做的决定** —— 三批顺序是**建议**，拍板权在用户。
- 「已完成 / 待裁 / 未产出」的判定**逐条附了落点或实测依据**（节号、文件、命令输出），未凭印象。
- 本文件**未修改任何既有文件**。
---

## 六、回放（**预留点**）的落点清单 —— 逐处实测

> 用户问「回放模块是否写入文档、作为预留点」。实测结论：**已写入，落了 6 处**；但**有 1 处缺口**（见 6.2）。

### 6.1 已落地的 6 处

| # | 落点 | 实测证据 |
|---|---|---|
| 1 | **S-1 预留清单第十项** | `S-1 §四` 预留清单 9 → **10 项**，第十项 = 「**历史数据回放**（按时间轴重演历史时段；**独立模块**，只留接口不实现 —— `CR-009`）」 |
| 2 | **S0 需求条目** | 新增 **`REQ-ARCH-009`**，判据含「④ **不实现**真正的数据回放」 |
| 3 | **S1 模块清单** | `§16.1` UI 层 4 → **5**，新增 **`app/replay`**（`CR-010`）；模块总数 24 → **25** |
| 4 | **S1 依赖边表** | 新增 **9 条回放边**（A 层 7 + B 层 2），边数 78 → **87** |
| 5 | **S1 接口清单** | `app/replay` 一节 **5 个 `I*` 接口**：`IReplaySource` · `IReplayFrameReader` · `IReplayClock` · `IReplaySink` · `IReplayPlayer`（与《`S1-回放-独立模块接口预留.md`》**同一批接口名**） |
| 6 | **决策记录** | `60-决策记录/` 已留档（用户原话「回放给个预留接口，而且回放应该是一个比较独立的功能」） |

**附加产出**：`20-设计/S1-回放-独立模块接口预留.md`（接口 + 依赖边 + 禁边 + 6 条硬约束 + 最轻量实现 = 空数据源 + 只走状态的播放器）

### 6.2 🔴 1 处缺口：`§17.2` 决策清单里没有"回放"

| 项 | 实测 |
|---|---|
| 现象 | `§17.2`（17 条根本性决策）里「回放」出现次数 = **0** |
| 为何是缺口 | 回放被定为"**独立模块**"（新增第 25 个模块）属**架构级决策**；`§17.2` 职责原文 = 只装**选型级**决策 ⇒ 应有一条 |
| 成因 | `§17.2` 是 09-20 定的 17 条；回放 09-21 后加（先走 `CR-009`/`CR-010`），**没回头补进决策表** |
| 影响 | **不阻塞**（另 6 处已完整落地）；但架构决策的"为什么"缺一处权威记载 |

### 6.3 建议（**不替用户拍板**）

| 选项 | 做法 |
|---|---|
| **A · 补决策 18** | `§17.2` 加第 18 条：回放**独立成模块且只预留**；依据 = 用户 09-21 裁定 |
| **B · 不补** | 靠现有 6 处 + 独立的《回放接口预留》文档 |
| **C · 只加指针** | `§17.2` 引言加一句指向《`S1-回放-独立模块接口预留.md`》，不新增表行 |

> ⚠️ `§17.2` 每条都标「均有已裁依据」⇒ **是否新增第 18 条属用户裁决**，本文只登记。
