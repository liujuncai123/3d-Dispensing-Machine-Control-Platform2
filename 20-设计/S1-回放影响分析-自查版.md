# 回放功能「若采纳」影响分析 · 自查版

> **一句话**：用户 2026-09-21 提问「我们考虑了回放功能吗？通过导入一段时间的数据进行回放的能力？」
> ⇒ 本文件是给**用户裁决**用的判断依据，**不是结论**（回放属范围新增，AI 不得自拟）。
> **性质**：✅ **已裁决（2026-09-21）＝ B · 只预留接口**，且**回放须为独立功能模块** ｜ **提出**：2026-09-21 ｜ **裁决人**：用户

---

## 🔴 更正声明（2026-09-21，架构师路复核后）

**本文件的判断已被架构师路推翻 / 修正，请以 `./S1-回放-数据支撑性分析.md` 与 `./S1-回放-独立模块接口预留.md` 为准：**

| 本文件的判断 | 改判 | 出处 |
|---|---|---|
| §二 角度 1「数据基础**大半已具备**」（依据 `REQ-ARCH-001` 趋势曲线预留） | 🔴 **架构师明确反对**：`REQ-ARCH-001` 是**监控统计**的**标量**时间序列（产量 / 稼动率），**不是位姿 / 轨迹时间序列**；且它「预留无实现」，连"数据写进去了"都没承诺 ⇒ **不构成回放的数据基础** | 架构师路 §6.3 |
| §二 角度 3「`P-3` 把『文件导入』点名列为只留接口」 | ⚠️ **待核**：`P-3` 的"文件导入"可能指 DXF / G 代码（**程序文件**）导入，与回放的**历史数据**导入不同义；若不同义，该论据不成立 | 本文件自述已标待核，**至今未验证** |
| 缺口数 5 项 | 架构师路列 **9 项** | 架构师路 §四 |

⚠️ 本文件是**未经验证的初版自查**，其定性判断在独立复核后被推翻 ⇒ 引用时须走更正后的版本。


---

## 〇、这份文件为什么是「自查版」

| 项 | 情况 |
|---|---|
| 原计划 | 派 10 路 worker × 10 轮 = **100 轮**对抗性检查 |
| 实际 | **7 路全部因平台 429 限流失败**（`loop3-a/b/c/e/f/g` 确认失败，`loop3-d` 无成果无报告）⇒ **0 份报告** |
| 应对 | 由主 Agent **直接自查**（不派 worker），覆盖 **10 个角度**，产出本文件 |
| ⚠️ 声明 | 本文件是 **10 轮自查**，**不等于**原定的 100 轮对抗检查；限流解除后可补跑 |

---

## 一、事实层：回放是不是空白？（已实测）

| 查什么 | 结果 |
|---|---|
| 全库搜 `回放` / `replay` / `重放` / `历史重现` / `再现` | **仅 1 处命中，且是比喻**（「把前 60 轮的全部发现逐条回放」，见 `20-设计/S1-模块划分-第61轮清账与交接.md`）⇒ **不是需求** |
| `S-1 §四` 不做清单（4 项） | 没有回放 |
| `S-1 §四` 预留清单（9 项） | 没有回放 |
| `S0` 需求基线（127 条） | 没有回放类条目 |
| `S1 §17.2` 决策清单（17 条） | 没有回放类决策 |
| `S1 §十二` 架构关注点（32 项） | 没有回放类关注点 |

⇒ **回放是 S-1 / S0 / S1 三层的空白，既不在"要做"，也不在"不做/预留"。**

---

## 二、10 个角度的自查结论（每条附原文出处，**无行号**）

| # | 角度 | 结论 | 归类 | 原文依据 |
|---|---|---|---|---|
| 1 | **数据基础够不够** | **大半已具备**：`REQ-ARCH-001`（趋势曲线**预留**）已要求「数据模型含时间序列字段 + 界面预留视图位」；`REQ-LOG-007` 误差日志含「设备 / 时间点 / 偏差量 / 阈值 / 是否已归正」 | **I**（有利信息） | `10-基线/S0-需求基线.md`：`REQ-ARCH-001` 行「① 数据模型含时间序列字段 ② 界面预留视图位」；误差日志行「含设备 / 时间点 / 偏差量 / 阈值 / 是否已归正」 |
| 2 | **缺的是什么** | **缺「回放器」**（按时间轴驱动 3D + 状态 + 轨迹），**不是**缺数据 | **C** | 同上：数据侧字段已有，无任何一条需求提到"按时间轴重演" |
| 3 | **回放 vs `P-3`（重活只预留）** | 🔴 **决定性依据**：`P-3` 原文把「**文件导入**」明确列入"只留接口"清单 ⇒ 而用户描述的回放正是「**导入**一段时间的数据」 | **D**（影响裁决，不阻塞） | `00-流程/01-开发流程.md`：`P-3` 行「需真实数据或高成本的（物理量仿真 / CCD 视觉算法 / PID 自适应 / **文件导入**）**只留接口**」 |
| 4 | **回放 vs `P-1`（闭环不可缺）** | **不属闭环必需**：删掉回放，链路（编程→校验→下发→执行→3D 映射→误差→归正）**仍通** ⇒ `P-1` 不强制做 | **C** | `00-流程/01-开发流程.md`：`P-1` 行「构成端到端闭环的环节**只可简化、不可删除**」，判据 = 「删掉它 ⇒ 链路断掉」 |
| 5 | **回放 vs `P-5`（安全优先）** | 🔴 **最大风险点**：回放若驱动 `core/execution`，有「把历史数据当新程序下发」的风险 ⇒ 设计上必须**只可视化、不下发** | **D** | `00-流程/01-开发流程.md`：`P-5` 行「急停本地优先 / 掉线即停 / 批量二次确认 / 单台故障隔离」；`20-设计/S1-架构基线草稿.md`「安全底线（原则 P-5）｜四项**不可配置**」 |
| 6 | **回放 vs `P-4`（不追求完整）** | 回放是典型的"以后可能用到的完整版" ⇒ `P-4` 倾向**不写实现** | **C** | `00-流程/01-开发流程.md`：`P-4` 行「不做"以后可能用到"的完整版｜留接口 ≠ 写实现」 |
| 7 | **回放与已有亮点重叠** | `REQ-3D-010` 已有 **L3 分析档 =「预期 vs 实际对比」** ⇒ 回放本质是给这个对比**加一个时间维度**，不是从零起 | **I** | `10-基线/S0-需求基线.md`：`REQ-3D-010` 行「③ **L3 分析**：L2 + **预期 vs 实际对比**，50ms —— 演示 / 排查手动开」 |
| 8 | **导出↔导入能否闭环** | `REQ-MON-003`（数据可导出为 CSV/Excel）只保证「导出」；**没有**任何一条需求承诺「导出的能再导入」⇒ **闭环未定义** | **C** | `10-基线/S0-需求基线.md`：`REQ-MON-003` 行「① 可导出 ② 内容与界面一致」——无导入项 |
| 9 | **单测能不能纳入** | 回放器若做成"纯读数据 + 按时间戳产出帧"，是**可单测**的（不依赖界面），符合 `REQ-INF-002` | **I** | `10-基线/S0-需求基线.md`：`REQ-INF-002` 核心模块有单元测试（对比：同表 `REQ-VMC-005` 的判断法「该断言可作为一条单测（不依赖界面）」） |
| 10 | **流程上怎么走** | 无论选 A/B/C，**都涉及已冻结层（S0 / S-1）** ⇒ **必须走 `50-变更单/`（CR）+ 用户逐项裁决** | **D**（流程硬约束） | `S-1 §七 R2/R4`「任何层新增的范围内容，都必须逐项交用户裁决」；`00-流程/01-开发流程.md` 变更单机制 |

---

## 三、四个选项的代价（**只列，不替用户拍板**）

| 选项 | 含义 | 需动的东西 | 工作量 | 依据 |
|---|---|---|---|---|
| **A · 做（简化版）** | 加「导入一段记录 → 3D 场景按时间轴重演」的最小版 | S0 新增需求（**CR**）+ S-1 预留清单 + S1 边表（新模块/新边）+ §十二 关注点 | **中~重**（新模块 + 时间轴驱动 + 与实时路径互斥） | 角度 3/5/8 |
| **B · 只预留** ✅ **成本最低** | 依 `P-3`/`P-4`：只留接口、不实现 | 加 1 条预留项 + 1 个 `I*` 接口点 | **几乎零**（数据基础已留一半：角度 1） | 角度 1/3/4/6 |
| **C · 明确不做** | 写进 `S-1 §四` 不做清单 | 1 条 CR | **零**（但放弃一个演示亮点） | 角度 4 + 边界清晰 |
| **D · 先不动** | 只登记在 `30-待裁决/` | 无 | 零，但**悬着** | — |

> 🔑 **三条最硬的判断依据**（供参考，不作决定）：
> ① `P-3` 把「文件导入」**点名**列为"只留接口" ⇒ 用户的"导入数据回放"正落在这一条上；
> ② 回放**不属** `P-1` 闭环必需（删了链路仍通）⇒ 无"必须做"的强制力；
> ③ 数据基础已由 `REQ-ARCH-001` 留了一半 ⇒ **B（只预留）的边际成本接近于零**。

---

## 四、若采纳，需新增/修改的东西（清单）

| 类别 | 具体 |
|---|---|
| **S0 需求** | 新增回放需求条目（走 CR，用户裁） |
| **S-1 §四** | 预留清单 +1 项（或不做清单 +1 项） |
| **S1 模块** | 新模块「回放器」（读 `data/store` → 驱动 `app/scene3d`） |
| **S1 边表** | 新增边（当前权威：**78 条边 A63/B6/D9 · 31 节点**） |
| **S1 §十二** | 新增关注点：时间轴 / 倍速 / 与实时路径互斥 |
| **安全** | 硬约束「回放只可视化、不下发」，对齐 `P-5` |
| **精度** | 回放用 `REQ-3D-010` 的哪档（L1/L2/L3） |

---

## 五、机器核验（收尾）

```
python 00-流程/check-deps.py        ⇒ 退出码 0（78 条边 A63/B6/D9 · 31 节点）
python 00-流程/check-consistency.py ⇒ 退出码 0
```

---

## 六、收敛小结

- **新增发现**：D 类 3 条（`P-3` 点名文件导入 / `P-5` 误下发风险 / 流程必须走 CR）· C 类 3 条 · I 类 3 条 · 事实层 1 条
- **重复投影**：0（本主题为全新，前 200+ 轮未覆盖"回放"）
- **结论**：**问题本身已查清，答案取决于用户裁决，不取决于再查多少轮。**
