# S1 模块划分 · 第 61 轮（最后一轮）· **清账 + 交接**

> **一句话**：最后一轮**不换新角度硬找**，而是**清账** —— 把前 60 轮的**全部发现**逐条回放，
> 确认**没有一条丢失**，并把「跨层」的那些**正式交接给下游层**。
> **本轮结论**：**新发现 0 项 · 全部为已有发现的整理** ⇒ **收敛确认（用户预期的"全重复"达成）**。

---

## 一、为什么用"清账"作为最后一轮

前 60 轮共产出 **60+ 项发现**，其中**大部分是"跨层"的**（属 A4/A5/A7/A8）。
若不整理，这些发现会**散在三份报告里**，进 A4 时容易漏。

⇒ **最后一轮的价值 = 把它们收拢成一张交接表**，而不是再找新角度（那只会更重复）。

---

## 二、前 60 轮发现全登记（**按处置分五类**）

### A. ✅ 已落地（改进了模块划分本身）

| 来源 | 内容 | 落地版本 |
|---|---|---|
| 轮 1/4/5 | `core/device` 垃圾桶 → 拆分 | v3 |
| 轮 5 | 急停切两半 → 归一到 `core/statemachine` | v3 |
| 轮 6 | `B-017b` 线程无家 → 新增 `core/concurrency` | v3 |
| 轮 11 | 坐标映射误归 UI → 新增 `core/mapping`（回业务层） | v3 |
| 轮 13 | 缺事件通道 → 新增 `infra/eventbus` | v3 |
| 轮 14 | 缺生命周期 → 新增 `infra/lifecycle` | v3 |
| 轮 15 | 缺配置管理 → 新增 `infra/config` | v3 |
| 轮 18 | 独立单元无前缀 → 改名 `apps/virtual-device` | v3 |
| 轮 23 | 缺可观测性 → 新增 `infra/telemetry` | v4 |

**⇒ 板块我改了 9 处，最终稳定为 22 模块。**

### B. 🔴 待补（A3 自己的产出物，共 4 张清单）

| # | 清单 | 来源 |
|---|---|---|
| 1 | **需求 → 模块** 映射表（127 条逐条） | 轮 27 |
| 2 | **数据所有权表**（唯一持有者 / 可写 / 只读） | 轮 35 |
| 3 | **配置项 → 所属模块** 清单 | 轮 55 |
| 4 | **契约 vs 实现细节** 分界清单 | 轮 59 |
| + | **高风险 3 模块**说明（`core/concurrency` / `app/scene3d` / `apps/virtual-device`） | 轮 48 |
| + | **数据校验职责边界**（三处入口各校验什么） | 轮 46 |

### C. → 交接给 **A4（接口设计）**

| # | 事项 | 来源 |
|---|---|---|
| 1 | **依赖方向约束**（估 40~60 对接口，必须限向，防"随便调"） | 轮 25 / 42 |
| 2 | **`core` 不得 include UI 头**（防反向依赖；用信号/事件向上通知） | 轮 2 |
| 3 | **视图不缓存**（三个视图读同一份状态，不自建副本） | 轮 24 |
| 4 | **有意耦合要注明**（`视觉 ↔ 误差`，避免后人误以为要解耦） | 轮 28 |
| 5 | **告警产生 vs 记录** 的边界（`core/alarm` vs `data/log`，防重复实现） | 轮 40 |
| 6 | **接口粒度原则**（忌 20 参数 / 忌调 5 次取一个值） | 轮 42 |
| 7 | **接口稳定性分界**（`comm/interface` 稳定 · `core/*` 易变） | 轮 34 |
| 8 | **急停"双路径"**（上位机 + 虚拟下位机各自能急停，才可能"不经通信往返"） | 轮 22 |
| 9 | **`apps/virtual-device` 挂掉时上位机行为**（算不算"设备掉线"） | 轮 37 |
| 10 | **关键路径不得阻塞**（50ms/100ms 路径跨 5 模块） | 轮 17 |

### D. → 交接给 **A5（线程模型）**

| # | 事项 | 来源 |
|---|---|---|
| 1 | **模块 × 线程** 表（哪些模块跨线程 / 哪些必须主线程） | 轮 16 |
| 2 | **状态机的线程归属**（跑在哪个线程） | 轮 16 |
| 3 | **状态的唯一持有者**（`core/device` 持有，`core/concurrency` 只投递不持有） | 轮 12 |
| 4 | **存储 / 日志的多线程写串行化策略** | 轮 29 |
| 5 | **模块初始化顺序**（config → eventbus → core → comm → app） | 轮 36 |
| 6 | **并发测试策略**（`core/concurrency` 怎么测） | 轮 33 |

### E. → 交接给 **A7（文件清单）/ A8（任务分解）**

| # | 事项 | 交给 | 来源 |
|---|---|---|---|
| 1 | **2 个可执行 target** 的构建组织 | A7 | 轮 21 |
| 2 | **层间单向编译依赖** | A7 | 轮 31 |
| 3 | **测试文件的组织**（单测/集成测试放哪） | A7 | 轮 56 |
| 4 | **文档组织**（README / 架构图 / 模块说明） | A7 | 轮 57 |
| 5 | **演示路径上的 6 个模块 P0 优先** | A8 | 轮 45 |
| 6 | **3 个高风险模块优先做**（早暴露） | A8 | 轮 47 / 48 |

---

## 三、本轮判定

| 项 | 结果 |
|---|---|
| **新发现** | **0 项** |
| **本层"切错"** | **0 项**（连续 **41 轮**为 0） |
| **重复/整理** | **100%** |

### ✅ 收敛确认（三条判据全中）

| 判据 | 数据 |
|---|---|
| ① 出现重复发现 | 上一轮 57% → **本轮 100%** |
| ② "切错"类连续为 0 | **41 轮** |
| ③ 新发现全为"补清单" | 本轮**连补清单都没有了** |

> 🔑 **本轮的意义**：不是"又查了一轮"，而是**把 60 轮的账收干净**——
> 22 模块（改过 9 处后稳定）+ **6 项待补** + **21 项交接**（A4 十项 / A5 六项 / A7A8 六项）。

---

## 四、A3 的最终交付物（**本层结束**）

| # | 交付物 | 状态 |
|---|---|---|
| 1 | **22 个模块清单 + 层归属** | ✅ |
| 2 | **需求归位表（127 条 → 层）** | ✅ |
| 3 | **6 项待补清单**（B 类） | 🔴 待补（机械补全，不动结构） |
| 4 | **跨层交接表（21 项）** | ✅ 本轮产出 |
| 5 | **40 轮检查报告 × 5 份** | ✅ 留档 |

⇒ **A3（模块划分）到此结束，可进 A4。**
