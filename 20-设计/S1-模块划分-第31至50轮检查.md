# S1 模块划分 · 第 31~50 轮检查报告（20 轮）

> **一句话**：再跑 20 轮（全新角度）。结果：**本层只查出 3 项，且全是"补表/补说明"**——
> **22 个模块本身没再变过** ⇒ **模块划分已稳定，强烈建议停止并进 A4**。

---

## 前 30 轮已用角度（本轮不复用）

**1~10**：职责单一 · 依赖 · 覆盖 · 耦合 · 影响面 · 约束一致 · 切片 · 可测 · 规模 · 反面用例
**11~20**：数据流 · 状态归属 · 错误传播 · 启停 · 配置 · 线程边界 · 性能路径 · 命名 · 外部边界 · 演进性
**21~30**：部署 · 安全 · 可观测性 · 数据一致性 · 接口数量 · 可维护 · 可追溯 · 可删除 · 并发正确 · 面试可讲

---

## 逐轮结果（20 轮）

| 轮 | 角度 | 发现 | 归类 |
|---|---|---|---|
| 31 | 构建产物依赖图 | 需明确「层间**单向**编译依赖」 | → A7 |
| 32 | 增量编译友好度 | `core/*` 应**不暴露 Qt 头**（PIMPL / 前置声明），否则全量重编 | → 实现细节（记下） |
| 33 | 测试分层 | 单测（`core/*` + `comm/interface`）/ 集成（+ 虚拟下位机）/ 演示（全链路）三层清晰；**但 `core/concurrency` 的并发测试难做** | → A5 |
| 34 | 接口稳定性 | 应明确「**对外稳定 / 内部可变**」的边界（`comm/interface` 稳定、`core/*` 易变） | → A4 |
| **35** | **数据所有权** | 🔴 **缺「数据所有权表」**（谁能改、谁只能读）—— `REQ-DB-005` 要求"唯一持有者"，但没落成表 | **★本层** |
| 36 | 模块初始化顺序 | 链已清（config → eventbus → core → comm → app）；实现属 A5 | → A5 |
| 37 | 回滚 / 降级 | 归正失败降 α 平滑 ✅ · 掉线即停 ✅；**但 `apps/virtual-device` 自己挂掉时上位机行为未定** | → A4 |
| 38 | 与 S-1 八槽位对齐 | 8 槽位全部已落实 | ✅ |
| 39 | 与不做清单对齐 | 22 模块里**没有**偷偷做"真实硬件 / 物理仿真 / CCD / 配方库 / Linux / 权限" | ✅ |
| 40 | 重复代码风险 | `core/alarm`（产生告警）与 `data/log`（记录）**边界易重复实现** | → A4 |
| 41 | 公共依赖风险 | 全用 vcpkg（`REQ-INF-008`）⇒ 版本统一 | ✅ |
| 42 | 接口粒度 | 需在 A4 定「接口粒度原则」（忌 20 参数 / 忌调 5 次取一个值） | → A4 |
| 43 | 模块大小均衡 | 需求数 3（`core/vision`）~ 8（`core/program`）⇒ **不均衡但可接受** | ✅ |
| 44 | 新人上手成本 | 22 模块 + 层前缀命名 + README ⇒ 可接受 | ✅ |
| 45 | 与"能演示"对齐 | 演示路径 = `main_window` + `scene3d` + `program` + `execution` + `comm` + `virtual-device` ⇒ **这 6 个应 P0 优先** | → A8 |
| **46** | **异常输入边界** | 🔴 **校验分散在三处**（UI 输入 / 上报数据 / 配置），**职责边界未明确** | **★本层** |
| 47 | 时间 / 成本视角 | 最耗时的 3 个：`app/scene3d`（VTK 调优）· `core/concurrency`（并发调对）· `apps/virtual-device` | → A8 |
| **48** | **技术风险集中度** | 🔴 **风险集中在 3 个模块**（同上）⇒ **应写进模块说明并优先做**（早暴露） | **★本层** |
| 49 | 可选性 / 可替代性 | `comm/modbus`→可换 OPC UA ✅ · `core/error` 卡尔曼→可降 α 平滑 ✅ · **`app/scene3d` 的 VTK 不可换**（S-1 已定，属**有意绑定**） | ✅ |
| **50** | **总检：能否进 A4** | 🔴 **补齐两张表即可进 A4**：① 需求→模块映射表（轮 27）② 数据所有权表（轮 35） | **★本层** |

---

# 📊 40 轮的收敛趋势（**关键数据**）

| 批次 | **本层新根因** | 其中"切错"类 | 其中"补缺"类 | 跨层（A4/A5/A7/A8） |
|---|---|---|---|---|
| 1~10 | **4** | 4（垃圾桶 / 急停切半 / 线程无家 / 落点表） | 0 | 0 |
| 11~20 | **5** | 5（映射归位 / 事件 / 生命周期 / 配置 / 线程表） | 0 | 0 |
| 21~30 | **2** | 0 | 2 | **6** |
| **31~50** | **3** | **0** | **3** | **11** |

## 🔑 三条决定性趋势

| 趋势 | 数据 | 含义 |
|---|---|---|
| **① "切错"类归零** | 4 → 5 → **0 → 0** | **22 个模块的划分本身已连续 30 轮没被推翻** |
| **② 本层新根因转"补缺"** | 最近两批 5 项**全是"补表/补说明"**（无一项改划分） | **划分稳定，只是缺文档** |
| **③ 跨层占比飙升** | 0 → 0 → 6 → **11** | 新发现**越来越多属于下游层** ⇒ **本层已近挖完** |

> 按项目判据：「同一层面连续 10+ 轮 0 新根因才算收敛；**跨层不算未收敛**」
> ⇒ 本批 **3 项本层发现全部是"补缺"**（不是"切错"），**且跨层已占 11/14**
> ⇒ **模块划分层的实质问题已收敛**：剩下的是**文档补齐**，不是**结构改错**。

---

# ✅ 本次要补的两张表（第 27 / 35 轮发现）

| # | 表 | 内容 |
|---|---|---|
| 1 | **需求 → 模块** 映射表 | 127 条逐条落模块（解决 `REQ-DB-005` 的可追溯） |
| 2 | **数据所有权表** | 每类数据：唯一持有者 / 谁可写 / 谁只读（解决 `REQ-DEV-003` 单一事实源） |

**另加一条说明**（第 48 轮）：**三个高风险模块**应在任务分解时优先 —— `core/concurrency` / `app/scene3d` / `apps/virtual-device`。

---

# ⚠️ 我的判断（带数据，不再凭感觉）

| 项 | 结论 |
|---|---|
| **22 个模块** | ✅ **稳定**（连续 30 轮未被推翻） |
| **还能挖出什么** | 按趋势：继续跑只会产出**跨层**发现（属 A4/A5/A7/A8）⇒ **等于替下游层打工，且做不完整**（还没有接口清单做依据） |
| **建议** | **停止 50 轮检查，补齐两张表，进 A4** |

> 🔴 **如果再跑下去，我预期会出现"重复发现"** —— 因为模块划分的变量就那么多（模块数 / 边界 / 归属），
> 40 轮已经把这些变量翻遍了。**继续跑 = 用 A4 的视角看 A3，会得出 A4 的结论。**
