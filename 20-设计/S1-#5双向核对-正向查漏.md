# S1 `#5` 依赖图 · 双向核对之「正向（查漏）」

- **本文件装什么**：对 `S1-依赖边表.txt`（依赖图唯一权威源）做**第三步·正向核对**——把 8 个关键场景从头到尾走的每一跳，逐个到边表里查「有没有这条边」；没有的记入漏边清单。本文件**只读核验产物，不改任何既有文件**（被核对象未被改动），也不做 git 提交。
- **被核对象**：`20-设计/S1-依赖边表.txt`（64 条边：A 层 48 / B 层 7 / D 层 9；31 个节点）。
- **对照物**：`20-设计/S1-关键场景清单.md`（8 个场景，源自 `50-变更单/CR-S0-003-补演示脚本十步.md` 的「多机 10 步」）。
- **比对依据**（逐跳附出处，禁用行号）：`S1-架构基线草稿.md` 的 §四 需求→模块映射（127 条）· §五 数据所有权 · §七 配置 · §14.2 外部实体 · §16.1 模块清单 · §17.5 数据流；以及 `10-基线/S0-需求基线.md`、`S1-#5依赖图方法-定稿.md` §七（核对规则）。
- **日期**：2026-09-21 ｜ **执行人**：dep-audit-forward（正向查漏，仅报漏边，不报多边）

---

## 表一 · 8 个场景逐跳明细

> 「边表里？」列：`命中`=边表有对应边；`漏边`=边表无对应边（记入表二）；`命中(回调)`=该跳经事件/回调/依赖倒置实现，依方法 §二 方向判据**不需直接 A 边**（不计入漏边）。
> 依据一律用 `REQ-*` / `§五:xx` / `§17.5:xx` / `§7.2.1②` / `决策17` 等，禁用行号。

### SC1 · 冷启动全链（启动 → 连接多台 → 编程 → 校验）

| 跳次 | A → B | 边表里？ | 依据 |
|---|---|---|---|
| 1 | main → app/main_window | **漏边** | §7.2.1② 组合根注入 / REQ-UI-001（主框架存在） |
| 2 | main → app/scene3d | 命中 | main→app/scene3d（§7.2.1② / REQ-3D-001） |
| 3 | main → app/dashboard | **漏边** | §7.2.1② 组合根注入 / REQ-MON-001（设备状态总览） |
| 4 | app/main_window → core/device | 命中 | app/main_window→core/device（§五 设备状态只读 / REQ-DEV-001） |
| 5 | core/device → core/concurrency | 命中 | core/device→core/concurrency（§17.5 下发投递第一跳） |
| 6 | core/concurrency → comm/interface | 命中 | core/concurrency→comm/interface（§17.5 状态汇聚） |
| 7 | comm/interface → apps/virtual-device | 命中 | comm/interface→apps/virtual-device（D 协议 §14.2 E2 下发） |
| 8 | apps/virtual-device → comm/interface | 命中 | apps/virtual-device→comm/interface（D 协议 §14.2 E2 上报） |
| 9 | core/concurrency → core/device | 命中 | core/concurrency→core/device（§五 只投递不持有） |
| 10 | core/device → infra/eventbus | 命中 | core/device→infra/eventbus（B sub §7.2.1④ / 事件发布） |
| 11 | core/device → app/scene3d | 命中 | core/device→app/scene3d（B pub 决策17 状态变更→3D 刷新） |
| 12 | core/device → app/dashboard | 命中 | core/device→app/dashboard（B pub 决策17 状态变更→总览） |
| 13 | app/main_window → core/program | **漏边** | §四 REQ-PRG-001/002 + REQ-UI-002（编程写/校验触发） |
| 14 | app/scene3d → core/program | 命中 | app/scene3d→core/program（§五 程序/轨迹只读 / REQ-3D-006） |

### SC2 · 多台批量下发（含二次确认 + 逐台结果）

| 跳次 | A → B | 边表里？ | 依据 |
|---|---|---|---|
| 1 | app/dialogs → core/device | 命中 | app/dialogs→core/device（REQ-DEV-005 批量下发二次确认） |
| 2 | app/dialogs → core/execution | 命中 | app/dialogs→core/execution（REQ-RUN-010 作业前点检确认） |
| 3 | core/device → core/concurrency | 命中 | core/device→core/concurrency（§17.5 下发投递） |
| 4 | core/concurrency → comm/interface | 命中 | core/concurrency→comm/interface（§17.5 状态汇聚） |
| 5 | comm/interface → apps/virtual-device | 命中 | comm/interface→apps/virtual-device（D 协议 下发） |
| 6 | apps/virtual-device → comm/interface | 命中 | apps/virtual-device→comm/interface（D 协议 上报） |
| 7 | core/concurrency → core/device | 命中 | core/concurrency→core/device（§五 落成唯一事实源） |

### SC3 · 执行 + 3D 实时映射（出胶反馈 / 进度 / 精度分级生效）

| 跳次 | A → B | 边表里？ | 依据 |
|---|---|---|---|
| 1 | app/dialogs → core/execution | 命中 | app/dialogs→core/execution（REQ-RUN-010 启动运行确认） |
| 2 | core/execution → core/statemachine | 命中 | core/execution→core/statemachine（REQ-RUN-004 急停迁移/状态） |
| 3 | core/execution → core/device | 命中 | core/execution→core/device（§五 设备清单/参数） |
| 4 | core/execution → core/program | 命中 | core/execution→core/program（§五 程序/轨迹） |
| 5 | core/device → core/concurrency | 命中 | core/device→core/concurrency（§17.5 下发） |
| 6 | core/concurrency → comm/interface | 命中 | core/concurrency→comm/interface（§17.5 汇聚） |
| 7 | comm/interface → apps/virtual-device | 命中 | comm/interface→apps/virtual-device（D 下发指令） |
| 8 | apps/virtual-device → comm/interface | 命中 | apps/virtual-device→comm/interface（D 上报坐标/状态/出胶） |
| 9 | core/concurrency → core/device | 命中 | core/concurrency→core/device（§五 事实源） |
| 10 | core/device → core/error | 命中 | core/device→core/error（B pub §17.5 状态变更触发偏差计算） |
| 11 | core/error → core/device | 命中 | core/error→core/device（§五 归正读实际位置/下发修正） |
| 12 | core/device → app/scene3d | 命中 | core/device→app/scene3d（B pub 决策17 3D 刷新） |
| 13 | app/scene3d → core/device | 命中 | app/scene3d→core/device（§五 设备状态只读 / REQ-3D-003） |
| 14 | app/scene3d → core/mapping | 命中 | app/scene3d→core/mapping（§五 坐标→几何映射只读 / REQ-3D-002/003） |
| 15 | app/scene3d → core/program | 命中 | app/scene3d→core/program（§五 程序/轨迹只读 / REQ-3D-006） |

### SC4 · 误差归正闭环（造误差 → 上报 → 归正 → 复测）

| 跳次 | A → B | 边表里？ | 依据 |
|---|---|---|---|
| 1 | apps/virtual-device → comm/interface | 命中 | apps/virtual-device→comm/interface（D 上报误差 §14.2 E2 / REQ-ERR-003） |
| 2 | comm/interface → core/concurrency | 命中(回调) | §17.5 汇聚；依方法 §二 方向判据「业务→抽象」，接收经回调/事件，无直接 A 边（不计入漏边） |
| 3 | core/concurrency → core/device | 命中 | core/concurrency→core/device（§五 事实源） |
| 4 | core/device → core/error | 命中 | core/device→core/error（B pub §17.5 状态变更触发偏差计算） |
| 5 | core/error → core/device | 命中 | core/error→core/device（§五 归正读实际位置/下发修正） |
| 6 | core/error → core/vision | 命中 | core/error→core/vision（REQ-VIS-003 对位/测高进误差链路） |
| 7 | core/error → data/log | 命中 | core/error→data/log（REQ-LOG-007 误差日志） |
| 8 | core/error → data/store | 命中 | core/error→data/store（§五 记录含误差日志） |
| 9 | core/device → core/concurrency | 命中 | core/device→core/concurrency（§17.5 修正量下发） |
| 10 | core/concurrency → comm/interface | 命中 | core/concurrency→comm/interface（§17.5 下发） |
| 11 | comm/interface → apps/virtual-device | 命中 | comm/interface→apps/virtual-device（D 修正量下发） |
| 12 | app/scene3d → core/program | 命中 | app/scene3d→core/program（§五 程序/轨迹只读 / REQ-3D-012 复测对比） |
| 13 | app/scene3d → core/device | 命中 | app/scene3d→core/device（§五 设备状态只读 / REQ-3D-012） |

### SC5 · 报警全链（三级 + 洪泛抑制 + 两次确认 + 声光 + 严重停机）

| 跳次 | A → B | 边表里？ | 依据 |
|---|---|---|---|
| 1 | core/error → core/alarm | 命中 | core/error→core/alarm（B pub REQ-ERR-006 超阈值告警） |
| 2 | core/alarm → core/device | 命中 | core/alarm→core/device（§五 设备状态只读） |
| 3 | core/alarm → core/execution | 命中 | core/alarm→core/execution（REQ-ALM-004 严重报警触发停机） |
| 4 | core/alarm → core/error | 命中 | core/alarm→core/error（§五 误差/修正量只读） |
| 5 | core/alarm → data/log | 命中 | core/alarm→data/log（§五 报警记录） |
| 6 | core/alarm → app/dashboard | 命中 | core/alarm→app/dashboard（B pub 决策17 报警→呈现） |
| 7 | core/alarm → E7 | 命中 | core/alarm→E7（D 协议 REQ-ALM-002 第一次确认关声光） |
| 8 | app/dashboard → core/alarm | 命中 | app/dashboard→core/alarm（§五 报警只读 / 两次确认） |
| 9 | core/execution → core/device | 命中 | core/execution→core/device（§五 停机下发设备参数） |
| 10 | core/device → core/concurrency | 命中 | core/device→core/concurrency（§17.5 下发停机指令） |
| 11 | core/concurrency → comm/interface | 命中 | core/concurrency→comm/interface（§17.5 下发） |
| 12 | comm/interface → apps/virtual-device | 命中 | comm/interface→apps/virtual-device（D 停机指令下发） |

### SC6 · 急停（本地优先 + 插队，不经通信往返）

| 跳次 | A → B | 边表里？ | 依据 |
|---|---|---|---|
| 1 | app/dialogs → core/execution | 命中 | app/dialogs→core/execution（REQ-RUN-010 / 急停触发本地） |
| 2 | core/execution → core/statemachine | 命中 | core/execution→core/statemachine（REQ-RUN-004 急停迁移） |
| 3 | core/execution → core/device | 命中 | core/execution→core/device（§五 设急停态/设备参数） |
| 4 | core/device → infra/eventbus | 命中 | core/device→infra/eventbus（B sub 事件发布） |
| 5 | core/device → app/scene3d | 命中 | core/device→app/scene3d（B pub 决策17 状态→3D） |
| 6 | core/device → app/dashboard | 命中 | core/device→app/dashboard（B pub 决策17 状态→总览） |

> 说明：急停「不经通信往返」在跳 3 即本地切断（core/execution→core/device 设急停态），不出现 `comm/interface` 边，符合 REQ-RUN-004。状态机到执行的「生效」由 `core/execution → core/statemachine`（读状态）承载，无缺失直接边。

### SC7 · 掉线（3 秒判定 → 停机 → 恢复后保持急停）

| 跳次 | A → B | 边表里？ | 依据 |
|---|---|---|---|
| 1 | comm/interface → core/concurrency | 命中(回调) | §七 掉线判定阈值（comm/interface 3 秒）；检测到掉线后经回调入 core/concurrency → core/device 置离线（§17.5 汇聚，无直接 A 边，不计入漏边） |
| 2 | core/execution → core/device | 命中 | core/execution→core/device（§五 读状态 / REQ-RUN-009 掉线即停） |
| 3 | core/device → core/alarm | **漏边** | SC7 列出 core/alarm（通信失败报警 REQ-ALM-003）；当前边表唯一入边 core/alarm 的来源是 core/error，掉线/离线无发布者路由到 core/alarm |
| 4 | core/device → app/dashboard | 命中 | core/device→app/dashboard（B pub 决策17 显示离线） |
| 5 | core/device → app/scene3d | 命中 | core/device→app/scene3d（B pub 决策17 3D 显示离线） |
| 6 | core/device → core/concurrency | 命中 | core/device→core/concurrency（§17.5 停机指令下发） |
| 7 | core/concurrency → comm/interface | 命中 | core/concurrency→comm/interface（§17.5 下发） |

### SC8 · 多设备 3D 呈现（多实例 + 精度分级 L0~L3 + 切换详情）

| 跳次 | A → B | 边表里？ | 依据 |
|---|---|---|---|
| 1 | app/scene3d → core/device | 命中 | app/scene3d→core/device（§五 设备状态只读 / REQ-3D-009 多实例） |
| 2 | app/scene3d → core/mapping | 命中 | app/scene3d→core/mapping（§五 坐标→几何映射只读 / REQ-3D-002/003） |
| 3 | app/scene3d → core/program | 命中 | app/scene3d→core/program（§五 程序/轨迹只读 / REQ-3D-006） |
| 4 | core/device → app/scene3d | 命中 | core/device→app/scene3d（B pub 决策17 状态→3D / 多实例刷新） |

---

## 表二 · 漏边清单（正向查漏，仅 8 场景功能跳）

> 每条按 `使用者 → 被使用者 | 建议层 | 类型 | 出处` 给出，便于直接补进边表。归类：**D**=缺陷(阻塞) **C**=完整性(漏边，非真缺陷) **I**=不一致 **Q**=疑问。本项目仅作「查漏」，以下均非 D。

| # | 建议补的边 | 归类 | 依据（场景 + 来源） |
|---|---|---|---|
| 1 | `main -> app/main_window \| A \| usage \| §7.2.1② 组合根注入 / REQ-UI-001` | C | SC1 跳1：组合根 main 必须实例化主窗口外壳；边表已有 `main -> app/scene3d` / `main -> app/dialogs`，独缺主窗口外壳，内部不一致 |
| 2 | `main -> app/dashboard \| A \| usage \| §7.2.1② 组合根注入 / REQ-MON-001` | C | SC1 跳3：启动即显示总览；与 `main -> app/scene3d` / `main -> app/dialogs` 同性质，缺 dashboard（亦可表达为 `app/main_window -> app/dashboard`，两者当前皆无） |
| 3 | `app/main_window -> core/program \| A \| usage \| §四 REQ-PRG-001/002 + REQ-UI-002` | C | SC1 跳13：编程（三种轨迹基元）与校验的写/触发需 UI 调 core/program；边表仅有 `app/scene3d -> core/program`（只读），无 UI 写边。若编程 UI 落在 `app/dialogs`，则改补 `app/dialogs -> core/program` |
| 4 | `core/device -> core/alarm \| B \| pub \| SC7 掉线/离线→报警（REQ-ALM-003 通信失败类型）` | C / I | SC7 跳3：场景清单把 core/alarm 列入覆盖模块，但边表唯一入边 core/alarm 的来源是 `core/error -> core/alarm`；掉线/离线（设备 报警态）无发布者路由到 core/alarm。建议补 B 层 `core/device -> core/alarm`（pub），或在场景清单把 core/alarm 从 SC7 移除以消除不一致 |

---

## 表三 · 横切检查（日志 / 统计链路齐不齐）

> 横切出题：`REQ-LOG-001~009` / `REQ-MON-002` 在 SC1~SC8 每一跳都应产生日志/统计；核对「谁产生日志/统计 → 是否有 `X → data/log` / `X → data/store` 边」。
> 已具备日志写者边：`main / core/execution / core/concurrency / core/error / core/alarm → data/log`。已具备统计落库边：`app/dashboard → data/store`（导出）、`core/execution → data/store`、`core/error → data/store`。

| # | 场景/动作 | 应产生 | 现状 | 建议补的边 | 归类 |
|---|---|---|---|---|---|
| 1 | SC1 编程/校验（程序编辑、保存、校验） | 操作日志 REQ-LOG-001 | `core/program` 在边表中**零条出边**（仅作被使用者），无写日志边 | `core/program -> data/log \| A \| usage \| REQ-LOG-001` | C |
| 2 | SC2 批量下发二次确认 / SC6 作业前点检确认 / 重要设置变更确认 | 确认记入日志 REQ-LOG-002 / REQ-LOG-001 | `app/dialogs` 出边仅 `core/execution / core/device / qt`，无日志边 | `app/dialogs -> data/log \| A \| usage \| REQ-RUN-010 / REQ-DEV-005 / REQ-INF-007（确认记入日志）` | C |
| 3 | SC7 掉线 / 各设备状态变化 | 事件日志（每次状态变化一条） REQ-LOG-002 | `core/device` 出边无 `data/log`；状态唯一持有者的状态变化未路由到日志 | `core/device -> data/log \| A \| usage \| REQ-LOG-002（状态变化事件日志）` | C |
| 4 | SC5/SC6 状态迁移（急停/报警态进入等） | 状态迁移事件日志 REQ-LOG-002 | `core/statemachine` 在边表中**零条出边**（仅作被使用者），无日志边 | `core/statemachine -> data/log \| A \| usage \| REQ-LOG-002（状态迁移事件日志）` | C |
| 5 | 横切（B-018 步9）产量/统计持久化 | 统计落库 REQ-MON-002 / REQ-DB-001 | `core/stats` 出边仅 `core/device`（读），无 `data/store` | `core/stats -> data/store \| A \| usage \| REQ-MON-002 / REQ-DB-001` | C |

> 可选（置信度较低，待裁）：`app/dashboard -> data/log`（REQ-LOG-004 日志查询 UI 读日志，属 usage 读边）；`core/device -> data/store`（REQ-DB-001 设备参数持久化）。两者当前边表均无，但属「读/持久化」而非每跳强写，是否入边表待你定。

---

## 收敛小结

1. **8 场景共几跳**：78 跳（SC1=14 · SC2=7 · SC3=15 · SC4=13 · SC5=12 · SC6=6 · SC7=7 · SC8=4）。其中 `命中(回调)` 计入命中（依方法 §二 方向判据，接收经回调/事件，无需直接 A 边）。
2. **其中几跳在边表里**：74 跳命中（含 2 跳「命中(回调)」），4 跳漏边。
3. **漏边几条**：
   - 8 场景功能跳漏边 **4 条**（表二 #1~#4）：`main→app/main_window`、`main→app/dashboard`、`app/main_window→core/program`、`core/device→core/alarm`。
   - 横切漏边 **5 条**（表三 #1~#5）：`core/program→data/log`、`app/dialogs→data/log`、`core/device→data/log`、`core/statemachine→data/log`、`core/stats→data/store`。
   - 合计建议补边 **9 条**（另 2 条可选待裁）。
4. **8 个场景的覆盖率 = 命中跳数 / 总跳数 = 74 / 78 ≈ 94.9%**（未计横切；横切为独立检查，不并入覆盖率分母）。

> 归类说明：所有发现均为 **C（完整性/漏边）**，无 **D**（无真架构缺陷、无环、无禁边被触发）；#4 同时具 **I（不一致）** 属性（SC7 场景清单列了 core/alarm 但边表无支撑边）。按方法 §十 判据4，双向核对要求「漏边 0」方算无缺口——当前漏边 9 条，需补后再做反向（查多）核对。

---

## 脚本退出码（仅核验既有边表，未改动任何文件）

- `python 00-流程/check-deps.py` ⇒ **退出码 0**（64 条边 A48/B7/D9、31 节点；① A 层无环 ② 无禁边 ③ 端点合法 ④ 出处无行号，全部通过）
- `python 00-流程/check-consistency.py` ⇒ **退出码 0**（共 23 项检查，失败 0 项，全部通过）

> 注：两脚本校验的是「既有边表是否合法/无环/端点合法」，不校验「漏边」；上述 9 条漏边为人工正向走查所得，脚本无法发现，须人工补入边表后重跑确认。
