# S1 `#5` 循环检查 · 第 2 批（轮 41–50 · 元层面 / 收尾）

> **本文件装什么**：`20-设计` 下 `#5 依赖图` 交付物的**第 2 批对抗性检查报告**（轮 41~50，10 个「元层面 / 收尾」角度）。
> **被检对象**（全部 `#5` 产出）：`S1-#5依赖图方法-定稿.md`、`S1-依赖边表.txt`（74 条边 · 31 节点 · 脚本实测）、`S1-关键场景清单.md`（9 场景）、`00-流程/check-deps.py`、`00-流程/check-consistency.py`、`S1-架构基线草稿.md`，及既有 7 份检查报告。
> **轮次范围**：41（跨文件一致性）· 42（引用可达性）· 43（交付完整性）· 44（下游交接）· 45（面试视角）· 46（过设计）· 47（失效信号）· 48（旧项目 6 根因对照）· 49（复现性）· 50（收敛检验）。
> **纪律**：只核验、不改任何既有文件、不 git 提交。每条结论附「文件路径 + 原文引用片段」，能实测的已实测（跑脚本 / grep 计数）。归类 D/C/I/Q，仅 D 阻塞。

---

## 表一：轮次表（轮 / 角度 / 发现 / 归类 / 是否已知重复）

| 轮 | 角度 | 核心发现 | 归类 | 是否已知项的同根因投影 |
|---|---|---|---|---|
| 41 | 跨文件一致性（五者之间） | 边计数三套口径：轮1-3报告写「75」、双向核对×2+场景清单写「64」、脚本实测「74」；方法仍写「6~8 场景」而场景清单已 9；方法§三节点规则漏列 `main`；边表 B 节混入 1 条 A 边；禁边清单与脚本 forbidden() 对 `app→qt/vtk` 范围不一致 | C | 是（根因①「改完不同步引用点」+ 根因④「枚举漏项」）；本批首次实证于 `#5` 边计数 |
| 42 | 引用可达性（逐类实测） | 边表 74 条「出处」+ 方法/场景引用的 45 个编号 token（§阿拉伯 5 / §中文 10 / REQ 23 / B 3 / 决策 1 / P 2 / ARCH 1）**全部可达（0 不可达）**；但 4 条边出处为「用户定」自由文本，不可追溯 | C/I | 是（根因⑥「常量无出处」投影于 4 条用户定边） |
| 43 | 交付完整性（两件套） | `#5` 判据 = 依赖图 + 接口清单。依赖图（边表）齐；**`S1-接口清单.md` 缺失（判据⑥未满足）**；`S1-依赖图.md`（§八步7 渲染物）亦缺失 | D | 是（已知「8 步只做 3 步」→ 步6/步7 未做） |
| 44 | 下游交接测试 | `#18` 构建组织 / `S2` 接口基线 / `#6` 运行时 三接手方：边表可给，但**接口清单缺失→#18/S2 推不出编译期图/接口基线**；`#6` 的 B 层事件清单**缺「配置变更广播」事件通道**（SC9 要求却无 B 边） | C/I | 是（根因⑤「载体缺失」投影） |
| 45 | 面试视角 | 13+ 份 `#5` 专属文件、约 200KB，产出一个 74 边的依赖图。可讲成「单人防漂移纪律」亮点，但对单人秋招档期属偏重流程 | I | 否（新视角结论，非根因投影） |
| 46 | 过设计检查 | 可砍：7 份检查报告 + 3 份优化轮次可合并精简（留 1 终稿 + diff）。不可砍：边表、check-deps.py、场景清单、方法定稿、B层约定/环处理/单人化核验 | I | 否（新视角结论） |
| 47 | 失效信号 | 可观察征兆：①报告声称边数 ≠ 脚本输出（已发生：75/64/74）②脚本常年绿却未真核对源码（S4 前源码交叉校验未实现）③方法场景数 stale；可设告警：把 `#5` 纳入 check-consistency.py，比对各文件声称边数 vs 脚本实测值 | C | 是（根因① + 根因⑤「无闸门载体」） |
| 48 | 旧项目 6 根因对照 | 6 根因全部在 `#5` 重犯（详见表五）：①计数漂移 ②用户定边无基准 ③自评计数不可信 ④禁边/脚本枚举漏 app→qt/vtk ⑤接口清单/渲染图/模块表载体缺失 ⑥用户定边无出处 | C | 是（全部为已知 6 根因投影，0 新根因） |
| 49 | 复现性 | 来源（§五+§17.5）写明，但「方向人工判」「正向查漏补边 9」「用户裁决红1/红2」三步依赖人；补边 9 的审计（dep-audit-forward 78 跳）无留存脚本、S4 前无源码→不可完全重跑 | I | 是（根因③「自评覆盖不可信」+ 根因①） |
| 50 | 收敛检验 | 本批（41-50）新发现 9 条（N1-N9），全部为已知 6 根因的重复投影，**新根因 = 0**；`#5` 检查**尚不能停**——剩 6 件必须做（见结尾） | — | 见结尾 |

---

## 表二：新增 D / C / I 清单（本批 41-50，且非已知 40+ 条里的具体项）

> 说明：本批 9 条新发现均为「已知根因的重复投影」，但具体现象（75/64/74 三套边数、方法 6-8 vs 9 场景、禁边↔脚本对 app→qt/vtk 不一致等）未出现在已知 40+ 条中，故列为新增投影。

| 编号 | 归类 | 现象（附证据） | 落回根因 |
|---|---|---|---|
| N1 | C | 边计数三套口径自相矛盾：第1-3轮报告「75 条边(A58/B8/D9)」、`S1-#5双向核对-反向查多.md`/`正向查漏.md`+`S1-关键场景清单.md` 写「64 条边(A48/B7/D9)」、脚本实测「74 条边(A59/B6/D9)」。`S1-#5依赖边表.txt` 头部与 `S1-#5依赖图方法-定稿.md` 均未声明当前边数 | ① |
| N2 | C | 方法 §七「对照物 = #17 的 6~8 个关键场景」、§八步3「双向核对（6~8 场景）」、§十判据4「对照物 = 6~8 个场景」；而 `S1-关键场景清单.md` 标题与正文均为「9 个」 | ① |
| N3 | C | 禁边清单 §五「`app/* → 第三方库**直连**（未经 comm/interface）` 禁」（REQ-COM-001）；但 `check-deps.py` 的 `forbidden()` 只查 `a.startswith(('core/','comm/','data/')) and b in ('qt','vtk')`，**漏查 `app/* → qt/vtk`**；边表有 5 条 `app/* -> qt/vtk`（用户定）被脚本放行（exit 0）→ 方法文字与脚本闸门范围不一致，且若日后出现 `app→真实第三方通信库直连` 脚本拦不住 | ④ + ① |
| N4 | C | 方法 §三「节点：必须是 §16.1 的 24 个模块之一，或外部实体 E1~E7，或第三方库（qt / vtk）」**未列 `main`**；但边表有 12 条边以 `main` 为端点（main→infra/config 等），脚本 `EXTERNAL` 含 `main`（装配点） | ① / ④ |
| N5 | C | 边表 B 节（「B 层 · 事件通道」标题下）混入 1 条 A 边：`infra/config -> infra/eventbus | A | usage`（§7.2.1④）；脚本按层字段解析故计入 A（A=59、B=6），但节标题与层字段不一致，肉眼易误读 | ① |
| N6 | I | 4 条边出处为「用户定(界面框架 Qt)」（app/main_window→qt、app/scene3d→vtk、app/scene3d→qt、app/dashboard→qt、app/dialogs→qt 中 4 条）→ 出处非空但**不可追溯到任何需求/节**，属决策无出处 | ⑥ |
| N7 | C/I | `#6` 运行时接手的 B 层事件清单（边表 B 节 6 条）**缺「配置变更广播」事件通道**：SC9「运行期改重要设置→二次确认→落库→广播变更通知→订阅者更新」需要该事件，但边表仅以 A 边 `infra/config -> infra/eventbus` 表达注入，无对应 B 边 ⇒ `#6` 接手会漏这条安全链 | ⑤ |
| N8 | C | 方法 §六「脚本豁免配置：check-deps.py 顶部 ALLOWED_CYCLES = {}，空字典是默认值」与脚本实际含 1 条豁免（`('core/concurrency','core/device')` 用户裁 A）文字不符（该豁免合规，仅措辞） | ① |
| N9 | I | 方法 §六「A 层会出 4 个环（13 节点 / 19 边 / 4 环）」数字无推导出处，实际仅 1 个 2-环且已合法豁免 ⇒ 自评数字可信度问题 | ② / ⑥ |

> 已知 D（非本批新发现，列此备查）：`S1-接口清单.md` 缺失（判据⑥）→ 阻塞 `#5` 宣布完成，属已知「8 步只做 3 步」投影。

---

## 表三：轮 41 · 五文件一致性对照表（事实 / 五份文件各怎么说 / 是否一致）

> 五份文件 = 方法定稿 / 边表 / 场景清单 / 脚本(check-deps) / 架构基线草稿。补充：轮次报告（第1-3轮、双向核对）作旁证。

| 事实 | 方法定稿 | 边表(.txt) | 场景清单(.md) | 脚本(check-deps) | 架构基线草稿 | 是否一致 |
|---|---|---|---|---|---|---|
| 依赖边数 | 未声明（称边表为权威源） | 未声明（仅历史注释） | 「64 条边」（line11：「覆盖不了 24 个模块的 64 条边」） | **74 条边（A59/B6/D9）**（运行时输出） | 未声明（引用边表） | ❌ 场景清单 64 vs 脚本 74（旁证：第1-3轮报告写 75） |
| B 层数 | 未声明 | 未声明 | 未声明（双向核对旧文写 B7） | **B 层 6** | 未声明 | ❌ 脚本 6 vs 双向核对报告 7 vs 第1-3轮报告 8 |
| 场景数 | **「6~8 个」**（§七/§八步3/§十判据4） | 未声明 | **「9 个」**（标题+正文） | 未声明 | 未声明 | ❌ 方法 6~8 vs 场景清单 9 |
| 模块数 | 「24 个模块」（§三） | 未声明（§三引用 24） | 「24 个模块」（line11） | MODULES 集 = 24 | 「模块数 = 24」（§十六，唯一权威源） | ✅ 24 一致 |
| 节点数 | 未声明 | 未声明 | 「31 节点」（line11） | **节点 31 个**（运行时输出） | 未声明 | ✅ 31 一致 |
| 步骤数 | 「8 步」（§八） | — | — | — | — | ✅ 内部一致（但执行仅 ~3 步，见已知「8 步只做 3 步」） |
| 判据条数 | 「6 条」（§十） | — | — | — | — | ✅ 内部一致 |
| 节点规则含 `main`？ | ❌ 不含（只列 24 模块+E1-E7+qt/vtk） | ✅ 含（12 边用 main） | — | ✅ 含（EXTERNAL 含 main） | — | ❌ 方法漏 main |
| 禁边范围 `app→第三方库` | 「`app/* → 第三方库直连` 禁」（§五，REQ-COM-001） | 有 5 条 `app/*->qt/vtk`（用户定） | — | forbidden() 只查 core/comm/data→qt/vtk（**放行** app→qt/vtk） | — | ❌ 方法文字禁 vs 脚本放行 |

**结论**：模块数(24)、节点数(31)、步骤数(8)、判据数(6) 四者一致；**边数、B 边数、场景数、节点规则(`main`)、禁边范围** 五处不一致。其中边数三套口径 + 场景数 6-8/9 + 禁边/脚本范围 三处为会误导人的硬不一致（N1/N2/N3）。

---

## 表四：轮 42 · 引用可达性统计（类别 / 可达 / 总数 / 不可达逐条）

> 实测方法：`/tmp` 分析脚本解析 方法+边表+场景清单 的全部引用 token，对全仓库 `.md` 做存在性 grep（见正文末「脚本退出码」前说明）。逐条边（74 条）出处亦逐条打标（抽样列见下）。

| 类别 | 可达 | 总数 | 不可达（逐条） |
|---|---|---|---|
| § 阿拉伯数字（§7.2.1 / §14.2 / §16.1 / §17.5 / §17.1 等） | 5 | 5 | 无 |
| § 中文数字（§五 / §六 / §四 / §七 等） | 10 | 10 | 无 |
| `REQ-*` | 23 | 23 | 无 |
| `B-*` | 3 | 3 | 无 |
| `决策*` | 1 | 1 | 无 |
| `P-*` | 2 | 2 | 无 |
| `ARCH-*` | 1 | 1 | 无 |
| **合计** | **45** | **45** | **0** |

**逐条边出处可达性（74 条，全量，非抽样）**：E1–E74 的锚点（§五 / §7.2.1 / §17.5 / §14.2 / REQ-* / 决策17 / §六 / §四 / P-006 / P-5 / B-016 等）**全部 Y（可达）**，仅 4 条为「用户定」自由文本（E12 `app/main_window->qt`、E17 `app/scene3d->vtk`、E22 `app/dashboard->qt`、E26 `app/dialogs->qt`）无锚点、不可追溯。

**重要限定**：本表「可达」= **文本存在性**（锚点字符串在仓库某处出现）。未验证「语义一致性」——例如 §五 在架构基线里确为「数据所有权表」，但边表 `§五:设备状态(只读)` 是否真对应表中那一行语义，未逐行核对。可达性通过 ≠ 引用正确。

---

## 表五：轮 48 · 旧项目（`../new project/docs/REVIEW-LOG.md`）6 根因逐条对照

> 旧项目 6 根因：① 改完不同步引用点 ② 定义了量没定基准 ③ 自评覆盖性声明不可信 ④ 枚举式定义总有漏网项 ⑤ 载体缺失 ⑥ 常量无出处。

| 旧根因 | 在 `#5` 是否重犯 | `#5` 中的具体实证（附路径+原文） | 新根因？ |
|---|---|---|---|
| ① 改完不同步引用点 | ✅ 重犯 | 边计数 75/64/74 三套口径互不同步（N1）；方法§七/§八/§十「6~8 场景」未随场景清单扩到 9 同步（N2）；方法§三节点规则漏 `main`（N4）；边表 B 节混入 A 边（N5）；§六「ALLOWED_CYCLES 默认空」未随脚本加 1 条豁免同步（N8）。**机制根因**：`check-consistency.py` 只 glob `10-基线/*.md`，**完全不覆盖 `#5` 文件** ⇒ 无自动化闸门，计数漂移无人发现 | 否（根因①投影） |
| ② 定义了量没定基准 | ✅ 重犯 | 4 条「用户定」边无需求/节基准（N6）；方法§六「4 个环（13 节点/19 边/4 环）」数字无推导（N9）；方法§四「稳定度」属性**载体 `S1-模块表.md` 缺失** ⇒ 稳定度无落点 | 否（根因②投影） |
| ③ 自评覆盖性声明不可信 | ✅ 重犯 | 边计数自评（75/64）不准且未回验（=自评计数不可信，N1）；完成判据④「漏边 0 + 多边 0」依赖人工核对，而核对本身先查出 9 漏 + 3 多 ⇒ 自评不可信；复现性中「补边 9」靠一次性审计（dep-audit-forward 78 跳）无脚本留存（轮49） | 否（根因③投影） |
| ④ 枚举式定义总有漏网项 | ✅ 重犯 | 禁边清单与 `check-deps.py` 的 `forbidden()` 枚举不一致——后者漏 `app/* → qt/vtk`，前者文字却含「app/* → 第三方库直连」（N3）；方法§三节点枚举漏 `main`（N4）；双向核对「逐条」靠人 ⇒ `infra/telemetry` 孤立节点（已知）即漏网项 | 否（根因④投影） |
| ⑤ 载体缺失 | ✅ 重犯 | `S1-接口清单.md` 缺失（判据⑥载体，D）；`S1-依赖图.md` 缺失（§八步7 渲染物载体）；`S1-模块表.md` 缺失（§四稳定度载体，已知）；`#6` 接手 B 层清单缺「配置变更广播」事件通道（N7） | 否（根因⑤投影） |
| ⑥ 常量无出处 | ✅ 重犯 | 4 条「用户定」边决策无出处（N6）；方法§六「4 环/13 节点/19 边」数字无出处（N9）；方向判据中「用户定」未标依据 | 否（根因⑥投影） |

**结论**：旧项目 6 根因在 `#5` **全部重犯，0 个新根因**。最危险的是根因①——它本该被 `check-consistency.py`（其自身文档字符串就写着「纪律要求拦不住，只有可执行的检查拦得住」）拦住，但该脚本**不覆盖 `#5` 文件**，于是 `#5` 恰好犯了它最该防的错（计数漂移）。

---

## 结尾：总收敛小结

**① 本批（41-50）新增几条**：9 条（N1–N9，表二），均为已知 6 根因的重复投影；另 1 条已知 D（接口清单缺失）备查。

**② 重复投影几条**：9 条新发现全部为重复投影（根因①×5、④×2、⑥×2、②×2、③×1、⑤×1，跨根因叠加）；加上已知「8 步只做 3 步」含的接口清单/渲染图缺失，本轮确认其为根因⑤投影。

**③ 还剩几个新根因**：**0 个**。`#5` 这 10 个元层角度未发现超出旧项目 6 根因的新缺陷类型。

**④ 该停了吗 + 还剩什么必须做**：
- **不建议现在停**。依据：边计数仍在文档间漂移（75/64/74，N1）证明「改完回看」纪律在 `#5` 上未生效；`#5` 判据⑥（接口清单）未产出，宣布完成必被打回；禁边与脚本闸门范围不一致（N3）是真实漏洞。
- **必须做（按阻塞排序）**：
  1. **产出 `S1-接口清单.md`**（每模块 输入/输出/错误，24 模块全覆盖）—— 满足判据⑥，否则 `#5` 不可宣布完成（D）。
  2. **修 N3**：统一「禁边清单」与 `check-deps.py` 对 `app→qt/vtk` 的范围——方法§五 明确「app→qt/vtk（UI 框架）允许；core/comm/data→qt/vtk 禁」，并让脚本 `forbidden()` 与文字同口径（否则日后 `app→真实第三方通信库` 直连会被放行）。
  3. **把 `#5` 纳入 `check-consistency.py` 自动化闸门**（根因①机制根因）：加一项——用脚本解析边表实际边数/场景数，比对方法/场景清单/各报告里声称的「N 条边 / N 场景」，不一致即失败。这是防 N1/N2 复发的根本手段。
  4. **同步方法§七/§八/§十「6~8 场景」→ 9**（N2）；补方法§三节点规则中的 `main`（N4）；清边表 B 节里的 A 边或加注释（N5）；修§六「ALLOWED_CYCLES 默认空」措辞（N8）。
  5. **补 `#6` 交接的「配置变更广播」事件通道**（N7）：在 B 层清单显式登记 SC9 的配置变更事件，避免 `#6` 接手漏这条安全链。
  6. **（可选）S4 后实现 `check-deps.py` 源码 `#include` 交叉校验骨架**（已知「脚本 3 缺口」之一），使脚本绿≠真核对（N6/轮47 失效信号）。
- 上述 1–5 完成后，`#5` 可宣布完成、检查可停；第 6 项属增强，不阻塞。

---

## 附：脚本运行结果（收尾要求）

```
python 00-流程/check-deps.py      → 退出码 0
  输出：依赖边表核对：共 74 条边（A 层 59 · B 层 6 · D 层 9）｜ 节点 31 个
        ✅ ① A 层无环  ② 无禁边  ③ 端点合法  ④ 出处无行号

python 00-流程/check-consistency.py → 退出码 0
  输出：仅核对 10-基线/S-1-立项声明.md 与 S0-需求基线.md（共 23 项，失败 0）
        ⚠️ 注意：该脚本 glob 仅 10-基线/*.md，**不覆盖 20-设计/#5 任何文件** ⇒ #5 无自动化一致性闸门（见轮 47 / 表五 根因①）
```

> 引用可达性分析脚本（`/tmp/analyze_deps.py`，项目外临时文件，未写入仓库）：45/45 编号 token 全部可达，74/74 边出处锚点全部可达，4 条边出处为「用户定」自由文本不可追溯。
