# S1 `#5` 依赖图 · 循环第 2 批（轮 21~30）· 质量 / 验证 / 可维护类对抗检查

- **本文件装什么**：对 `S1-依赖边表.txt`（依赖图唯一权威源）做第 21~30 轮对抗性检查，10 个角度全部属于「质量 / 验证 / 可维护」类。本文件**只装核验结论，不改任何既有文件**，也不做 git 提交。
- **被核对象**：
  - `20-设计/S1-依赖边表.txt`（本轮实测 **74 条边 · A59/B6/D9 · 31 节点**，非任务书所述 75/A58/B8/D9）
  - `20-设计/S1-#5依赖图方法-定稿.md`（§二 方向判据 · §五 禁边 · §六 环处理 · §八 8 步 · §十 6 判据 · §三 B 层约定）
  - `20-设计/S1-关键场景清单.md`（9 场景 SC1~SC9）
  - `20-设计/S1-架构基线草稿.md`（§五 数据所有权 · §九 风险模块 · §十二 横切 · §16.1 24 模块）
  - `50-变更单/CR-S0-003-补演示脚本十步.md`（B-018 十步）
  - `20-设计/S1-#5双向核对-正向查漏.md`（上一轮 8 场景逐跳明细，本轮对照物之一）
  - `00-流程/check-deps.py` · `00-流程/check-consistency.py`（实测）
- **轮次范围**：21（可验证性）· 22（覆盖矩阵）· 23（B-018↔边表）· 24（面试可讲性）· 25（单人可维护）· 26（变更影响面）· 27（技术债标注）· 28（风险集中）· 29（可观测性）· 30（收敛自检）。
- **比对了哪些对象（满足"作废"条款）**：每条结论均对照上述 7 个文件 + 两脚本实测输出；轮 22 另以 `S1-#5双向核对-正向查漏.md` 的 8 场景逐跳明细与 `S1-关键场景清单.md` 的 SC9 为对照物独立复算。

---

## 实测脚本结果（收尾前先记，末尾复述）

```
python 00-流程/check-deps.py       ⇒ 退出码 0
  输出：共 74 条边（A 层 59 · B 层 6 · D 层 9）｜ 节点 31 个
  ① A 层无环 ② 无禁边 ③ 端点合法 ④ 出处无行号 ⇒ 全部通过

python 00-流程/check-consistency.py ⇒ 退出码 0
  （注：该脚本核对 10-基线/*.md，不核对 S1 边表内容；本轮仅记录其退出码）
```

> ⚠️ **关键实测事实**：边表实测为 **74 条边（A59/B6/D9）**，与任务书所述「75 条边 A58/B8/D9」不符（A 多 1 / B 少 2 / 总数少 1）。脚本自报数即本文所有统计的权威基准。

---

## 表一 · 轮次表（轮 / 角度 / 发现 / 归类 / 是否已知同根因投影）

| 轮 | 角度 | 核心发现（一句话） | 归类 | 是否已知项同根因投影 |
|---|---|---|---|---|
| 21 | 每条边的可验证性 | 74 条边**全部**属"设计期断言"；实现级验证被方法 §七 显式推迟到 S4，当前仅元数据可被脚本验；5 条 qt/vtk 边出处为"用户定"不可独立溯源 | I | 部分：§七"待S4"已知，但"每条边怎么验"未从逐条边层面框过 |
| 22 | 24×9 覆盖矩阵 | 独立复算：零覆盖模块 = `infra/telemetry`(0 边0场景) + `infra/build`(0 运行时场景,§三豁免)；单覆盖 = `core/vision`/`infra/lifecycle` | C | 是：「未覆盖活动5 / 粒度4」已知；本轮回测确认同样结论并定位到具体模块 |
| 23 | B-018 ↔ 边表 | 十步均能在边表找到支撑；但 `core/stats` **零条入边**（统计来源在边表无体现），且 SC6/SC7 安全链不在十步内（已另立场景） | C | 部分：漏边9已补为已知；"core/stats 无入边"为新投影 |
| 24 | 面试可讲性 | 亮点：唯一事实源/SPSC、事件解耦、依赖倒置、禁边纪律；露馅点：A43 同步破例、ALLOWED_CYCLES 豁免环、telemetry 零边 | I | 否：均为新视角梳理 |
| 25 | 单人可维护性 | 8 步方法仅完成约 3 步（接口清单/渲染图/冷却自审未果），74 边 + 73 步流程对单人秋招期负担重；最易被放弃 = 接口清单与渲染图 | C | 是：「8步只做3步」已知；"单人会放弃哪块"为新投影 |
| 26 | 变更影响面 | 5 个最可能变的需求各自牵动 2~8 条边；"加第9台"影响最深（device/concurrency/store/scene3d） | I | 否：新 |
| 27 | 技术债标注 | 边表无"例外/临时/技术债"字段；A43、ALLOWED_CYCLES 豁免环、A59 记录不一致本应标技术债却无法标注 | C | 否：新（字段缺失） |
| 28 | 风险集中 | 3 高风险模块（concurrency/scene3d/virtual-device）仅占约 18% 边；风险被**低估**而非"稀释"——边表给人"低耦合"假象 | I | 否：新（反向结论） |
| 29 | 可观测性覆盖 | `infra/telemetry` 在边表完全孤立（已知）；补 4 条边（telemetry 读 concurrency/device/scene3d + 发布指标）即可闭环 | C | 是：「telemetry 孤立」已知；"补哪几条边"为新 |
| 30 | 收敛性自检 | 本批新增约 8 条（I/C 级，无 D）；同根因投影约 4 条，新根因约 4 条（计数不符/缺失边/统计无入边/技术债字段缺）。**#5 尚未完全收敛** | — | — |

---

## 表二 · 新增 D / C / I 清单（本批，剔除已知重复）

| 编号 | 归类 | 发现 | 比对对象 / 原文引用 | 是否已知重复 |
|---|---|---|---|---|
| N1 | I | 边表实测 **74 条边(A59/B6/D9)**，与任务书「75(A58/B8/D9)」不符；B 段第 95 行 `infra/config -> infra/eventbus` 标 `A` 却落在 B 段，造成"按段数 B=8"错觉 | `S1-依赖边表.txt` 第95行 `infra/config -> infra/eventbus | A | usage | §7.2.1④ 广播配置变更`；`check-deps.py` 输出 `共 74 条边（A 层 59 · B 层 6 · D 层 9）` | 否（任务书数字未在校验过） |
| N2 | C | 文件顶部注释声称把 D#50 拆成 `infra/config -> infra/eventbus` + **`infra/eventbus -> core/device`** 两条，但边表里**只有前者、缺后者**——文档化修复未完整落表 | `S1-依赖边表.txt` 注释 `⇒ 拆为 infra/config -> infra/eventbus(pub) + infra/eventbus -> core/device(sub)`；全表检索 `infra/eventbus ->` 无任何边 | 否 |
| N3 | C | `core/stats` 在边表中**零条入边**（仅出边 `core/stats->core/device`、`core/stats->data/store`）；"产量统计"由谁计算/喂入在边表无体现，B-018 步9"统计"缺生产者支撑 | `S1-依赖边表.txt`：`core/stats -> core/device`、`core/stats -> data/store` 为其唯二出现；反向检索 `-> core/stats` 无结果 | 否（与"漏边9已补"不同根） |
| N4 | C | 边表格式仅有 3 字段（层/类型/出处），**无"例外/临时/技术债"标志位**；A43 同步破例、ALLOWED_CYCLES 豁免环、A59 记录不一致本应标技术债却无法标注，易被误读为普通依赖 | `S1-#5依赖图方法-定稿.md` §三 `格式：<使用者> -> <被使用者> | <A|B|D> | <类型> | <出处>`；`S1-依赖边表.txt` A43 `core/alarm -> core/execution … 同步停机命令，非事件` | 否（字段缺失） |
| N5 | I | 3 高风险模块（§九：concurrency/scene3d/virtual-device）仅占约 18% 边，边表**低估**其风险（非"稀释"）；与"风险被稀释"的直觉相反 | `S1-架构基线草稿.md` §九 `核心/concurrency … 每设备一线程+SPSC 无锁汇聚 … 你简历上最硬的技术点`；边表 concurrency 仅 5 条、scene3d 6 条、virtual-device 2 条 | 否（反向结论） |
| N6 | C | 可观测性：`infra/telemetry` 完全孤立，需补 4 条边（详见轮 29）才能在边表层面闭环 §12.2 的"队列深度/丢帧/线程健康/帧率" | `S1-架构基线草稿.md` §十二 A8 `可观测性 … infra/telemetry（队列深度 / 丢帧 / 线程健康 / 帧率）`；边表检索 `telemetry` 零命中 | 是（"孤立"已知；补边为新） |
| N7 | I | 轮 21 元结论：全部 74 条边当前**不可被实现级验证**（方法 §七 把源码交叉校验推迟到 S4）；唯一自动验证是 check-deps.py 的 4 项**元数据**校验；5 条 qt/vtk 边出处为"用户定"无法独立溯源 | `S1-#5依赖图方法-定稿.md` §七 `S1 阶段源码不存在 → 该步标记为"待 S4"`；`S1-依赖边表.txt` `app/scene3d -> qt | A | usage | 用户定(界面框架 Qt)` 等 | 否（逐条层面新） |
| N8 | C | 单人维护隐患：方法 §八 8 步仅约 3 步有产物（边表/脚本/补边），**接口清单(S1-接口清单.md)、渲染图、冷却自审无证据**；74 边 + 73 步流程对单人秋招期偏重 | `S1-#5依赖图方法-定稿.md` §八 步6 `S1-接口清单.md`、§九 `冷却期自审`；已知"8步只做3步" | 是（"8步只做3步"已知；"最易被放弃哪块"为新投影） |

> 本批**无 D 级**（无新增环、无新增禁边、脚本退出码 0；N1~N8 均不阻塞冻结，归入 I/C）。

---

## 表三 · 逐条边可验证性表（轮 21 产出，全部 74 条）

> 说明三列含义：
> - **建议验证手段**：该边将来靠 单测(UT) / 集成(IT=含虚拟下位机) / 演示(Demo) / 脚本(=check-deps 元数据) / 人工(审计出处) 哪种验。
> - **实现级**：全部 = **待 S4**（方法 §七 显式推迟源码交叉校验）；check-deps.py 已对**全部 74 条**做元数据验证（格式/环/禁边/出处），退出码 0。
> - **溯源级**：出处能否被独立追到 §/REQ（Y）/ 仅"用户定"不可独立验证（弱）。
> - **验不了的边算不算边**：在当前 S1 阶段，所有边都是"设计断言 + 可追溯出处"即算有效边（方法 §十 判据2）；但实现级一律待 S4，故"纸面边"与"已验边"目前无法区分——这是轮 21 的核心风险。

### A 层（59 条）

| # | 边 | 建议验证手段 | 实现级 | 溯源级 | 备注 |
|---|---|---|---|---|---|
| A1 | main -> infra/config | 脚本+演示 | 待S4 | Y | 组合根加载；仅"能启动"可验 |
| A2 | main -> core/device | 脚本+演示 | 待S4 | Y | 注入配置结构体 |
| A3 | main -> core/execution | 脚本+演示 | 待S4 | Y | 注入 |
| A4 | main -> core/concurrency | 脚本+演示 | 待S4 | Y | 注入 |
| A5 | main -> core/alarm | 脚本+演示 | 待S4 | Y | 注入 |
| A6 | main -> core/error | 脚本+演示 | 待S4 | Y | 注入 |
| A7 | main -> app/scene3d | 脚本+演示 | 待S4 | Y | 注入 |
| A8 | main -> app/dialogs | 脚本+演示 | 待S4 | Y | 注入 |
| A9 | main -> comm/interface | 脚本+演示 | 待S4 | Y | 注入 |
| A10 | main -> data/log | 脚本+演示 | 待S4 | Y | 注入 |
| A11 | app/main_window -> core/device | 单测+演示 | 待S4 | Y | §五设备状态只读 |
| A12 | app/main_window -> qt | 演示 | 待S4 | **弱** | 出处`用户定(界面框架 Qt)`，不可独立溯源 |
| A13 | app/scene3d -> core/device | 单测+演示 | 待S4 | Y | 只读 |
| A14 | app/scene3d -> core/mapping | 单测+演示 | 待S4 | Y | 坐标→几何映射只读 |
| A15 | app/scene3d -> core/program | 单测+演示 | 待S4 | Y | 程序/轨迹只读 |
| A16 | app/scene3d -> vtk | 集成+演示 | 待S4 | Y | `P-006(3D渲染用VTK)` |
| A17 | app/scene3d -> qt | 演示 | 待S4 | **弱** | `用户定` |
| A18 | app/dashboard -> core/device | 单测+演示 | 待S4 | Y | 只读 |
| A19 | app/dashboard -> core/alarm | 单测+演示 | 待S4 | Y | 报警只读 |
| A20 | app/dashboard -> core/stats | 单测+演示 | 待S4 | Y | REQ-MON-002/004 |
| A21 | app/dashboard -> data/store | 集成 | 待S4 | Y | REQ-MON-003 导出 |
| A22 | app/dashboard -> qt | 演示 | 待S4 | **弱** | `用户定` |
| A23 | app/dialogs -> core/execution | 单测+演示 | 待S4 | Y | REQ-RUN-010 点检确认 |
| A24 | app/dialogs -> core/device | 单测+演示 | 待S4 | Y | REQ-DEV-005 二次确认 |
| A25 | app/dashboard -> comm/interface | 集成+演示 | 待S4 | Y | REQ-COM-003 连接状态 |
| A26 | app/dialogs -> qt | 演示 | 待S4 | **弱** | `用户定` |
| A27 | core/device -> core/statemachine | 单测 | 待S4 | Y | REQ-STA-001；可单测状态集合 |
| A28 | core/execution -> core/device | 单测+集成 | 待S4 | Y | §五设备清单/参数 |
| A29 | core/execution -> core/program | 单测 | 待S4 | Y | §五程序/轨迹 |
| A30 | core/execution -> core/statemachine | 单测 | 待S4 | Y | REQ-RUN-004 急停迁移（强可测） |
| A31 | core/execution -> data/log | 单测 | 待S4 | Y | §五硬约束②唯一写者经接口 |
| A32 | core/execution -> data/store | 单测+集成 | 待S4 | Y | REQ-DB-001 |
| A33 | core/concurrency -> core/device | 单测(关键) | 待S4 | Y | §五只投递不持有；SPSC 可单测 |
| A34 | core/device -> core/concurrency | 单测(关键) | 待S4 | Y | §17.5 第一跳 |
| A35 | core/device -> infra/eventbus | 集成+演示 | 待S4 | Y | §17.5 唯一事实源→事件 |
| A36 | core/concurrency -> comm/interface | 集成 | 待S4 | Y | §17.5 第二跳 |
| A37 | core/concurrency -> data/log | 单测 | 待S4 | Y | REQ-LOG-005 通信日志 |
| A38 | core/error -> core/device | 单测+集成 | 待S4 | Y | 归正读实际位置 |
| A39 | core/error -> core/vision | 集成 | 待S4 | Y | REQ-VIS-003 |
| A40 | core/error -> data/log | 单测 | 待S4 | Y | REQ-LOG-007 |
| A41 | core/error -> data/store | 单测+集成 | 待S4 | Y | §五记录含误差日志 |
| A42 | core/alarm -> core/device | 单测+演示 | 待S4 | Y | §五设备状态 |
| A43 | core/alarm -> core/execution | 单测+演示 | 待S4 | Y | REQ-ALM-004 **同步停机破例**；需标例外(见N4) |
| A44 | core/alarm -> core/error | 单测 | 待S4 | Y | 误差只读 |
| A45 | core/alarm -> data/log | 单测 | 待S4 | Y | 报警记录 |
| A46 | core/stats -> core/device | 单测 | 待S4 | Y | §五设备状态；**但无入边(N3)** |
| A47 | comm/modbus -> comm/interface | 集成 | 待S4 | Y | REQ-COM-002 对外语义 |
| A48 | data/store -> infra/lifecycle | 单测 | 待S4 | Y | §六 ARCH-007 依赖倒置（接口在lifecycle） |
| A49 | main -> app/main_window | 脚本+演示 | 待S4 | Y | 组合根装配UI外壳（原漏边已补） |
| A50 | main -> app/dashboard | 脚本+演示 | 待S4 | Y | 组合根装配UI外壳（原漏边已补） |
| A51 | app/main_window -> core/program | 单测+演示 | 待S4 | Y | REQ-PRG-001/002（原漏边已补） |
| A52 | core/program -> data/log | 单测 | 待S4 | Y | §五硬约束②（原漏边已补） |
| A53 | app/dialogs -> data/log | 单测 | 待S4 | Y | REQ-LOG-001（原漏边已补） |
| A54 | core/device -> data/log | 单测 | 待S4 | Y | REQ-LOG-002（原漏边已补） |
| A55 | core/statemachine -> data/log | 单测 | 待S4 | Y | REQ-LOG-002（原漏边已补） |
| A56 | core/stats -> data/store | 单测+集成 | 待S4 | Y | REQ-MON-002（原漏边已补） |
| A57 | app/dashboard -> data/log | 单测 | 待S4 | Y | REQ-LOG-004（原可选漏边已补） |
| A58 | core/device -> data/store | 单测+集成 | 待S4 | Y | REQ-DEV-007（原可选漏边已补） |
| A59 | infra/config -> infra/eventbus | 集成+演示 | 待S4 | Y | §7.2.1④；注释称 pub/B 但记 A，记录不一致(N1/N2) |

### B 层（6 条）

| # | 边 | 建议验证手段 | 实现级 | 溯源级 | 备注 |
|---|---|---|---|---|---|
| B1 | core/device -> app/scene3d | 演示+集成 | 待S4 | Y | 决策17 状态→3D刷新 |
| B2 | core/device -> app/dashboard | 演示+集成 | 待S4 | Y | 决策17 状态→总览 |
| B3 | core/alarm -> app/dashboard | 演示+集成 | 待S4 | Y | 决策17 报警→呈现 |
| B4 | core/error -> core/alarm | 演示+集成 | 待S4 | Y | REQ-ERR-006 超阈值告警 |
| B5 | core/device -> core/alarm | 演示+集成 | 待S4 | Y | REQ-ALM-003 掉线→报警 |
| B6 | core/device -> core/error | 演示+集成 | 待S4 | Y | §17.5 状态变更触发偏差 |

### D 层（9 条）

| # | 边 | 建议验证手段 | 实现级 | 溯源级 | 备注 |
|---|---|---|---|---|---|
| D1 | comm/interface -> apps/virtual-device | 集成(虚拟下位机) | 待S4 | Y | §14.2 E2 Modbus下发 |
| D2 | apps/virtual-device -> comm/interface | 集成(虚拟下位机) | 待S4 | Y | §14.2 E2 上报 |
| D3 | app/main_window -> E1 | 演示 | 待S4 | Y | §14.2 E1 图形界面 |
| D4 | data/store -> E4 | 集成(文件API) | 待S4 | Y | §14.2 E4 文件API |
| D5 | data/log -> E4 | 集成(文件API) | 待S4 | Y | §14.2 E4 |
| D6 | infra/config -> E4 | 集成(配置文件) | 待S4 | Y | §14.2 E4 |
| D7 | data/log -> E5 | 集成(系统时钟) | 待S4 | Y | §14.2 E5 时间戳 |
| D8 | infra/build -> E6 | 演示(构建) | 待S4 | Y | §14.2 E6 构建环境；§三豁免非运行时 |
| D9 | core/alarm -> E7 | 集成(声光) | 待S4 | Y | REQ-ALM-002 首次确认关声光 |

**轮 21 小结**：74 条边中 **4 条（A12/A17/A22/A26）溯源级=弱**（出处为"用户定"，属框架选型断言，无法被需求反向证伪，但也不影响正确性）；**0 条**在当前阶段可被实现级验证（全待 S4）；**全部 74 条**元数据已被 check-deps.py 验证通过。结论：**"验不了的边算不算边"——算，但前提是出处可追溯；5 条弱出处边应补一句"依据 P-006/用户定"的来源说明以闭环溯源。**

---

## 表四 · 24×9 覆盖矩阵（轮 22 产出，独立复算 + 与上一轮差异）

> 复算方法：以 `S1-关键场景清单.md` 的 9 场景 + `S1-#5双向核对-正向查漏.md` 的 8 场景逐跳明细为对照物，逐模块判"该场景是否走到它"。符号：●=覆盖，空=未覆盖，括号内为覆盖场景数。
> **与上一轮差异说明**：上一轮（已知"未覆盖活动5 / 粒度4"）基于 8 场景；本轮补入 SC9（运行期改重要设置）并独立复算。结论方向一致——仍存在零覆盖与薄覆盖模块；差异在于本轮回测**精确定位到具体模块**：零覆盖 = `infra/telemetry`、`infra/build`(豁免)；单覆盖 = `core/vision`、`infra/lifecycle`。若上一轮"未覆盖活动5"指 5 个薄/零覆盖模块，本轮回测得到 2 零 + 2 单，与"5"量级相近但口径更细（上一轮矩阵原文未在本批可读取范围，故以方法层面比对，不谎称逐条对照）。

| 模块（24） | SC1 | SC2 | SC3 | SC4 | SC5 | SC6 | SC7 | SC8 | SC9 | 覆盖数 | 判语 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| app/main_window | ● | | | | | | | | ● | 2 | 薄 |
| app/scene3d | ● | | ● | ● | | ● | ● | ● | | 6 | 厚 |
| app/dashboard | ● | | | | ● | ● | ● | | | 4 | 中 |
| app/dialogs | | ● | ● | | | ● | | | ● | 4 | 中 |
| core/program | ● | | ● | ● | | | | ● | | 4 | 中 |
| core/execution | | | ● | | ● | ● | ● | | ● | 5 | 厚 |
| core/statemachine | | | ● | | | ● | | | | 2 | 薄 |
| core/device | ● | ● | ● | ● | ● | ● | ● | ● | | 8 | 枢纽 |
| core/concurrency | ● | ● | ● | ● | ● | ● | ● | | | 7 | 厚 |
| core/alarm | | | | | ● | | ● | | | 2 | 薄(安全核心却少) |
| core/stats | | | ● | | ● | | | | | 2 | 薄(且无入边,N3) |
| core/error | | | ● | ● | ● | | | | | 3 | 中 |
| core/vision | | | | ● | | | | | | 1 | **单覆盖** |
| core/mapping | | | ● | | | | | ● | | 2 | 薄 |
| comm/interface | ● | ● | ● | ● | ● | | ● | | | 6 | 厚 |
| comm/modbus | ● | ● | ● | ● | ● | | ● | | | 6 | 厚 |
| data/store | ● | | ● | ● | ● | | | | ● | 5 | 厚 |
| data/log | ● | ● | ● | ● | ● | ● | ● | ● | ● | 9 | 横切全覆盖 |
| infra/build | | | | | | | | | | 0 | **零(§三豁免,非运行时)** |
| infra/config | ● | | | | | | | | ● | 2 | 薄 |
| infra/eventbus | ● | | ● | ● | ● | ● | ● | | ● | 7 | 事件骨干 |
| infra/lifecycle | ● | | | | | | | | | 1 | **单覆盖** |
| infra/telemetry | | | | | | | | | | 0 | **零(完全孤立,N6)** |
| apps/virtual-device | ● | ● | ● | ● | ● | | | | | 5 | 厚 |

**轮 22 小结**：
- 零覆盖：`infra/build`（§三明文豁免构建期，合理但意味着它**不在任何依赖边也不在任何场景**，仅靠 CI 验证）、`infra/telemetry`（见轮 29）。
- 单覆盖：`core/vision`（仅 SC4 误差对位）、`infra/lifecycle`（仅 SC1 启动快照；崩溃恢复 NFR-003 未成独立场景）。
- `core/alarm` 作为安全核心仅 2 场景列出，但其边参与 SC5/SC7——属"列表明细薄、实际边厚"，非真缺口。
- 全部 24 模块中 **22 个** 至少 1 场景覆盖；**2 个零覆盖**（其中 1 个为豁免）。与上一轮"未覆盖活动5"结论方向一致，本轮回测给出更细的模块级定位。

---

## 轮 23 · B-018 十步 ↔ 边表映射

逐步映射（步源 `CR-S0-003-补演示脚本十步.md`）：

| B-018 步 | 内容 | 边表支撑 | 缺口 |
|---|---|---|---|
| 1 | 启动加载3D | `main->app/main_window`(A49) `main->app/scene3d`(A7) `app/scene3d->vtk`(A16) `app/scene3d->qt`(A17) | 无 |
| 2 | 连多台 | `comm/interface->apps/virtual-device`(D1) `apps/virtual-device->comm/interface`(D2) `comm/modbus->comm/interface`(A47) `app/dashboard->comm/interface`(A25) `core/device->core/concurrency`(A34) | 无 |
| 3 | 编程/校验 | `app/main_window->core/program`(A51) `app/scene3d->core/program`(A15) `core/program->data/log`(A52) `core/execution->core/program`(A29) | 无 |
| 4 | 校验 | `core/program` 内部（校验逻辑）；出边仅 `core/program->data/log`(A52) | 校验本身无独立边（内部，可接受） |
| 5 | 批量下发+二次确认 | `app/dialogs->core/device`(A24) `app/dialogs->core/execution`(A23) `core/device->core/concurrency`(A34) `core/concurrency->comm/interface`(A36) | 无 |
| 6 | 执行+3D映射 | `core/execution->core/device`(A28) `core/execution->core/statemachine`(A30) `core/device->core/concurrency`(A34) `core/concurrency->comm/interface`(A36) `core/device->app/scene3d`(B1) `core/device->app/dashboard`(B2) | 无 |
| 7 | 误差归正 | `core/device->core/error`(B6) `core/error->core/device`(A38) `core/error->core/vision`(A39) `core/error->data/log`(A40) `core/error->data/store`(A41) | 无 |
| 8 | 严重报警停机 | `core/error->core/alarm`(B4) `core/device->core/alarm`(B5) `core/alarm->core/execution`(A43) `core/alarm->core/error`(A44) `core/alarm->data/log`(A45) `core/alarm->E7`(D9) `core/alarm->app/dashboard`(B3) | 无 |
| 9 | 日志与统计查询 | `data/log` 多入边、`core/stats->data/store`(A56) `app/dashboard->data/store`(A21) `app/dashboard->data/log`(A57) | **`core/stats` 无入边(N3)**：统计由谁计算未体现 |
| 10 | 多设备3D呈现 | `app/scene3d->core/device`(A13) `app/scene3d->core/mapping`(A14) `core/device->app/scene3d`(B1) | 无 |

**结论**：十步均能在边表找到 A/B/D 级支撑；唯一结构性缺口 = **步 9 的"统计"缺生产者边（N3：`core/stats` 零入边）**。另：B-018 十步本身不含 SC6 急停 / SC7 掉线（已在场景清单另立），边表对这两步有支撑（SC6/SC7 边全命中），故非缺口。

---

## 轮 24 · 面试可讲性

**能讲的亮点（边表可作素材）**：
1. **唯一事实源 + SPSC**：`core/device` 唯一持有设备状态（`§五 硬约束①`），`core/concurrency` 只投递不持有（A33/A34），可讲"每设备一线程 + 无锁汇聚"——这是简历最硬点（`S1-架构基线草稿.md` §九）。
2. **事件解耦**：`core` 绝不依赖 `app`，状态变更走 `infra/eventbus`（B1~B6，决策17）——可讲"上下解耦靠事件"。
3. **依赖倒置**：`data/store -> infra/lifecycle`（A48）接口定义在 lifecycle、store 实现、装配点注入——可讲" infarction 不认识业务类型"。
4. **禁边纪律可脚本化**：check-deps.py 强制 `core→app`/`infra→业务`/`comm→业务` 等禁边，退出码 0——可讲"架构纪律可执行"。

**被追问会露馅**：
- A43 `core/alarm -> core/execution` 是**同步破例**（"非事件"），被问"为什么 alarm 能直接调 execution 而不是走事件"需答 P-5 安全优先——若答不出则露馅。
- `ALLOWED_CYCLES` 豁免了 `core/concurrency ↔ core/device` 2-环，被问"你不是说 A 层无环吗"需答"机制保证不死锁"——边表看不出豁免理由（脚本注释有，但边表无标注）。
- `infra/telemetry` **零边**，被问"50ms/SPSC 健康怎么观测"答不上（轮 29 补边可解）。
- 单向 `core/execution -> core/program`(A29) 但 program 不读 execution（工艺参数 ownership 裁 A）——边界微妙，易被追问。

---

## 轮 25 · 单人可维护性

- 方法 §八 定义 8 步，已知"8步只做3步"：当前产物 = 边表(A/B/D) + check-deps.py + 9 补边；**缺接口清单 `S1-接口清单.md`（步6）、渲染图（步7）、冷却自审证据（步8 §九）**。
- 74 条边 + 方法 8 步 + 架构基线 73 步流程，对单人 + 秋招档期偏重。**最可能被放弃的顺序**：① 接口清单（24 模块全覆盖，工作量大且无即时反馈）→ ② 渲染图（纯交付物）→ ③ 冷却期自审（无外部约束易跳过）。
- 建议：把"接口清单"与"冷却自审"写成**可脚本闸门的一部分**（如接口清单模板 + 自审日期戳），降低对自律的依赖。

---

## 轮 26 · 变更影响面（5 个最可能变的需求 → 牵动边）

| 可能变更 | 牵动边（建表） | 影响边数量 |
|---|---|---|
| 加轨迹基元（REQ-ARCH-005） | A29(执行→程序) A51(UI→程序) A15(scene3d→程序) A52(程序→log)；接口清单 core/program | 4 + 接口 |
| 换协议（§14.2 E2） | D1/D2(comm/interface↔virtual-device) A47(comm/modbus→interface)；因"业务依赖抽象"，**业务边不动** | 2 + 实现 |
| 加第 9 台（容量 8→9） | A2/A27/A28/A33/A34/A46/A58(device 系) A4/A33/A34(concurrency) A58(store 参数) A13/A14(scene3d 多实例) | 8+（最深的变更） |
| 改精度分档 | A14/A15(scene3d→mapping/program) + app/scene3d 内部 LOD；边表基本不动，动的是 scene3d 内部 | 2（边界内） |
| 加报警类型（REQ-ALM-003） | A42/A43/A44/A45(alarm 系) D9(alarm→E7) | 5 |

**结论**：最该在边表/接口里留扩展点的 = "加第9台"（牵动 device/concurrency/store/scene3d 多模块）与"换协议"（考验抽象是否真解耦）。其余变更边表层面影响可控。

---

## 轮 27 · 技术债标注

边表格式（§三）只有 3 字段，**无"例外/临时/技术债"标志位**。应被标注却无法标注的边/环：
- **A43** `core/alarm -> core/execution`：同步停机破例（"非事件"），是刻意的 P-5 安全例外，应标 `exception`，否则被误读为普通依赖。
- **`ALLOWED_CYCLES` 豁免环** `core/concurrency ↔ core/device`：脚本注释有理由，但边表本身不显示"此环被豁免"，读者看边表会以为有环。
- **A59** `infra/config -> infra/eventbus`：注释称应记 `pub/B`，实际记 `A`，记录本身不一致（N1/N2），应标 `record-inconsistent`。
- **缺失边 `infra/eventbus -> core/device`**（N2）：文档化修复未落表，属"未完成债务"。

**建议**：边表格式加第 4 可选字段 `| <flag>`（`normal/exception/temp/debt`），或在边表顶部为每条异常边加一行注释标签，使"例外"显式化、可脚本 grep。

---

## 轮 28 · 风险集中

§九 点名 3 高风险模块：`core/concurrency`（SPSC 最硬技术点）、`app/scene3d`（VTK+多实例 LOD）、`apps/virtual-device`（独立进程+通信）。
- 边表占比：concurrency 出现 5 条、scene3d 6 条、virtual-device 2 条，合计 **13/74 ≈ 18%**。
- **与"风险被稀释"直觉相反**：高风险模块在边表里占比低，给人"耦合低、风险小"的假象；但它们是**实现复杂度最高**处（SPSC 无锁、LOD、跨进程）。即风险被**低估**，非稀释。
- 叠加轮 29：`infra/telemetry` 零边 → 这些高风险模块的**健康度无法在边表/架构层面被观测**，风险闭环缺最后一环。

---

## 轮 29 · 可观测性覆盖（telemetry 孤立的补边方案）

`infra/telemetry` 在边表零命中（已知"孤立"），但 §12.2 要求它观测**队列深度 / 丢帧 / 线程健康 / 帧率**。补以下 4 条边即可在依赖图层面闭环：

| 建议补边 | 层 | 观测什么 | 对应 §12.2 项 |
|---|---|---|---|
| `infra/telemetry -> core/concurrency` | A | 队列深度 / 背压（呼应 A9 限流） | 队列深度 |
| `infra/telemetry -> core/device` | A | 线程健康（每设备线程存活） | 线程健康 |
| `infra/telemetry -> app/scene3d` | A | 帧率 / 丢帧（NFR-001 ≥10Hz） | 帧率 / 丢帧 |
| `core/concurrency -> infra/eventbus`(或 `-> infra/telemetry`) | B/A | 发布指标事件供 telemetry 订阅 | 横切采集 |

> 注：telemetry 作"贯穿观测者"宜构造注入读取（A 层 `telemetry -> X`），与 `infra/config`/`infra/eventbus` 同列"构造注入禁单例"（§八 约束2）。补这 4 条边后，`infra/telemetry` 从 0 边变为 4 入边（或被订阅），孤立问题解除。

---

## 轮 30 · 收敛性自检（本批汇总）

**本批新增条目（N1~N8）**：8 条，全部 I/C 级，**无 D**。
- **同根因投影（约 4 条，对应已知项）**：N6(telemetry 孤立，已知) · N8(8步只做3步，已知) · 轮22零覆盖(未覆盖活动5，已知) · 轮21中"§七 待S4"(已知)。
- **新根因（约 4 条，非已知）**：
  1. **N1 计数不符**（74 vs 75，A59/B6/D9 vs A58/B8/D9）—— 任务书数字未被校验过。
  2. **N2 缺失边 `infra/eventbus -> core/device`** —— 文档化修复未完整落表。
  3. **N3 `core/stats` 零入边** —— 统计来源在边表无体现（与"漏边9已补"不同根）。
  4. **N4 技术债字段缺失** —— 例外/临时边无法标注。
  （另 N5 风险被低估、N7 逐条边验证层面为全新视角。）

**`#5` 检查到这批为止，收敛了吗？**
→ **尚未完全收敛**。理由：仍识别出 4 个新根因（N1/N2/N3/N4），且其中 N2/N3 是**真实漏边/缺边**（虽不阻塞冻结，但违背方法 §十"双向核对漏边0"），N4 是**方法论缺口**（边表字段不足以表达例外）。
→ **建议下一轮闭环的 3 件事**：① 把 `infra/eventbus -> core/device`(N2) 与 `core/stats` 入边(N3) 补入边表并重跑脚本；② 边表加 `exception/debt` 标志位或顶部注释标签(N4)；③ 校正任务书/文档中的"75(A58/B8/D9)"为实测"74(A59/B6/D9)"(N1)。完成后 `#5` 可宣告收敛。

---

## 收尾 · 脚本退出码（仅核验，未改动任何文件）

```
python 00-流程/check-deps.py       ⇒ 退出码 0  （共 74 条边 A59/B6/D9 · 31 节点 · ①无环②无禁边③端点合法④出处无行号）
python 00-流程/check-consistency.py ⇒ 退出码 0  （核对 10-基线/*.md，23 项检查失败 0 项）
```

> 两脚本均通过；本文件所有结论基于上述实测 + 7 个被核对象的人工对照，未修改任何既有文件，未做 git 提交。
