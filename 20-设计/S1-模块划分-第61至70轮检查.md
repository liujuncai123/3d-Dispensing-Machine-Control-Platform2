# S1 模块划分 · 第 61~70 轮检查报告（**最后一轮**）

> ⚠️ **2026-09-21 更新（`CR-010`）**：模块计数由 **24 改为 25**（新增 `app/replay`，口径不变 = 数全部路径）。本文正文里的旧数（18 / 21 / 22 / 24）**一律作废**，以 `./S1-架构基线草稿.md` **§十六** 为唯一权威源。

> ⚠️ **计数作废声明（2026-09-20）**：本文件正文里的模块计数（18 / 21 / 22）**已作废**。
> **权威数 = 24**（用户已裁；口径 = 含独立部署单元 `apps/virtual-device`），定义见 `S1-架构基线草稿.md` **§十六**。

> **一句话**：用户把最后 10 轮当"**收敛验证**"用（"重复最好了"）。
> 结果：**"切错"类连续 50 轮为 0** ⇒ **结构收敛确认**；本轮 3 项新发现**全是"补约束/补说明"**。
> ⚠️ **但我如实报告：重复率 33%，低于上轮的 57%** —— 所以**不能只说"重复高"**，
> 真正的收敛判据是「**切错类为 0 + 22 模块连续 50 轮未变**」。

---

## 一、逐轮结果（10 轮，全新角度）

| 轮 | 角度 | 结果 | 判定 |
|---|---|---|---|
| 61 | **可插入性**（加新模块要动几个已有模块） | 加 `comm/opcua` ⇒ **只加自己** | 🔁 **重复**（轮 20 / 49） |
| 62 | **命名与职责匹配**（名字说的和做的一致吗） | `core/device` / `core/mapping` / `core/concurrency` 名字与职责**都匹配** | ✅ **无问题** |
| 63 | 数据格式转换点 | 3 个转换点（字节→业务 / 业务→几何 / 业务→存储）⇒ 需约定"转换不丢信息" | → A4 |
| **64** | **启动成本** | 🔴 **`app/scene3d`（VTK 初始化）与 `comm`（连接超时）是启动瓶颈** ⇒ **应异步启动**（UI 先出、后台连设备） | **★本层（新）** |
| **65** | **隐式依赖（单例/全局）** | 🔴 **`infra/config` 与 `infra/eventbus` 极易退化为"隐式全局依赖"** ⇒ 应约束"**构造注入，不用单例**" | **★本层（新）** |
| 66 | 测试替身需求 | `comm/interface` mock · 虚拟下位机用真的 | 🔁 **重复**（轮 33 / 8） |
| 67 | 日志密度 | `LOG-007`（每点一条）会让数据量爆炸 ⇒ 但 `LOG-009` 保留策略已兜住 | 🔁 **重复**（轮 15） |
| 68 | 配置读取时机 | "配置改了是否**立即生效**"未定 | → A4 |
| **69** | **错误码归属** | 🔴 **`REQ-NFR-007`（错误码/故障代码对照表）无归属模块** | **★本层（新）** |
| 70 | 最终收敛检验 | 汇总见下 | — |

---

## 二、🔑 关键判据：**「切错类」连续 50 轮为 0**

| 批次 | 本层新根因 | **其中"切错"** | "补缺" | 跨层 | **重复** |
|---|---|---|---|---|---|
| 1~10 | 4 | **4** | 0 | 0 | — |
| 11~20 | 5 | **5** | 0 | 0 | — |
| 21~30 | 2 | **0** | 2 | 6 | — |
| 31~50 | 3 | **0** | 3 | 11 | — |
| **51~60** | 3 | **0** | 3 | 2 | **4**（57%） |
| **61~70** | **3** | **0** | **3** | 2 | **3**（33%） |

> ### ✅ **「切错」类：4 → 5 → 0 → 0 → 0 → 0**
> ⇒ **22 个模块的划分，连续 50 轮（第 21~70 轮）没有被推翻过一次。**
>
> ### ⚠️ 但"重复率"这个指标**不稳定**（57% → 33%）
> ⇒ 所以**收敛的真正判据不是"重复率高"**，而是：
> **①「切错」类为 0** ＋ **② 22 模块连续 50 轮未变** ＋ **③ 新发现全是"补缺"**

---

## 三、本轮的 3 项新发现（**又是"补约束"，不动结构**）

| 轮 | 补什么 | 为什么需要 |
|---|---|---|
| **64** | **启动必须异步**（UI 先出，`comm`/`scene3d` 后台初始化） | 否则 VTK 初始化 + 连接超时会让**启动界面卡住**（违反 `NFR-002` 的"响应可用"） |
| **65** | **禁止单例**（`config` / `eventbus` 必须**构造注入**） | 单例会造成**隐式全局依赖** ⇒ 单测无法替换、依赖关系看不见 |
| **69** | **错误码表归属 = `infra`** | `REQ-NFR-007` 要求"错误码/故障代码对照表"，但**没有模块负责维护它** |

---

## 四、A3 的完整产出清单（**现在齐了**）

| # | 产出 | 状态 |
|---|---|---|
| 1 | 22 个模块 + 层归属 | ✅ |
| 2 | 需求 → 模块 映射表（127 条） | 🔴 待补 |
| 3 | 数据所有权表 | 🔴 待补 |
| 4 | 配置项 → 模块 清单 | 🔴 待补 |
| 5 | 契约 / 实现细节 分界清单 | 🔴 待补 |
| 6 | **高风险 3 模块说明** | 🔴 待补 |
| 7 | **A3 约束清单**（本轮新增 2 条）🆕 | 🔴 待补 |

**第 7 项的 2 条约束**（本轮 64 / 65）：
- 启动必须**异步**（UI 先行）
- **禁止单例**（`config`/`eventbus` 构造注入）

**另**：错误码表归 `infra`（本轮 69）→ 并入第 4 项清单。

---

## 五、最终结论

| 项 | 结论 |
|---|---|
| **22 个模块** | ✅ **稳定 —— 连续 50 轮未被推翻** |
| **"切错"类** | ✅ **连续 50 轮为 0** |
| **剩余工作** | **机械补 6 张清单/说明**（全部是"补缺"，**不动结构**） |
| **建议** | 🔴 **停止检查，补清单，进 A4** |

> 🔴 **我的最终判断**：本轮重复率 33%（比上轮低）⇒ **说明"重复率"不是好判据**。
> **真正该看的是「切错类是否为 0」——它已经连续 50 轮为 0。**
> ⇒ **模块划分这件事做完了。**
