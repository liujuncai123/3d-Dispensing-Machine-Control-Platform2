# S1·A3 产出 · 自主检查 30 轮 + 验收者检查 20 轮

> **任务**：① **自主检查 30 轮**（我主动找问题）② **验收者检查 20 轮**（站在验收者立场判断"能不能交付"）
> **对象**：A3 的 **8 项产出**（S1 草稿 `§三` ~ `§十`）

---

# 第一段 · 自主检查 30 轮

**方法**：聚焦**新补的 5 节**（§六~§十），每节 6 轮。

## §六 约束落点表（轮 1~6）

| 轮 | 角度 | 结果 |
|---|---|---|
| 1 | 17 条是否列全 | ✅ 6+3+8=17 |
| 2 | 每条有明确落点吗 | ✅（`NFR-004/005` 落"全模块"，合理） |
| 3 | 落点都是 22 模块吗 | ✅ |
| 4 | 与映射表一致吗 | ✅（映射表已指向 §六） |
| 5 | 有无落到"不存在模块" | ✅（`infra/lifecycle` 是 v3 新增，存在） |
| 6 | 可执行性 | ✅ |

**⇒ §六 无新问题。**

## §七 配置项清单（轮 7~12）

| 轮 | 角度 | 结果 |
|---|---|---|
| 7 | 12 项覆盖所有"可配置"需求吗 | ✅ |
| 8 | 每项都有推荐标准吗 | ⚠️ 2 项写"待 `AS-003` 定"（**无数值**） |
| 9 | 所属模块正确吗 | ✅ |
| 10 | 与"禁单例"一致吗 | ✅（已写"构造注入"） |
| 11 | 漏项检查 | ⚠️ 缺"日志级别"等（小） |
| **12** | **`REQ-INF-006` 判据④"安全项不可配置"体现了吗** | 🔴 **未体现** —— 清单里**没有"哪些不可配置"** |

**⇒ 发现 `P1`**：配置项清单缺**「不可配置项」**（而 `REQ-INF-006` 判据④明确要求）。

## §八 A3 约束清单（轮 13~18）

| 轮 | 角度 | 结果 |
|---|---|---|
| 13 | 6 条都可判定吗 | ⚠️ "启动必须异步"需定义判据（如"UI 在 X ms 内可交互"） |
| 14 | 与 §五 硬约束重复吗 | ⚠️ 第 4/5/6 条是 §五 的重述（合理但应注明"同 §五"） |
| 15 | 3 条带进 A4 的明确吗 | ✅ |
| 16 | 有没有漏的约束 | ✅（事件通道/急停双路径/存储串行化已列） |
| **17** | **违反后果写了吗** | 🔴 **未写** |
| **18** | **谁负责验证写了吗** | 🔴 **未写** |

**⇒ 发现 `P2`**：约束缺「**违反后果 + 谁验证**」⇒ 无法判定"是否被违反"。

## §九 风险模块说明（轮 19~24）

| 轮 | 角度 | 结果 |
|---|---|---|
| 19 | 3 个真是最高风险吗 | ⚠️ 可辩（`core/statemachine` 也不低） |
| 20 | "为什么优先"说清了吗 | ✅ |
| **21** | **有缓解措施吗** | 🔴 **没有** —— 只说"优先做"，**没说怎么降风险** |
| 22 | 有风险等级吗 | ⚠️ 未标 |
| **23** | **与 `SLICE-00` 的关系** | 🔴 **矛盾！** 风险说"`core/concurrency` 优先做（早暴露）"，<br>但 `SLICE-00`（编程序→校验→单机下发→执行→3D）**根本不涉及多设备并发** |
| 24 | 照着做能降风险吗 | ⚠️ 不能（因为 §24 的矛盾） |

**⇒ 发现 `P3`（缺缓解措施）+ `P4`（🔴 与切片顺序矛盾）**

### 🔴 `P4` 详解（最重的一条）

**矛盾**：
- 风险表说 `core/concurrency`（多设备并发）要**优先做**（早暴露）
- 但 `SLICE-00` 是**单台最小闭环**，**不含多设备** ⇒ 并发风险**根本不在第一片里**

**可能的解法（待你选）**：
| 选项 | 做法 |
|---|---|
| **① 第一片就用"单设备但保留线程结构"** ✅ | 即使只有 1 台也走"每设备一线程 + SPSC 汇聚" ⇒ **并发架构在第一片被验证** |
| ② 接受矛盾，把并发风险挪到第二片 | 第一片纯单线程（简单），并发留到加多设备时 |
| ③ 第一片就做 2 台 | 稍微超 `G1/G2`，但直接验证并发 |

## §十 字段定义归属（轮 25~30）

| 轮 | 角度 | 结果 |
|---|---|---|
| 25 | 覆盖完整吗 | ⚠️ 9 类数据里**少了"报警"** |
| 26 | 待定项标了吗 | ✅（"数据字典"标了待 A4） |
| 27 | 与 §五 一致吗 | ⚠️ 因轮 25 而不完全一致 |
| 28 | 可执行吗 | ✅ |
| **29** | **漏的数据类** | 🔴 **"报警的字段定义"没列**（`REQ-ALM-*` 有三级/类型等字段） |
| 30 | 与 `REQ-DB-005` 判据③一致吗 | ✅（已写"状态复用 8 态"） |

**⇒ 发现 `P5`**：字段定义归属漏了**"报警"**。

---

# 📊 第一段汇总（30 轮 → 5 项发现）

| 编号 | 发现 | 级别 |
|---|---|---|
| **P1** | 配置项清单缺**「不可配置项」** | 🔴 |
| P2 | 约束缺「**违反后果 + 谁验证**」 | ⚠️ |
| **P3** | 风险说明缺「**缓解措施**」 | 🔴 |
| **P4** | 🔴 **风险模块（并发）不在 `SLICE-00` 里 ⇒ 与"早暴露"矛盾** | 🔴🔴 **最重** |
| **P5** | 字段定义归属漏「**报警**」 | 🔴 |

---

# 第二段 · 验收者检查 20 轮

**方法**：不问"还能改什么"，只问"**这份产出能不能交付 / 能不能支撑下游**"。

| 轮 | 验收角度 | 判定 |
|---|---|---|
| 1 | **交付物完整性**（8 项齐吗） | ✅ 8/8 |
| 2 | 每条产出**可判定**吗 | ⚠️ 部分（§八 约束缺判据 ⇒ P2） |
| 3 | 与**上游**（S0 v1.2 / S-1 v1.9）一致吗 | ✅ |
| 4 | 与**下游**（A4/A7/A8）可达吗 | ✅ 3 条明确带进 A4 |
| 5 | **数字准确**吗 | ✅ 127 / 17 / 12 / 6 / 3 实测一致 |
| 6 | **有无自相矛盾** | 🔴 **有 1 处**（P4：风险 vs 切片顺序） |
| 7 | 有无"**无法执行**"的条目 | ⚠️ P2（约束无法验证） |
| 8 | 命名 / 格式一致吗 | ✅ |
| 9 | **引用可达**吗（REQ/B 编号都存在） | ✅ 实测无断链 |
| 10 | 有无"**承诺了但没做**" | 🔴 P1 / P3 / P5 |
| 11 | **单条产出质量** | ⚠️ §七/§八/§九 偏薄（P1/P2/P3） |
| 12 | 能否**支撑开工** | ⚠️ **补 3 项后可以** |
| 13 | **风险已披露**吗 | ⚠️ 部分（缺缓解措施） |
| 14 | **需求可追溯**吗 | ✅ 127 条全映射 |
| 15 | 有**过度设计**吗 | ✅ 无 |
| 16 | 有**设计不足**吗 | 🔴 P4 |
| 17 | 术语一致吗 | ✅ |
| 18 | 格式可读吗 | ✅（381 行，结构清晰） |
| 19 | **能交给别人执行吗** | ⚠️ 需补缓解措施（P3） |
| 20 | **最终验收判定** | 🟡 **有条件通过** |

---

# 🎯 验收者最终判定

> ## 🟡 **有条件通过（Conditional Pass）**
>
> **通过条件（4 项）**：
> | # | 条件 | 对应 |
> |---|---|---|
> | 1 | 配置项清单补「**不可配置项**」 | P1 |
> | 2 | 风险说明补「**缓解措施**」 | P3 |
> | 3 | 字段定义归属补「**报警**」 | P5 |
> | 4 | **澄清 P4**（并发风险与 `SLICE-00` 的关系） | P4 |
>
> **另**：P2（约束缺违反后果/验收人）**建议补**，但不阻塞。

## 🔑 验收者视角的三条评语

| # | 评语 |
|---|---|
| **①** | **结构完整、数字准确、引用无断链** —— 这三点做得好（实测过） |
| **②** | **问题集中在"新补的 5 节"**（§七/§八/§九/§十）—— 说明**老产出（§三~§五）质量高，新补的赶工了** |
| **③** | **P4 是唯一的"逻辑硬伤"**（不是缺项，是**矛盾**）⇒ **必须澄清，不能绕过** |
