# S0 检查指引 —— 你要检查什么

> **一句话**：S0 已展开完毕（111 条，形态判据 9 项全过）；本文件告诉你**看什么、按什么顺序、重点问什么**。
> **权威源**：`./S0-需求条目表-初稿.md`（1201 行）

---

## 〇、先给结论：**B 方案已足够**（9 项形态判据）

| # | 判据 | 结果 |
|---|---|---|
| 1 | **完整性**：111 条是否都以展开形式存在 | ✅ 深度卡片 51 + 标准行 60 = **111** |
| 2 | **深度卡片字段完整性**（10 字段） | ✅ 51 张，**字段不全 0** |
| 3 | **标准行字段完整性**（7 列） | ✅ 60 行，**列数≠7 的 0** |
| 4 | **判据 ≥2 条** | ⚠️ **2 条不足**（`REQ-3D-011` / `REQ-NFR-001`）—— **原因见下** |
| 5 | **模糊词**（尽量/合理/差不多…） | ✅ **无** |
| 6 | **优先级覆盖**（P0/P1/P2） | ✅ 60/60 |
| 7 | **可测性覆盖**（单测/演示/人工） | ✅ 60/60 |
| 8 | **S-1 §6 点名的 5 个单测对象** | ✅ **5/5 都有条目** |
| 9 | **依赖可达性** | ✅ 49 个依赖**全部可达** |

### ⚠️ 第 4 项那 2 条的真相（**重要**）

`REQ-3D-011`（3D 刷新率）与 `REQ-NFR-001`（3D 视区刷新率）**判据为空** ——
**不是展开不够，是它们的数值卡在 `AS-001`（刷新率未定）上。**

> ⇒ 这恰好印证：**`R3` 缺一句「判据为空不得进基线」**。
> ⇒ **只要 `AS-001` 定了值，这 2 条立刻可验收。** 建议走变更单把该句补进 S-1 §七。

---

## 一、当前内容 —— **先看这 3 处就够**（约 30 分钟）

| 顺序 | 看什么 | 在哪 | 行数 | 为什么先看 |
|---|---|---|---|---|
| **1** | **〇 头部**：裁决记录 / **术语定义** / 待裁项 / 覆盖方法修正 | 文件开头 ~1–70 行 | ~70 | **2 分钟掌握 S0 全貌**；尤其**术语定义**（"程序 = 路径 + 参数"）是我刚定的，最可能与你理解不同 |
| **2** | **批 1**：D5 运行控制（9 条）+ D13 状态迁移（4 条） | `# 条目展开` → `## 批1` | ~130 | **安全相关 + 面试必问 + 逻辑最复杂** |
| **3** | **批 2**：D8 虚拟下位机 + D8b I/O + D9 误差归正（13 条） | `## 批2` | ~190 | **核心闭环所在**（"能演示"的根本） |

> **只看这 3 处，你就掌握了 S0 里最关键的 26 条 + 全部术语与裁决。**

---

## 二、全部内容 —— 完整清单（按需抽查）

| 批 | 内容 | 条数 | 位置 |
|---|---|---|---|
| — | **〇 头部** | — | 开头 |
| **1** | D5 运行控制 + D13 状态迁移 | 13 | `## 批1` |
| **2** | D8 虚拟下位机 + D8b I/O + D9 误差归正 | 13 | `## 批2` |
| **3** | D4 设备管理 + D10 通信 | 11 | `## 批3` |
| **4** | D1 3D 场景 + D2 程序 + D3 校验 | 24 | `## 批4` |
| **5** | D6 工艺参数 + D7 视觉 + D11 基础设施 + N1~N5 | 32 | `## 批5` |
| **6** | F 非功能 + X 结构 + REQ-ARCH 架构冗余 | 18 | `## 批6` |
| — | **速查索引**（111 行 ID→条目名） | — | `# 附录` |
| — | **合计** | **111** | — |

> 也可以直接看 `./S0-展开稿/` 下的 6 个批次文件 —— **内容与权威源一致**，但每批独立、更好读。

---

## 三、看的时候重点问 **4 个问题**

| # | 问题 | 为什么问这个 | 哪类条目最容易出问题 |
|---|---|---|---|
| **1** | **这条的"验收判据"能不能打勾？** | 判据不可打勾 ⇒ 需求无法验收 | 判据 ≥3 条的深度卡片 |
| **2** | **"异常 / 边界"栏我写得对不对？** | **这是新增字段，最能暴露我对你意图的理解偏差** | 全部深度卡片（51 张） |
| **3** | **"依赖"栏有没有漏 / 错？** | 漏了依赖 ⇒ 将来做的时候才发现被卡 | 涉及 **`B5`** 的 6 条（见下） |
| **4** | **优先级 P0/P1/P2 分得对不对？** | **它决定将来时间紧时先砍什么** | P2 那批（约 20 条） |

---

## 四、⚠️ 别忘了这几件必须由你裁的

### 4.1 🔴 **`B5` 最优先**（6 条条目依赖它）

| 依赖 `B5` 的条目 |
|---|
| `REQ-RUN-004`（急停本地优先）· `REQ-RUN-006`（回零）· `REQ-RUN-009`（掉线即停）· `REQ-VMC-006`（独立进程）· `REQ-COM-004`（掉线检测）|

> **`B5`（虚拟下位机是否走通信）不定 ⇒ "本地优先"和"掉线即停"的边界就定不了。**

### 4.2 其余待裁（详见文件 〇.4）

| 组 | 项 |
|---|---|
| **8 项** | 编号规则 · `B-018` 演示脚本 · `B-030` 视觉形式 · `B-020` 状态集合 · `B-021` 持久化选型 · `B5` · `B1` 参数集归属 · D 级 U4/U5 |
| **3 个数值** | `AS-001` 刷新率 · `AS-002` 台数 · `AS-005` 精度分级 |
| **1 个可选** | `U10` 故障注入：做 or 预留 |

### 4.3 各批末尾的「本批自检」——**共约 18 项**

这些是我**明确标出"不敢替你定"**的点，例如：
- `REQ-3D-005` 与 `REQ-3D-008` 的重叠怎么切
- `REQ-3D-004` 是否加"色觉友好"双通道
- `REQ-DEV-006` 故障台要不要自动停机
- `REQ-MON-001` 与 `REQ-DEV-001` 的边界
- `REQ-LOG-002` 记录粒度 · `REQ-NFR-003` 恢复程度
- `REQ-ERR-005` "误差变小"在随机扰动下不总成立（我建议改"均值变小"）

---

## 五、你回我任意一件，我就往下走

| 你回 | 我做什么 |
|---|---|
| `S0 通过，冻结` | 跑闸门（母清单 8 角度 + 脚本）→ 冻结 → 落 `10-基线/S0-需求基线.md` → 进 S1 架构 |
| 指出某条不对 | 改那一条，并回验 |
| 裁掉待裁项 | 落库，并更新受影响的条目 |
| `还有问题` | 再查一轮（但我如实说：9 项形态判据已过，再查的边际收益可能很低） |
