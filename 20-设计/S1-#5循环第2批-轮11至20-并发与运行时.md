# S1 `#5` 依赖图 · 循环第 2 批（轮 11~20）· 并发 / 运行时 类对抗检查

- **本文件装什么**：对 `S1-依赖边表.txt`（依赖图唯一权威源）做第 11~20 轮对抗性检查，10 个角度全部属于「并发 / 运行时」类（线程归属、跨线程边、锁/队列边界、事件通道线程安全、启动序、关闭序、死锁、资源释放、背压、超时/重试）。**本文件只装核验结论，不改任何既有文件，也不做 git 提交。**
- **被检对象**：
  - `20-设计/S1-依赖边表.txt`（实测 **74 条边 · A59/B6/D9 · 31 节点**：A 层 59 · B 层 6 · D 层 9；由 `00-流程/check-deps.py` 实报；任务书称「75 条边 A58/B8/D9」，该计数差已由**轮21至30 的 N1** 记录，非本批新发现）
  - `20-设计/S1-#5依赖图方法-定稿.md`（§一 四层/C层不画成图 · §三 边表格式/B层约定 · §五 禁边 · §六 环处理 · §十 判据）
  - `20-设计/S1-架构基线草稿.md`（§五 数据所有权/硬约束①~③ · §七 配置项 · §八 约束清单 · §九 风险模块 · §12.2 横切 12 项待设计(A9/A10/A11/B8…) · §16.1 24 模块 · §17.2 决策9/11/16/17）
  - `20-设计/S1-关键场景清单.md`（9 场景 SC1~SC9）
  - `00-流程/check-deps.py` · `00-流程/check-consistency.py`（实测）
- **轮次范围**：11（线程归属）· 12（跨线程边安全性）· 13（锁/队列边界）· 14（事件通道线程安全）· 15（启动序 vs 依赖方向）· 16（关闭序逆序）· 17（死锁可能性）· 18（资源释放责任链）· 19（背压/限流）· 20（超时/重试/幂等）。
- **已存在的同批文件（须避免重复）**：`S1-#5循环第2批-轮21至30-质量与验证.md`（N1 计数差、N2 缺边、N3 stats零入边、N4 技术债字段、N6 telemetry孤立、N7 不可实现级验证）、`S1-#5循环第2批-轮31至40-对抗红队.md`（F32 满队列/超时=A9~A11/B8 投影、F33 错误缺上行边、F34-2 事件订阅者缺 `X->infra/eventbus` 注入边【D级】、F35 concurrency/data sink 瓶颈、F36 data/* 无归档、F37 第三方无版本锁定、F38 concurrency 单中继/单台隔离需#6、F39 输入验证 1/3 入口、F31-2 eventbus 重放语义未定）。本批每条均标注「是否已知项的同根因投影」，已覆盖者标注「投影」，不重复报。
- **比对了哪些对象（满足"作废"条款）**：每条结论均对照上述 5 个文件 + 两脚本实测输出；轮 11/12 另以 `S1-#5循环第2批-轮21至30` 的 A1~A59/B1~B6/D1~D9 逐条边表与 `S1-#5循环第2批-轮31至40` 的 F32/F34-2/F35/F38 为对照，确认重叠口径。

---

## 实测脚本结果（收尾前先记，末尾复述）

```
python 00-流程/check-deps.py       ⇒ 退出码 0
  输出：共 74 条边（A 层 59 · B 层 6 · D 层 9）｜ 节点 31 个
  ① A 层无环 ② 无禁边 ③ 端点合法 ④ 出处无行号 ⇒ 全部通过

python 00-流程/check-consistency.py ⇒ 退出码 0
  （注：该脚本核对 10-基线/*.md，不核对 S1 边表内容；本轮仅记录其退出码）
```

> 关键实测基准：边表 74 条边（A59/B6/D9）；A 层唯一环 = 合法豁免环 `core/concurrency ↔ core/device`（check-deps.py `ALLOWED_CYCLES` 仅 1 条）；`infra/telemetry` 为 0 边孤立节点（轮21至30 N6 已记）。

---

## 表一 · 轮次表（轮 / 角度 / 发现 / 归类 / 是否已知同根因投影）

| 轮 | 角度 | 核心发现（一句话） | 归类 | 是否已知项同根因投影 |
|---|---|---|---|---|
| 11 | 线程归属（P-6 要求） | 24 模块仅 10 个线程归属可定；**14 个（含 `core/device` 关键持有者）在 #5 与 #6 引用文本中均未指明**；`apps/virtual-device` 为独立进程。全量枚举见 表三 | I | 部分：一般"线程归属不在 #5（C层）"已知（F38-3 投影）；"14/24 未定 + core/device 关键未定"的**全量枚举为新** |
| 12 | 跨线程边的安全性 | 跨线程边清单见 表四：app→core/data/comm 约 14 条 UI↔后台边 + 3 条 B 事件（core→app）+ 2 条 D 跨进程 + 5 条 concurrency↔device/comm/log 投递边；**#5 对全部跨线程边零同步/投递标注**。结论：根因同 RT-13/CT-1（usage 不区分 sync/async）+ CT-3（线程未定） | C | 是：F38-3（隔离需#6）、F38-2（concurrency 单中继）、F35-1（concurrency 单点）已投影；**表四的逐边枚举为新** |
| 13 | 锁/队列边界 | **CT-1（新）**：A 层边类型固定 `usage`，不区分"同步调用"与"异步投递(enqueue)"；两条合法环边 `core/concurrency -> core/device`、`core/device -> core/concurrency` 在边表中都是 `\| A \| usage \|`，"投递"只写在出处注释 → #5 无法表达 B-017b 机制本身、A9 背压、A11 插队。另实测 core/concurrency 触边 **5 条**（非轮13假设的 6 条） | C | 部分：A9/A11 并发待设计已知（F32/F35 投影）；"边类型词汇不区分 sync/async 投递"为**新表达力角度** |
| 14 | 事件通道线程安全 | B 层实测 **6 条**（非任务书/简报所述 8 条，计数差见 N1）；B 事件 `core/device->app/scene3d/dashboard`、`core/alarm->app/dashboard` 从后台发往 UI 线程**需线程跳（queued connection）**，#5 无标注；订阅者缺 `X->infra/eventbus` 注入边已由 F34-2【D】覆盖，本轮回其补充"跨线程投递机制未注明" | C | 是：F34-2（D，订阅者缺边）已覆盖主缺口；"core→app 事件需线程跳"为同根因下**新小点 CT-9**；B 层数差 N1 已知 |
| 15 | 启动序 vs 依赖方向 | A 层唯一环即合法豁免环；去豁免后无真环 ⇒ 无"依赖成环但启动不能成环"；但该 2-环使 A 层**无法单独推出启动顺序**，须 #6 补线程/启动序（CT-6） | I | 部分："合法环已知""C层不在#5"投影；"启动序无法由#5推出"为新 |
| 16 | 关闭序（逆序） | #5 完全不含关闭/启动序；`data/log` 单写者被 9 条 A 边 fan-in，释放竞争 #5 不可见；"优雅关闭"（§八 约束1）零表达（CT-7） | C | 部分：F36-2（data/log 串行点）投影；"关闭序零表达 + fan-in 释放竞争"为新 |
| 17 | 死锁可能性 | CT-2（新）：ALLOWED_CYCLES 豁免理由"机制保证不死锁"依赖 SPSC 无锁（§17.2 决策9/§九），**该机制未被 #5 编码/校验**；若实现层在环两侧加互斥锁，该 2-环将真死锁而 #5 不报警 | C | 部分：合法环已登记（循环第1批）；"豁免依据未被#5验证 + 加锁即失效"为**新缺口** |
| 18 | 资源释放责任链 | 外部资源(qt/vtk/E4/E5/E6/E7/E1/E2)释放责任可由 A/D 边推出"使用者持有释放"；但 vtk 多实例子资源(LOD per-device actor) 是 scene3d 内部 #5 不可见；data/log fan-in 释放竞争同 CT-7（CT-8） | C | 部分：F36-2 / C层不在#5 投影；"vtk 子资源释放 #5 不可见"为新 |
| 19 | 背压/限流 | CT-4（新）：§九 已裁 SLICE-00 即走完整"每设备线程+SPSC" ⇒ A9 背压在**切片0即生效**，不可递延到多设备；但 A9 仍标"待设计"，#5 无背压边属性；4 条投递边(A33/A34/A35/A36)需背压标注 | C | 部分：A9 并发待设计已知（F32/F35 投影）；"切片0即生效/不可递延"的**新张力** + 投递边清单为新 |
| 20 | 超时与重试边界 | CT-5（新）：#5 无超时/重试/幂等边属性；重试(A10)作用于非幂等写边（execution→store / device→store / error→store / stats→store）破坏 B8；跨进程 D 边(comm/interface↔virtual-device)需超时(§14.5 约束1)无标注 | C | 部分：A10/B8 并发待设计已知（F32 投影）；"具体非幂等写边清单 + 跨进程超时无属性"为新 |

> 本批**无 D 级**：被检边表 `check-deps.py` 退出码 0（无环/无禁边/端点合法/出处无行号），结构正确；唯一的 D（F34-2 事件订阅者缺边）已由**轮31至40** 记录，本批不重复报。本批新增均为 C/I 级（表达力/验证信息缺口）。

---

## 表二 · 新增 D / C / I 清单（本批，逐条给证据原文）

| 编号 | 轮 | 归类 | 发现 | 比对对象 / 原文引用 | 是否已知重复 |
|---|---|---|---|---|---|
| **CT-1** | 13 | C | A 层边类型固定 `usage`（方法 §三「类型：A 层固定 usage」），不区分"同步调用"与"异步投递(enqueue)"；两条合法环边在边表中均为 `\| A \| usage \|`，"投递"语义只写在出处注释，**导致本图无法表达 B-017b 的"只投递不持有"机制本身，也无法表达 A9 背压、A11 插队**。建议：边类型增加 `sync\|enqueue` 或单独"投递"边类，或在边表顶部为 A33/A34 加 `投递` 标签 | 方法 §三 `类型：A 层固定 usage`；边表 `core/concurrency -> core/device \| A \| usage \| §五:设备状态(只投递不持有)` 与 `core/device -> core/concurrency \| A \| usage \| §17.5:下发投递(第一跳)`；§17.2 决策9 `每设备一线程 + SPSC 无锁汇聚` | 部分：A9/A11 并发待设计已知（F32/F35 投影）；"边类型词汇不区分 sync/async 投递"为**新表达力角度** |
| **CT-2** | 17 | C | ALLOWED_CYCLES 对 `core/concurrency ↔ core/device` 的豁免理由写"机制保证不死锁"，但该机制 = SPSC 无锁汇聚（§17.2 决策9、§九），**未被 #5 自身编码/校验**；若实现层在环两侧加互斥锁做"持有-调用"，该 2-环将真正死锁，而 #5 豁免不会报警 → 豁免的安全性是"靠约定"，不是"靠图可验" | `00-流程/check-deps.py` `ALLOWED_CYCLES` 条目理由 `机制保证不死锁`；边表两条豁免环边；方法 §六「合法环 … 有明确机制保证不会死锁/循环调用（如"只投递不持有"）」 | 部分：合法环已登记为已知（循环第1批）；"豁免依据未被#5验证 + 加锁即失效"为**新缺口** |
| **CT-3** | 11 | I | 24 模块中仅 10 个线程归属可由现有文本确定（app/*4 + infra/*5 + core/concurrency）；其余 **14 个线程归属未定**，其中 `core/device` 作为 8 台设备状态"唯一持有者"（§五 硬约束①）其线程身份尤其关键却未定，使 B-017b"每设备一线程 + 唯一事实源"无法被本图验证。全量枚举见 表三 | 方法 §一「C · 时序 \| 启动顺序 · 线程归属 \| ⛔（不画成图）」；§16.1 仅分层未给线程；边表无任何线程标注（实测）；§五 硬约束①「core/device 是设备状态的唯一持有者」 | 部分：一般"线程归属不在 #5（C层）"已知（F38-3 投影）；"14/24 未定 + core/device 关键未定"的**全量枚举为新** |
| **CT-4** | 19 | C | §九 已裁 `SLICE-00 即使只有 1 台设备，也走完整的每设备一线程 + SPSC 汇聚 结构 ⇒ 并发架构在第一片就被验证` ⇒ 背压(A9)在**切片0即生效**、不可递延到多设备；但 §12.2 仍标 A9「🔴 待设计」，且 #5 无队列/背压边属性。"等加多设备再设计背压"的直觉与本裁冲突 | §九 `SLICE-00 即使只有 1 台设备，也走完整的每设备一线程 + SPSC 汇聚 结构 ⇒ 并发架构在第一片就被验证`；§12.2 A9 `SPSC 队列满了怎么办？丢帧？阻塞？🔴 待设计`；边表 59 条 usage 无背压语义（实测） | 部分：A9 并发待设计已知（F32/F35 投影）；"切片0即生效/不可递延"的**新张力**为新 |
| **CT-5** | 20 | C | #5 无超时/重试/幂等边属性；重试(A10)若作用于非幂等写边（`core/execution -> data/store` REQ-DB-001、`core/device -> data/store` REQ-DEV-007、`core/error -> data/store` §五、`core/stats -> data/store` REQ-MON-002）将重复写库破坏 B8 幂等；跨进程 D 边 `comm/interface -> apps/virtual-device` / `apps/virtual-device -> comm/interface`（§14.2 E2）需超时（§14.5 约束1：独立进程只能靠超时/心跳判定存活）却无标注 | 边表上述 4 条写边 + D1/D2；§12.2 B8 `幂等性 … 重复下发同一程序会怎样？重复点击下发？🔴 待设计`；§14.5 约束1 `E2 是独立进程 ⇒ 上位机与它之间只能靠超时/心跳判定存活` | 部分：A10/B8 并发待设计已知（F32 投影）；"具体非幂等写边清单 + 跨进程超时无属性"为**新** |
| **CT-6** | 15 | I | A 层唯一环即合法 `core/concurrency ↔ core/device` 豁免环（check-deps.py `ALLOWED_CYCLES` 仅 1 条）；去除豁免后 A 层无真环 ⇒ 不存在"依赖成环但启动不能成环"的真启动环；但该 2-环使 A 层**无法单独推出启动顺序**（环两侧谁是"先起"未标注），须 #6 运行时视图补线程/启动序 | `00-流程/check-deps.py`「A 层无环（仅豁免环）」；`ALLOWED_CYCLES` 仅 1 条；方法 §一「C · 时序 不画成图」 | 部分：合法环已知、C层不在#5 投影；"启动序无法由#5推出"为**新** |
| **CT-7** | 16 | C | #5 完全不含关闭/启动序（C 层不画成图）；`data/log` 为单写者、被 9 条 A 边作为汇点 fan-in，其释放时序依赖"所有写者先停"，而 #5 无关闭序信息 ⇒ "优雅关闭"（§八 约束1）与"逆序关闭"在 #5 中零表达、不可由本图验证；data/log fan-in 在关闭期存在释放竞争 | §八 约束1「启动必须异步（UI 先出，comm/scene3d 后台初始化）」+ 同节标题"优雅关闭"；边表实测 data/log 入边 9 条（execution→log、error→log、alarm→log、concurrency→log、program→log、device→log、statemachine→log、dashboard→log、dialogs→log）；§五 硬约束② | 部分：F36-2（data/log 串行点）投影；"关闭序零表达 + fan-in 释放竞争"为**新** |
| **CT-8** | 18 | C | 外部资源(qt/vtk/E4/E5/E6/E7/E1/E2)释放责任可由 A/D 边推出"使用者持有并负责释放"（如 `app/scene3d -> vtk` 持有 vtk 对象、`data/store/data/log/infra/config -> E4` 持有文件/SQLite 句柄）；但 (a) vtk 多实例子资源（§17.2 决策16 单场景多实例 LOD 的 per-device actor）是 scene3d 内部子资源，#5 不可见其释放；(b) `data/log` 共享单写者 fan-in 的释放竞争（同 CT-7）不可见 | 边表 `app/scene3d -> vtk \| A \| usage \| P-006(3D 渲染用 VTK)`、D4/D5/D6 `data/store/data/log/infra/config -> E4`；§17.2 决策16 `单场景多实例 + 渲染精度分级` | 部分：F36-2 / C层不在#5 投影；"vtk 子资源释放 #5 不可见"为**新** |
| **CT-9** | 14 | C | B 层事件边 `core/device -> app/scene3d` / `core/device -> app/dashboard` / `core/alarm -> app/dashboard`（B1/B2/B3）从后台(core) 发往 UI(app) 线程，**需经 queued connection / 线程跳才能安全投递**；#5 记录方向但无线程跳标注（与 F34-2"订阅者缺 eventbus 注入边"互为补充：不仅订阅者无句柄，且跨线程投递机制未注明） | 边表 B1 `core/device -> app/scene3d \| B \| pub \| 决策17(状态变更→3D 刷新)`、B2、B3；F34-2（轮31至40）已指出订阅者缺 `X -> infra/eventbus` A 边；方法 §三 B 层「事件流向」 | 部分：F34-2（D）已覆盖订阅者缺边；"core→app 事件需线程跳"为同根因下**新小点** |

> 本批**新增 9 条**（C 级 8 + I 级 1），**无 D**；其中纯新根因见结尾「还剩几个新根因」。

---

## 表三 · 24 模块 × 线程归属表（轮 11 产出，逐条，未抽样）

> 判定口径：线程归属是否可由现有文本（`S1-#5依赖图方法-定稿.md` §一「C层不画成图」、`S1-架构基线草稿.md` §16.1 分层 / §九 / §17.2 决策9）确定；凡方法/基线未指明具体线程者标「⚠️ 未指定」。

| # | 模块 | 层 | 线程归属（依现有文本） | 可确定性 | 依据 / 备注 |
|---|---|---|---|---|---|
| 1 | `app/main_window` | UI | Qt GUI 主线程（UI 线程） | 可定 | §17.5「UI 层 … 只读业务状态」；Qt 单 GUI 线程 |
| 2 | `app/scene3d` | UI | Qt GUI 主线程 | 可定 | 同上；用 vtk 但渲染在 GUI 线程 |
| 3 | `app/dashboard` | UI | Qt GUI 主线程 | 可定 | 同上 |
| 4 | `app/dialogs` | UI | Qt GUI 主线程 | 可定 | 同上 |
| 5 | `main` | 组合根 | Qt GUI 主线程（启动/装配期） | 可定 | 方法 §三「main = 装配点（组合根）」；注入各模块 |
| 6 | `infra/build` | 基础设施 | 非运行时（构建期，不占运行线程） | 可定 | §14.3「构建期活动」；场景清单 §三 明文豁免 |
| 7 | `infra/config` | 基础设施 | 启动期主线程（构造注入，运行期只读） | 可定 | §7.2.1②「构造注入，启动后只读」 |
| 8 | `infra/eventbus` | 基础设施 | 主线程（事件总线实例由构造注入） | 可定 | 方法 §三「X -> infra/eventbus 永远记 A（构造注入）」 |
| 9 | `infra/lifecycle` | 基础设施 | 主线程（启动/关闭编排） | 可定 | §八 约束1「异步启动 + 优雅关闭」编排者 |
| 10 | `infra/telemetry` | 基础设施 | 主线程（观测采集；边表 0 边） | 可定 | 边表实测 0 边；§12.1 A8 可观测性；轮21至30 N6 |
| 11 | `core/concurrency` | 业务 | **线程拥有者**：N 个"每设备一线程"工作线程（N≤8） | 可定 | §17.2 决策9「每设备一线程 + SPSC 无锁汇聚」；§九「SLICE-00 即走完整每设备线程」 |
| 12 | `core/device` | 业务 | ⚠️ **未指定**（8 台设备状态唯一持有者，其线程身份在 #5/#6 文本均未定） | **未定** | §五 硬约束①「设备状态唯一持有者」；§16.1 只分层未给线程 |
| 13 | `core/execution` | 业务 | ⚠️ 未指定 | 未定 | §16.1 只分层未给线程 |
| 14 | `core/program` | 业务 | ⚠️ 未指定 | 未定 | 同上 |
| 15 | `core/statemachine` | 业务 | ⚠️ 未指定 | 未定 | 同上 |
| 16 | `core/alarm` | 业务 | ⚠️ 未指定 | 未定 | 同上 |
| 17 | `core/stats` | 业务 | ⚠️ 未指定 | 未定 | 同上（且轮21至30 N3：零入边） |
| 18 | `core/error` | 业务 | ⚠️ 未指定 | 未定 | §16.1 只分层未给线程 |
| 19 | `core/vision` | 业务 | ⚠️ 未指定 | 未定 | 同上（场景清单：单覆盖） |
| 20 | `core/mapping` | 业务 | ⚠️ 未指定 | 未定 | 同上 |
| 21 | `comm/interface` | 通信 | ⚠️ 未指定（通信层线程/线程池未定） | 未定 | §16.1 只分层 |
| 22 | `comm/modbus` | 通信 | ⚠️ 未指定 | 未定 | 同上 |
| 23 | `data/store` | 存储 | ⚠️ 未指定（SQLite 连接线程亲和未定） | 未定 | §16.1 只分层 |
| 24 | `data/log` | 存储 | ⚠️ 未指定（单写者但写线程未定） | 未定 | §五 硬约束②「日志唯一写者」 |

> **轮 11 结论**：24 模块中 **10 个可定**（UI 4 + infra 5 + core/concurrency + main），**14 个线程归属未定**（core 9 除 concurrency + comm 2 + data 2；另 `apps/virtual-device` 为**独立进程**而非上位机线程，见下）。其中 **`core/device` 的线程归属最关键却未定**：它是 8 台设备状态的唯一持有者，若它在主/UI 线程而 8 个 `core/concurrency` 工作线程向它"投递"，则汇聚点是主线程的 fan-in 争用；若它自有线程，则又是一套跨线程。两种实现都对，但 #5 与 #6 引用文本都没定，B-017b"每设备一线程 + 唯一事实源"因此**无法被本图验证**（CT-3）。方法 §一已声明"线程归属属 C 层、不画成图"，故这是**范围边界**问题，需在 #6 运行时视图补齐，而非 #5 的缺陷。

> `apps/virtual-device`：独立进程（`§14.2 E2` 独立外部程序 + 经通信；`B5`），**非上位机线程**，跨进程边界由 D 层 `comm/interface ↔ apps/virtual-device` 承载。

---

## 表四 · 跨线程边清单（轮 12 产出）

> 判定口径：以 表三 的"可定"线程为基准；**确定跨线程**=两端线程不同且均可定；**跨进程**=D 层 `comm/interface ↔ apps/virtual-device`；**可能跨线程**=至少一端线程未定（含 core↔concurrency 工作线程 vs device 持有者线程）；**同线程(框架)**=app→qt/vtk（同 UI 线程，非跨线程）。
> 同步/投递标注列：#5 实测**全部边均无**同步或投递语义标注（A 层统一 `usage`，B 层统一 `pub`）。

| 边（原文，不引行号） | 层 | 两端线程 | 跨线程判定 | #5 是否有同步/投递标注 |
|---|---|---|---|---|
| `app/main_window -> core/device` | A | UI ↔ 未定(后台) | 可能跨线程 | 无 |
| `app/main_window -> core/program` | A | UI ↔ 未定 | 可能跨线程 | 无 |
| `app/scene3d -> core/device` | A | UI ↔ 未定 | 可能跨线程 | 无 |
| `app/scene3d -> core/mapping` | A | UI ↔ 未定 | 可能跨线程 | 无 |
| `app/scene3d -> core/program` | A | UI ↔ 未定 | 可能跨线程 | 无 |
| `app/dashboard -> core/device` | A | UI ↔ 未定 | 可能跨线程 | 无 |
| `app/dashboard -> core/alarm` | A | UI ↔ 未定 | 可能跨线程 | 无 |
| `app/dashboard -> core/stats` | A | UI ↔ 未定 | 可能跨线程 | 无 |
| `app/dashboard -> data/store` | A | UI ↔ 未定 | 可能跨线程 | 无 |
| `app/dashboard -> comm/interface` | A | UI ↔ 未定 | 可能跨线程 | 无 |
| `app/dialogs -> core/execution` | A | UI ↔ 未定 | 可能跨线程 | 无 |
| `app/dialogs -> core/device` | A | UI ↔ 未定 | 可能跨线程 | 无 |
| `app/dialogs -> data/log` | A | UI ↔ 未定 | 可能跨线程 | 无 |
| `app/main_window -> qt` / `app/scene3d -> qt` / `app/dashboard -> qt` / `app/dialogs -> qt` | A | UI ↔ UI（框架） | **同线程**（非跨线程） | 无（框架边） |
| `app/scene3d -> vtk` | A | UI ↔ UI（渲染后端在 GUI 线程） | **同线程**（非跨线程） | 无（框架边） |
| `core/device -> app/scene3d` | B | 后台 ↔ UI | **确定跨线程（事件投递需线程跳）** | 无（仅记 pub） |
| `core/device -> app/dashboard` | B | 后台 ↔ UI | **确定跨线程（事件投递需线程跳）** | 无 |
| `core/alarm -> app/dashboard` | B | 后台 ↔ UI | **确定跨线程（事件投递需线程跳）** | 无 |
| `core/error -> core/alarm` | B | 后台 ↔ 后台 | 可能跨线程（两端线程均未定） | 无 |
| `core/device -> core/alarm` | B | 后台 ↔ 后台 | 可能跨线程 | 无 |
| `core/device -> core/error` | B | 后台 ↔ 后台 | 可能跨线程 | 无 |
| `core/concurrency -> core/device` | A | 工作线程 ↔ 未定(持有者) | **可能跨线程（投递边）** | 无（仅记 usage） |
| `core/device -> core/concurrency` | A | 未定(持有者) ↔ 工作线程 | **可能跨线程（投递边）** | 无 |
| `core/device -> infra/eventbus` | A | 后台 ↔ 主线程 | 可能跨线程（发布事件） | 无 |
| `core/concurrency -> comm/interface` | A | 工作线程 ↔ 未定(通信) | 可能跨线程（投递边） | 无 |
| `core/concurrency -> data/log` | A | 工作线程 ↔ 未定(日志) | 可能跨线程（投递边） | 无 |
| `comm/interface -> apps/virtual-device` | D | 上位机 ↔ **独立进程** | **跨进程**（最强隔离） | 无（仅记 protocol） |
| `apps/virtual-device -> comm/interface` | D | **独立进程** ↔ 上位机 | **跨进程** | 无 |

> **轮 12 结论**：① 确定的跨线程边 = 3 条 B 事件（core→app，需 queued connection 线程跳，CT-9）；确定的跨进程边 = 2 条 D（comm↔virtual-device，§14.5 约束1）。② 其余大量 A 边（app→core/data/comm 约 14 条）与投递边（concurrency↔device 等 5 条）**跨线程与否取决于表三中"未定"模块的最终线程归属**——即这些边的线程安全性**当前在 #5 中不可判定**（CT-3 根因）。③ **#5 对所有跨线程/跨进程边零同步或投递标注**：A 层统一 `usage`、B 层统一 `pub`、D 层统一 `protocol`，既不能区分"同步调用"与"异步投递"（CT-1），也不能表达跨线程事件如何跳线程（CT-9）。这是 #5 表达力的结构性缺口，根因投影到"线程归属不在 #5（C层）"与"usage 不区分 sync/async（CT-1）"，本表的价值在于**首次把跨线程/跨进程边逐条枚举出来**，供 #6 运行时视图逐一落地同步机制。

---

## 结尾 · 收敛小结

### ① 本批（轮 11~20）新增几条
- **C 级 8 条**：CT-1（usage 不区分 sync/async 投递）· CT-2（豁免环依据=SPSC无锁未被#5验证、加锁即失效）· CT-4（SLICE-00 迫使 A9 切片0生效不可递延）· CT-5（非幂等写边清单 + 跨进程超时无属性）· CT-7（关闭序零表达 + data/log fan-in 释放竞争）· CT-8（vtk 子资源释放 #5 不可见）· CT-9（B 事件 core→app 需线程跳）。
- **I 级 1 条**：CT-3（14/24 模块线程未定 + core/device 关键未定，全量枚举）· CT-6（启动序无法由#5推出）。
- 合计 **新增 9 条**（8 C + 1 I），**无 D**（唯一 D 即 F34-2 已由轮31至40 记录，本批不重复报）。

### ② 重复投影几条
- 本批 10 轮中，轮 12/14/19/20 的主体结论均为已知待设计项的同根因投影，本批只补充了"逐边枚举 / 具体边清单 / 切片0张力"等新残点；明确投影到已知项的约 **4~5 条**：
  - A9/A10/A11/B8 并发待设计（轮31至40 F32/F35 投影）→ 轮13/19/20 的主体；
  - "线程归属/隔离需 #6"（轮31至40 F38-3）→ 轮11/12/15/16 的主体；
  - "concurrency 单中继/单点"（轮31至40 F38-2/F35-2）→ 轮12/17 的主体；
  - "data/log 单写者串行点"（F36-2）→ 轮16/18 的主体；
  - B 层计数差 / 订阅者缺边（轮21至30 N1 / 轮31至40 F34-2）→ 轮14 的主体。
- 即：本批 9 条新增中约 **4~5 条是已知项（A9/A10/A11/B8、C层不在#5、concurrency 单点、data/log 单写者、F34-2）的同根因投影**，其余 **4~5 条为新角度/新残点**（见③）。

### ③ 还剩几个新根因
本批暴露、且**不在已知 40+ 禁报清单内**的新根因约 **4 个**：
1. **#5 边类型词汇不区分"同步调用"与"异步投递(enqueue)"（CT-1）** —— A 层统一 `usage`，使 B-017b 的"只投递不持有"机制本身、A9 背压、A11 插队在依赖图层面无法表达；这是 #5 表达力的结构性缺口，需补边类型或标签。
2. **线程归属信息缺口（CT-3 + CT-6）** —— 14/24 模块（含 `core/device` 关键持有者）线程归属在 #5 与 #6 引用文本均未定，导致 B-017b、启动序、单台隔离均无法被本图验证；须在 #6 运行时视图补齐。
3. **豁免环的安全性靠约定不靠图（CT-2）** —— ALLOWED_CYCLES 的"机制保证不死锁"依赖 SPSC 无锁，该机制未被 #5 编码；实现层一旦加锁，2-环真死锁而 #5 不报警。
4. **切片0 即须设计的并发机制（CT-4）** —— §九 已裁 SLICE-00 走完整并发，故 A9 背压在切片0即生效、不可递延；与"待设计"状态冲突；另 CT-5（非幂等写边 + 跨进程超时无属性）、CT-7（关闭序零表达）、CT-8（vtk 子资源释放不可见）、CT-9（B 事件需线程跳）为上述根因的派生小点。

> **#5 是否可收敛**：结构正确性层面 `check-deps.py` 退出码 0（无环/无禁边/端点合法/出处无行号），双向核对已做，**无新结构环/禁边错误**；本批暴露的 9 条均为"#5 表达力 / #6 运行时视图缺口"的投影或新角度，**不构成 #5 冻结阻断**（唯一阻断 D=F34-2 已由轮31至40 记录并给出补边方案）。建议：① 在边表/方法为 A33/A34 等投递边加 `投递`/`enqueue` 标签（CT-1）；② 把"线程归属/启动序/关闭序/锁机制"明确挂接到 #6 运行时视图（CT-3/CT-6/CT-2/CT-7）；③ 在 #8 横切机制里把 CT-4(A9切片0)、CT-5(A10×B8)、CT-9(事件线程跳) 落到设计而非留"待设计"。完成后 #5 结构可冻结。

---

## 收尾 · 脚本退出码（仅核验，未改动任何文件）

```
python 00-流程/check-deps.py       ⇒ 退出码 0  （共 74 条边 A59/B6/D9 · 31 节点 · ①无环②无禁边③端点合法④出处无行号）
python 00-流程/check-consistency.py ⇒ 退出码 0  （核对 10-基线/*.md，23 项检查失败 0 项）
```

> 两脚本均通过；本文件所有结论基于上述实测 + 5 个被检对象 + 既存同批文件（轮21至30/轮31至40）的人工对照，**未修改任何既有文件，未做 git 提交**。被检边表当前 `check-deps.py` 实报 74 条边（与任务书"75"差 1，已由轮21至30 N1 记录为计数未同步，非结构缺陷）。
