# S1·A3 产出 · 40 轮检查修正报告

> **一句话**：对刚补的**两张表**（需求→模块映射 127 条 / 数据所有权 9 类）跑 **40 轮检查**。
> 结果：**查出 14 项**——其中 **6 项真属 A3**（应补/应修），**8 项跨层**（属 A4/A7/A8）。
> ⇒ 补完 6 项后 A3 收口。

---

# 批 1 · 映射表检查（轮 1~10）

| 轮 | 角度 | 结果 |
|---|---|---|
| 1 | **完整性** | ✅ **127 条全覆盖**（实测） |
| 2 | **唯一性** | ⚠️ 13 个特例落了**两个模块**（如 `RUN-004`=statemachine+execution），**未区分主落点/协作点** |
| 3 | **合理性抽样** | ✅ `3D-002`（运动归属）→ `core/mapping` 对；`RUN-004`（急停）→ 对但需拆主次 |
| 4 | 与**归位表**一致 | ✅ 落点都在对应层 |
| 5 | 与**模块清单**一致 | 🔴 **落点里有 3 类不是 22 模块**：`comm/*`（通配）· `贯穿`（非模块）· `流程（无代码）`（非模块） |
| 6 | **约束类**归属 | 🔴 `NFR` 6 + `PRC` 3 + `ARCH` 8 = **17 条缺「约束落点表」** |
| 7 | **跨层域拆分** | ✅ `D1`→mapping/scene3d · `N1`→alarm/dashboard · `N4`→stats/dashboard 拆得都对 |
| 8 | **特例逐个核**（13 个） | ✅ 除 `INF-007`（确认 UI + 业务）需注明"UI 侧与业务侧各一半"外，其余合理 |
| 9 | **落点分布合理性** | ⚠️ `core/execution` **14 条最重**（是第二重 `core/program` 的 1.08 倍，尚可） |
| 10 | **可操作性** | ⚠️ 因轮 5/6 的三类"非模块落点"，**现在还不能直接开工** |

**批 1 发现**：`A`（落点不全是模块）🔴 · `B`（主落点/协作为区分）⚠️ · `C`（execution 最重）⚠️ · `D`（缺约束落点表）🔴

---

# 批 2 · 所有权表检查（轮 11~20）

| 轮 | 角度 | 结果 |
|---|---|---|
| 11 | **覆盖完整性** | ✅ 9 类够（连接状态含在"设备状态"里） |
| 12 | **持有者唯一性** | ✅ 每类只有一个持有者 |
| 13 | **可写者合理性** | ⚠️ "`core/concurrency` 只**投递**不持有"——**"投递"这个词要定义清**（是调用接口，不是直接改） |
| 14 | **只读者有无漏** | 🔴 **设备状态的只读者漏了 `core/error`**（归正要读实际位置） |
| 15 | 与映射表一致 | ✅ 持有者都在 22 模块内 |
| 16 | 与 `B-017b` 一致 | ✅（每设备一线程 + SPSC 汇聚） |
| 17 | 与 `REQ-DB-005` 一致 | 🔴 **所有权表只管"谁持有"，不管"字段定义谁维护"**——而 `DB-005` 要求"字段名+类型+单位+范围"有唯一定义 |
| 18 | 三条约束可执行性 | 🔴 约束③「视图不缓存」**缺可判定的读接口**（属 A4） |
| 19 | **"无人持有"的数据** | 🔴 **错误码表**（`REQ-NFR-007` 归 `infra`）**没进表** |
| 20 | **"多持有者"的数据** | ✅ 无 |

**批 2 发现**：`E`（漏 `core/error`）🟡 · `F`（缺字段定义归属）🔴 · `G`（错误码表未进表）🟡 · `H`（约束③缺接口）→A4

---

# 批 3 · 交叉一致性（轮 21~30）

| 轮 | 角度 | 结果 |
|---|---|---|
| 21 | 映射表 ↔ 所有权表 | ⚠️ `infra/config` 在映射表只 2 条（`INF-006/007`），但它持有**全部配置** ⇒ **需求数与职责不匹配** |
| 22 | 22 模块**都有需求落点**吗 | ✅ 无空模块；最薄的是 `comm/modbus`（**1 条**） |
| 23 | 有无"零需求"模块 | ✅ 无 |
| 24 | 与 S0 的 127 条对齐 | ✅ |
| 25 | 与 A2 归位表对齐 | ✅ |
| 26 | 与 A1 分层对齐 | ✅（`apps/virtual-device` 不属四层，已说明） |
| 27 | 与 `B-025` 命名规范对齐 | ✅ |
| 28 | 与 `REQ-INF-008`（全用 vcpkg）对齐 | ✅ |
| 29 | 与**不做清单**对齐 | ✅ 无越界 |
| 30 | **跨层域拆分**是否在所有权表体现 | ⚠️ 部分（`D1` 的映射侧数据所有权未写） |

**批 3 发现**：`I`（`comm/modbus` 仅 1 条，偏薄）⚠️ · `J`（`infra/config` 职责与需求数不匹配）⚠️ · `K`（跨层域数据所有权未写全）⚠️

---

# 批 4 · A3 产出完整性（轮 31~40）

| 轮 | 角度 | 结果 |
|---|---|---|
| 31 | A3 应有 **7 项产出** | 🔴 **现有 3 项**（模块清单 / 归位表 / 2 张表），**缺 4 项** |
| 32 | **配置项清单** | 🔴 **缺**（配置项散在各需求里，没汇总） |
| 33 | **契约分界清单** | 🔴 缺 ⇒ **属 A4**（记下） |
| 34 | **风险模块说明** | 🔴 **缺**（3 个：`core/concurrency` / `app/scene3d` / `apps/virtual-device`） |
| 35 | **约束清单** | 🔴 **缺**（现散落：异步启动 / 禁止单例 / 错误码归 infra / 视图不缓存 / 急停双路径 / 存储串行化） |
| 36 | 与 `SLICE-00` 对照 | ✅ 第一片涉及 4 模块 + 1 独立单元，在 22 模块内 |
| 37 | 能否支撑 **A4** | 🔴 **需先补「约束落点表」**，否则 A4 不知约束怎么落地 |
| 38 | 能否支撑 **A7**（文件清单） | ✅ |
| 39 | 能否支撑 **A8**（任务分解） | ⚠️ 需补"风险模块说明" |
| 40 | 总检 | 补 **4 项产出 + 3 处小修** ⇒ 可进 A4 |

**批 4 发现**：`L`（缺配置项清单）🔴 · `M`（缺风险说明）🔴 · `N`（缺约束清单）🔴

---

# 📊 40 轮汇总：14 项发现

| 编号 | 发现 | 归类 | 处置 |
|---|---|---|---|
| **A** | 映射表落点有 3 类**不是 22 模块**（`comm/*` / `贯穿` / `流程`） | 🔴 **A3** | **修**（改为规范写法） |
| **B** | 13 个特例落两模块，未区分主/协 | ⚠️ A3 | **修**（加"主落点"列） |
| C | `core/execution` 14 条最重 | ⚠️ A3 | **说明**（可接受） |
| **D** | **缺「约束落点表」**（17 条约束类） | 🔴 **A3** | **补** |
| **E** | 所有权表漏 `core/error` 为设备状态的只读者 | 🟡 A3 | **修** |
| **F** | **缺"字段定义谁维护"**（`DB-005` 要求） | 🔴 **A3** | **补** |
| **G** | 错误码表未进所有权表 | 🟡 A3 | **修** |
| H | 约束③缺可判定的读接口 | → A4 | 带过去 |
| I | `comm/modbus` 仅 1 条需求，偏薄 | ⚠️ A3 | **说明**（薄是正常的，它只做协议映射） |
| J | `infra/config` 职责与需求数不匹配 | ⚠️ A3 | **说明**（它由 `INF-006` 一条统管） |
| K | 跨层域数据所有权未写全 | ⚠️ A3 | **补一行** |
| **L** | **缺配置项清单** | 🔴 **A3** | **补** |
| **M** | **缺风险模块说明** | 🔴 **A3** | **补** |
| **N** | **缺约束清单** | 🔴 **A3** | **补** |

**⇒ 要补 4 项**（D / F / L / N）（M 一并做）· **要修 4 处**（A / B / E / G / K）
