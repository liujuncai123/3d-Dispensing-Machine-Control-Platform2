# S1 批 1 · 待审明细（`#4` / `#5` / `#12`）

> **一句话**：这三项**产出已经写完，只差你点头**。本文逐项说明**要你审什么 / 详细解释 / 推荐 / 为什么**，供你一次看完再签。
> **建立日期**：2026-09-21 ｜ **依据**：`./S1-架构基线草稿.md` 逐节通读 + 实测命令

---

## 〇、本轮审核中我发现并已修的 3 处缺陷（**先报账**）

| # | 缺陷 | 实测证据 | 处置 |
|---|---|---|---|
| **D1** | 🔴 `§14.6` 的 ISO 23247 四域表**被注记打断** —— 「**④ 用户域**」行排在了两条 `>` 注记**之后** ⇒ 该行**落在表格之外**，渲染时会变成正文文本行 | 表内只剩 ①②③ + "不在四域内" 四行 | ✅ 已把 ④ 行**移回表内**；并补 `app/replay`（④ 域现 **5** 个模块） |
| **D2** | 🔴 `§17.4` 第 1 条注记「⚠️ `AS-002`（台数上限）**仍待定**」**已过期** | 台账 `AS-002` 实测 = 「**✅ 已裁（2026-09-20）：设计容量 8 台，上限可配置**」 | ✅ 已改为已裁结论并注明"原写仍待定已过期" |
| **D3** | 🔴 `§17.5` 铁律表的 **UI 层漏了 `app/replay`**（`CR-010` 改模块数时**漏改这一处**） | 该行原只列 4 个 UI 模块；`app/replay` 在 S1 正文当时仅 3 处出现 | ✅ 已补，UI 层现 **5** 个 |

> 修后：两脚本**全绿**（`check-deps` 退出码 0 · 87 边 A70/B8/D9 · 32 节点；`check-consistency` 23 项失败 0）。

---

## 一、`#4` 上下文视图（`§十四`，14.1 ~ 14.6）

### 要你审什么
**3 件产出的完成判据是否达成**（`§14.1` 定义）。

### 详细解释

| 小节 | 内容 | 要点 / 实测 |
|---|---|---|
| **14.1** | 产出物 **3 件**：① 外部实体清单 ② 系统边界图 ③ 边界内不负责什么 | 每件都给了**完成判据**（如实体须有：名字/交互内容/协议/方向/依据） |
| **14.2** | **外部实体 7 个** | `E1` 操作员（人）· `E2` **虚拟下位机**（独立进程·同机回环·×N，N≤8）· `E3` 真实点胶机/PLC（**不对接**，但走同一接口 ⇒ 换实现不改上层）· `E4` 本机文件系统 · `E5` 系统时钟 · `E6` 构建/开发环境（**不参与运行时**）· `E7` 报警声光输出<br>🔑 **自查命令实测 = 7** ✅（与自述一致） |
| **14.3** | **预留外部实体 4 个**（留口不实现） | MES/ERP · OPC UA/SECS-GEM · 真实 CCD 视觉 · DXF/G 代码导入；其中 **前 2 项明标「⚠️ 待 S2 验证」**（诚实：现有依据只是"能导出数据""业务不依赖协议"，**不足以证明那就是接口位置**） |
| **14.4** | **边界内不负责 6 条** | 🔑 **只引用 `S-1` 已冻条目、一条都不新写**（依 `S-1 §七 R2/R3` 防 AI 自拟范围）—— 这一节本身是**制度护栏** |
| **14.5** | **外部实体带来的 3 条硬约束**（对 `#5`/`#6` 的输入） | ① `E2` 独立进程 ⇒ 只能靠**超时/心跳**判存活，不能靠函数返回 ② `E4` 共享资源 ⇒ 读写**必须串行化** ③ `E5` 时间戳来源单一 |
| **14.6** | **ISO 23247 四域对照**（落实已裁 `§十一 A1` = C） | 明确写「C4 L1 答**系统内外**、四域答**系统内部分类**，**两套口径不要求 1:1 映射**」；并给出推论：**① 可观测制造域在本项目落在"系统外"**（被观测设备不存在，由虚拟下位机顶替）⇒ 解释了 `B5` 为何定「独立外部程序 + 经通信」 |

### 推荐
**批。**（附 1 个要你留意的点，见"为什么"③）

### 为什么
1. **它是 C4 Level 1 + arc42 ch.3 的标准产物**，缺它 S1 的"上下文"就是空的 —— 而"系统边界"是后面 `#6 运行时视图`/`#7 部署视图` 的输入。
2. **`14.4` 有独立制度价值**：它用"只引用、不新增"的写法，从结构上堵住 AI 自拟范围（这正是 `CR-005` 删掉 AI 自拟不做清单后立的规矩）。
3. ⚠️ **要你留意的是 `14.3`**：其中 **MES/ERP 与 OPC UA/SECS-GEM 两项标了「待 S2 验证」** —— 这不是缺陷，是**诚实的弱依据标注**。**若你认为这两个口子现在就不该留，现在说不比 S2 说便宜**（那时改要动 S2）。

---

## 二、`#5` 构建块视图（判据 = **依赖图 + 每模块接口清单**）

### 要你审什么
**这条判据的"两半"是否都达成。**

### 详细解释

| 半 | 产出 | 实测 |
|---|---|---|
| **依赖图** | `20-设计/S1-依赖边表.txt` —— **唯一权威源** | **87 条边（A 70 · B 8 · D 9）· 32 节点**；`check-deps.py` **退出码 0**（①A层无环 ②无禁边 ③端点合法 ④出处无行号） |
| **接口清单** | `20-设计/S1-接口清单.md` | **25 个模块 · 60 个 `I*` 抽象接口**；每模块四件事：① 一句话职责 ② 对外接口（接口名 + **C++ 方法签名** + 用途 + **依据**）③ 出边（逐条引用边表原文）④ 入边 |

**与边表一致性（交叉验证过两道）**：
- 模块 A-出边 **57** + `main` 装配边 **13** = **70**；模块 A-入边 **65** + 非模块被使用者（`qt`×4 / `vtk`×1）**5** = **70**；B **8** + D **9** ⇒ **A70+B8+D9 = 87** ✅
- **零边 / 单边模块全部显式声明**：`core/vision`/`core/mapping` 无出边 · `comm/modbus`/`infra/build` 无入边 · **`infra/telemetry` 零边**（有专节说明理由：方向说不清 ⇒ 依方法"不连边、记待裁"）

**已固化的两条硬口径**：
1. **`core`/`comm`/`data`/`infra` 的接口不暴露 Qt / VTK 头**（依禁边，模块划分轮 32）；**只有 `app/*` 可用 Qt 类型**
2. `app/replay` 与 `S1-回放-独立模块接口预留.md` **同一批接口名**（样板复用，不另起一套）

### 推荐
**批，并同步把 `§1.0.1` 里 `#5` 的状态改为 ✅。**

### 为什么
1. **判据两半齐备，且机器可验**（`check-deps.py` 退出码 0 是硬证据，不是自述）。
2. **一致性被交叉验证过两道**：架构师逐条核对 + 主理人复核脚本输出，两边都得出 87。
3. ⚠️ **请留意清单里的 4 条「无 S0 支撑（假设，待裁）」接口** —— `IBackpressurePolicy` · `IEventBus` · `ITelemetry` · `IVersionInfo`。**它们不影响本次判据达成**（判据要的是"清单存在且与边表一致"），但**它们是真缺口**，建议**另立一项处置**（补依据 / 降为不预留），**不要混进本次批准**。

---

## 三、`#12` 解决方案策略（`§十七`，17.1 ~ 17.6）

### 要你审什么
**整章是否可以定为 `#12` 的定稿**（含并入的 `#19` / `#20`）。

### 详细解释

| 小节 | 内容 | 要点 |
|---|---|---|
| **17.1** | 一句话架构策略 + **五根主干** | 每根都标了**已裁依据**（`B-017b`/`REQ-DEV-003`/`REQ-COM-001`/决策 17 等） |
| **17.2** | **根本性决策清单 17 条** | 🔑 **`§17.2` 自己写着：「第 1~13、15~17 条确认保留；第 14 条保留框架 + 补 5 条口径」⇒ 17 条里 16 条你 2026-09-20 已逐条看过**。⇒ **本次要裁的是"整章作为 `#12` 定稿"，不是重新逐条看** |
| **17.3** | 技术选型理由（**`#19` 并入**） | 8 行，每行 = 选了 / 备选 / **为什么不用备选** / 来源 ⇒ **面试必问的那张表** |
| **17.4** | 权衡点清单（**`#20` 并入**） | 8 条取舍（容量 vs 复杂度 / 保真 vs 性能 / 真机 vs 虚拟 …） ⇒ **被问"为什么不做 X"时的答案** |
| **17.5** | 架构总览（一页看懂） | 各层模块 + **铁律**（如 `core/device` ★状态唯一持有者 · `data/log` ★唯一写者）+ **一次"下发-执行"的完整数据流** |
| **17.6** | 质量目标 → 达成路线 | ✅ 只搭**骨架 + 落指针**：完整手段表留 `#15`、额度分配留 `#14`（**边界划清，不越界**） |

### 推荐
**批。**

### 为什么
1. **arc42 §4 要求的三要素齐全**：技术决策（`17.3`）· 顶层结构（`17.5`）· **质量目标达成路线**（`17.6`）—— 缺任何一个，`#12` 都不算完成。
2. **成本最低**：`§17.2` 的 17 条**已被你逐条确认过**，本次只是把整章"盖章定稿"。
3. **`17.3` 早定早能用**：它是简历/面试讲技术选型的直接素材，越早定稿越早能背。
4. **`17.6` 的边界处理得对**：它**只写"靠哪些决策达成"**，把"分多少额度"（`#14`）和"逐条手段"（`#15`）明确推给下游 ⇒ **不越界，也不留空**。

---

## 四、签完之后会发生什么（**便于你判断该不该批**）

| 批完 | 效果 |
|---|---|
| `#4` ✅ | S1 有了**系统边界** ⇒ `#6 运行时视图` / `#7 部署视图` 有了输入 |
| `#5` ✅ | **S1 的"构建块"立起来** ⇒ `#8 横切机制` / `#9 状态机` 有了接口约束 |
| `#12` ✅ | **S1 的"策略"立起来** ⇒ 后面所有设计都有"为什么这么切"的依据 |
| 三项齐 | **S1「主架构三项」全部立起来**（这是 S1 冻结的第一道门槛） |

> ⚠️ **`#4` + `#12` + `#5` 齐了 ≠ S1 可冻结** —— 还差 **`P-6` 硬闸门**（`#6` 线程架构 + `#9` 状态机，见 `./S1-收口评估.md` 结-3）与 `§十二` 的 12 项待设计（结-1）。

---

## 五、诚实声明

- 本文**逐项附了落点与实测证据**（节号、命令输出），未凭印象。
- 三处缺陷（D1/D2/D3）是**我在本轮审核中实测发现**的，**已修**；修的是**待裁章节**（未冻结，可改）。
- 本文**不含替用户做的决定** —— "推荐批"是**建议**，拍板权在用户。
