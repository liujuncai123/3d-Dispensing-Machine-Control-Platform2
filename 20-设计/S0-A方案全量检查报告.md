# S0 · A 方案全量检查报告（一次性跑完）

> ⚠️ **本文件是过程产物（A 方案检查过程记录），不是权威源。** S0 的权威源 = `./S0-需求条目表-初稿.md`


> **一句话**：把 A 方案剩下的**全部维度一次跑完**（不再一轮一轮挤），产出这份完整报告。
> **执行日期**：2026-09-19 ｜ **执行者**：AI ｜ **性质**：检查报告，**不含任何已生效的裁决**
> **对照物**：`../10-基线/S-1-立项声明.md`（v1.9）+ `./S0-需求基线草稿.md`

---

## 一、执行了什么（批 1~3）

| 批次 | 维度 | 查什么 | 结果 |
|---|---|---|---|
| **批1** | **A1+A2 · 165 条资料全量反向对照** | 此前只对照过 03 的 3 个方向 + 01/02 的 2 个方向，**剩下约 90% 从未对照** | 108 条候选（含噪音）→ **筛出 5 条真项** |
| **批2** | **A3 · 风险 → 需求** | S-1 §六 的 3 条风险，是否有需求承载 | 缺 **1** |
| **批2** | **A4 · 预留 → 需求** | "留接口"本身是否成为需求 | 缺 **1** |
| **批2** | **A5 · R3 可追溯性预判** | 每个域能否给出"来源" | 缺 **7** |
| **批3** | **A6 · 横切杂项** | 时间戳/编码/导出/保留/自检等最易漏的一类 | 缺 **5** |

> **批1 的方法与噪音说明**：用"最长公共中文字串 < 4 字"作为启发式筛查，**必然含大量噪音**——
> 例如 ISO 23247 框架、五层成熟度口径、SCADA 能力六要素**本身是背景知识，不是本项目功能**，
> 被扫出来属于正常。**下列 5 条是人工筛过的**（真属"行业有、我们没提"且与本项目相关）。

---

## 二、批1 筛出：5 条行业有、我们没提（→ 第 4 组 `U9`~`U13`）

| 编号 | 项 | 行业依据 | 疑似归属 | 备注 |
|---|---|---|---|---|
| **U9** | **历史趋势曲线**（参数随时间的变化曲线） | `01-…md` 方向 3：SCADA 六要素含「**历史数据记录与分析**」；HMI 通用能力含「**历史趋势**」 | `N4 监控统计` | 与 `N3 存储` 分工需定 |
| **U10** | **故障注入**（传感器偏置/丢失、执行器饱和、断电） | `01-…md` 方向 4：「**故障注入（fault injection）是虚拟仿真的突出价值**」 | `D8` + `D9` | ⚠️ **与 S-1 §7 的"误差"不同**：§7 是"固定幅度+随机扰动"的**误差**，故障注入是**另一类** |
| **U11** | **碰撞 / 干涉检查** | `01-…md` 方向 4：「VERICUT 跑一遍**提前抓碰撞/干涉**」 | `D3 程序校验` | S-1 只提了"程序检查"，未提碰撞 |
| **U12** | **理论加工 / 点胶时间预估** | `01-…md` 方向 4：「几何/运动学仿真…可输出**理论加工时间**」 | `D2` / `D8` | — |
| **U13** | **报警洪泛抑制** | `01-…md` 方向 3：「ISA 18.2：报警优先级、**报警洪泛抑制**、确认与升级流程」 | `N1 报警` | S-1 未提 |

---

## 三、批2 结果

### A3 · 风险 → 需求（缺 1）

| 风险（S-1 §六） | 是否有需求承载 |
|---|---|
| 1 VTK/Qt 集成 | ✅ 由「一键构建」承接（`D11`） |
| 2 多设备并发 | ✅ 由「多设备并发管理」（`D4`）+ 状态机/线程（`D13`/S1）承接 |
| 3 **单人无法完成全部开发** | ❌ **没有"砍范围/降级"的触发条件** —— 即 `../70-Backlog/` 的 **BL-006**，至今未进 S0 |

### A4 · 预留 → 需求（缺 1）

| 项 | 是否有需求承载 |
|---|---|
| 多平台编码纪律（不硬编码 Windows 路径等） | ✅ `F3` |
| **"接口预留、实现可替换"本身** | ❌ **缺** —— S-1 §四 写了"预留 9 项（有接口、无实现）"，但**"必须留接口"这件事没有成为一条需求**，展开时极易被忽略 |

### A5 · R3 可追溯性预判（缺 7 / 17 个域）

草稿中**未见明确"来源"栏**的域：`D8` · `D10` · `D13` · `N1` · `N2` · `N4` · `N5`（7 个）。

> 注：这不是错，是**域列表行本来就没写来源栏**。但它提示：**展开成条目时必须逐条补来源**，否则违反 S-1 §七 `R3`。

---

## 四、批3 结果：横切杂项（缺 5）

| # | 项 | 为什么容易漏 |
|---|---|---|
| **W1** | **时间戳 / 时区** | 报警、日志、记录**都要带时间戳**，但没有任何一个域"负责定义它" |
| **W2** | **数据导出格式**（CSV / Excel） | 导出是"跨域能力"（记录、报表、日志都要），不属任何单一域 |
| **W3** | **开机自检 / 初始化流程** | 属"启动时序"，域列表里没有"启动"这一项 |
| **W4** | **中断恢复**（上位机崩溃/重启后现场） | 属可靠性，但 `F2` 只写了"掉线/超时/故障恢复"，未含"上位机自身崩溃" |
| **W5** | **错误码 / 故障代码对照表** | 行业惯例（`03` 方向 4 提到"故障代码对照表现场定位"），我们零提及 |

---

## 五、汇总：本轮新增 13 项待裁

| 组 | 新增 | 项 |
|---|---|---|
| 第 4 组（未认领） | **U9~U13** | 历史趋势曲线 · 故障注入 · 碰撞干涉检查 · 时间预估 · 报警洪泛抑制 |
| **第 7 组（横切）** 🆕 | **W1~W5** | 时间戳时区 · 导出格式 · 开机自检 · 中断恢复 · 错误码对照 |
| **第 8 组（结构缺口）** 🆕 | **X1~X3** | 砍范围触发条件 · "接口预留"需求 · 域级补来源 |

---

## 六、⚠️ 三个判断（不是结论，供你参考）

| # | 判断 | 依据 |
|---|---|---|
| 1 | **U10 故障注入值得优先考虑** | 它是虚拟下位机的**突出价值**（行业原话），且**不需要真实硬件就能演示** —— 与本项目定位高度契合；且与 `D9 误差`分工需明确 |
| 2 | **W1 / W2 / W5 属"低成本高收益"** | 都是横切的小能力（时间戳、导出、错误码表），实现量小但缺了会显得不专业 |
| 3 | **X2 是"制度性"缺口** | "预留要留接口"没成为需求 ⇒ 与之前"AI 自造范围"是同一类问题的另一面：**该管的没管** |

---

## 七、A 方案到此的完整清单（**共 8 组**）

| 组 | 内容 | 项数 |
|---|---|---|
| 第 1 组 | 功能域 | 12 |
| 第 2 组 | 待拍候选 | 5 |
| 第 3 组 | 非功能类 | 6 |
| 第 4 组 | 未认领项 | **13**（U1~U13） |
| 第 5 组 | 域间边界 | 4（+4 留 S1） |
| 第 6 组 | 数量引用 | 2 |
| **第 7 组** | **横切项** 🆕 | **5** |
| **第 8 组** | **结构缺口** 🆕 | **3** |
| 另 | 展开必含子项 S1~S5 | 5 |
| 另 | 前置 B018 / B030 + 编号规则 | 3 |
| — | **合计待裁** | **≈ 53 项** |

> ⚠️ **53 项已远超"队列 >15 需清理"的阈值。** 建议你**只挑有异议的**，其余批量确认。
