# S0 需求条目表（初稿 · A+B 级展开）

> **一句话**：把 **A 级 + B 级**（≈40 项）展开成**可勾选验收判据**的需求条目；**C 级 8 项**只产出**架构冗余要求**（`REQ-ARCH-*`），不产出功能实现条目。
> **状态**：⏳ **草稿，未裁决** —— 按 S-1 §七 `R2`，**未经你逐条裁决前不构成约束**。
> **来源**：`../10-基线/S-1-立项声明.md`（v1.9）· `./S0-需求基线草稿.md`（〇 裁决记录）· `../80-资料/`（已采用条目）
> **编号**：暂用**方案 A**（`REQ-<域>-<序号>`）—— ⚠️ **编号规则你尚未裁**，若改选方案 B，本表需重编
> **规模**：**约 110 条**（推算，非实测）

---

## 〇、本文件是 S0 的**唯一权威源**

> **一句话**：S0"做什么"的全部内容**以本文件为准**；其他 S0 文件均为**过程记录**（非权威）。

| 文件 | 性质 |
|---|---|
| **`S0-需求条目表-初稿.md`（本文件）** | 🔴 **唯一权威** —— 裁决记录 / 术语定义 / 域清单 / **全部条目** / 待裁项 |
| `S0-需求基线草稿.md` | 已降级为**指针** |
| `S0-A方案全量检查报告.md` | **过程产物**（A 检查记录，非权威） |
| `S0-B检查报告与采用建议.md` | **过程产物**（B 检查记录，非权威） |
| `S0-待裁项-必要性分级.md` | **过程产物**（分级依据，非权威） |
| **`S0-检查指引.md`** | 🔴 **给用户看的检查入口**（看什么/什么顺序/重点问什么） |
| `S0-条目展开方案.md` | **已选方案乙**（分级展开：25 深度 + 55 标准 + 30 轻量）· 分批交付 |
| **`S0-展开稿/`（子目录）** | **派生的批次阅读稿**（便于你分批逐条看；**非权威**）—— 批1 已完成 |

### 〇.1 裁决记录（用户已裁）

| 来源 | 裁决 | 用户原话 |
|---|---|---|
| 必要性分级 | **A + B 级 ⇒ 做** | 「A+B 就行」 |
| C 级（8 项） | **预留**，**且必须在架构上留冗余** | 「c 预留（给一下架构冗余就行）」 |
| D 级（U4/U5） | ⚠️ 用户未明说 ⇒ 暂按 AI 建议记「不做」，**可随时推翻** | — |

### 〇.2 术语定义（2026-09-19 补 —— 此前**从未定义过**）

| 术语 | 定义 | 依据 |
|---|---|---|
| **程序**（Program） | **一条运行路径** = **轨迹基元序列**（点 / 直线 / 圆弧）+ **程序级工艺参数**；**点可覆盖程序级** | `B-016`（点级覆盖程序级）+ 附录 A.1 #5 |
| **配方**（Recipe） | **可复用的参数集**（即"配方库"）—— 本项目**已进预留，不做** | `B-011`「不做配方库 / 版本 / 审计」+ S-1 §四 预留清单 |

> ⚠️ **修正记录**：本表原 `REQ-PRG-008` 写作「**程序/配方**的调用与切换」，**把两者当成了同义词** —— 这是错的。
> 二者关系：**程序 = 路径 + 参数**；**配方 = 可复用参数集（预留，不做）**。已修正为「**程序**可被调用切换」。

### 〇.3 覆盖检查的方法修正（2026-09-19）

> **一句话**：判"某条有没有被展开"，**不能查"ID 有没有出现"** —— 那样注释里提一句就算覆盖了。

| 项 | 说明 |
|---|---|
| **错的查法** | 查 `REQ-xxx` 是否出现在文件中 ⇒ **假阳性**：`REQ-VMC-005` 当时只在注释里被提到，就被判成"已覆盖" |
| **对的查法** | 查该条**是否以"展开形式"存在**（有描述字段 / 有卡片结构） |
| **实际漏掉的** | `REQ-VMC-005` —— **已补展开**（见 `# 条目展开` 的批2 段） |

### 〇.4 待裁项（不裁则无法冻结）

| # | 事项 | 影响 |
|---|---|---|
| 1 | **编号规则**（A 按域前缀 / B 全局流水） | 本表暂用 A；改 B 需**重编全部 ID** |
| 2 | `B-018` 演示脚本 | 影响 `REQ-NFR-*` 与整机验收判据 |
| 3 | `B-030` 简化版视觉形式 | 影响 `REQ-VIS-001~003` |
| 4 | `B-020` 状态集合 | 影响 `REQ-STA-001` |
| 5 | `B-021` 持久化选型 | 影响 `REQ-DB-004` |
| 7 | `B1` 设备参数集归属 | 影响 `REQ-DEV-007` |
| 8 | ~~`AS-001`/`AS-002`~~ **已裁** · **`AS-005` 精度分级仍待裁** | `AS-001`=50ms/100ms，`AS-002`=8 台，均可配置；剩 `AS-005` |
| 9 | `U10` 故障注入：做 or 预留 | 建议考虑提到"做" |

---

---

---

# 条目展开（全部 111 条）

> 以下为**展开后的正式内容**；原"一行一条"表已由本节取代。按 6 批组织，便于对照 `S0-展开稿/` 阅读。

## 批1 · D5运行控制+D13状态迁移

### 一、D5 · 运行控制（9 条）

#### `REQ-RUN-001` · 启动 / 运行

| 字段 | 内容 |
|---|---|
| **描述** | 系统应能对指定设备下达"启动"指令，令其开始执行当前程序。 |
| **验收判据** | ① 就绪态下下达启动 ⇒ 设备进入**运行**态<br>② 运行中界面显示**当前执行到的基元序号**<br>③ 启动后 `D1` 的 3D 机构**开始随坐标移动** |
| **前置条件** | 设备状态 = **就绪**；且**已回零**；且当前程序**已通过校验** |
| **异常 / 边界** | 处于暂停态时下达启动 ⇒ 语义为**继续**（非重头开始）；程序为空或校验未过 ⇒ 拒绝并给出原因 |
| **依赖** | `REQ-RUN-006`（回零）· `REQ-CHK-*`（校验）· `REQ-STA-003`（非法迁移拒绝） |
| **来源** | `C1`（8 项之 1）· `03-资料` 方向 1「启动是必备项」 |
| **优先级** | **P0** |
| **可测性** | **单测**（状态迁移）+ **演示** |
| **状态** | ⏳ 待裁 |

#### `REQ-RUN-002` · 暂停

| 字段 | 内容 |
|---|---|
| **描述** | 系统应能在运行中暂停执行，并支持随后继续。 |
| **验收判据** | ① 运行中下达暂停 ⇒ 进入**暂停**态，机构**停止移动**<br>② 暂停态保留**当前执行位置**（不回到起点）<br>③ 再次启动 ⇒ 从**暂停处继续** |
| **前置条件** | 设备状态 = 运行 |
| **异常 / 边界** | 暂停期间收到新程序 ⇒ 按 `REQ-DEV-*` 的互斥规则处理（不静默替换） |
| **依赖** | `REQ-RUN-001` · `REQ-STA-*` |
| **来源** | `C1`（2/8）· `03-资料` 方向 1「暂停是必备项，部分设备单键复用」 |
| **优先级** | **P0** |
| **可测性** | **单测** + 演示 |
| **状态** | ⏳ 待裁 |

#### `REQ-RUN-003` · 停止

| 字段 | 内容 |
|---|---|
| **描述** | 系统应能停止执行，并使设备退出运行流程（**区别于暂停**）。 |
| **验收判据** | ① 运行 / 暂停中下达停止 ⇒ 退出运行，转入**就绪或待机**<br>② 停止后**不保留**执行进度（再次启动为**从头开始**）<br>③ 界面上"停止"与"暂停"**语义可区分**（不同按钮/提示） |
| **前置条件** | 运行 / 暂停 / 回零中 |
| **异常 / 边界** | 停止过程中的中间态须可见（不得界面卡住无反馈） |
| **依赖** | `REQ-RUN-002`（两者语义必须区分）· `REQ-STA-001` |
| **来源** | `C1`（3/8）· `03-资料` 方向 1 |
| **优先级** | **P0** |
| **可测性** | **单测** + 演示 |
| **状态** | ⏳ 待裁 |

#### `REQ-RUN-004` · **急停（本地优先）**

| 字段 | 内容 |
|---|---|
| **描述** | 系统应提供急停能力，且急停**不依赖与设备的通信往返**：触发后立即进入急停态并切断危险动作。 |
| **验收判据** | ① 触发急停后**不等待通信确认**即转入急停态（界面 ≤ 1 个刷新周期内可见）<br>② 急停态下所有运动指令**被拒绝**并给出提示<br>③ 急停后必须**显式复位 + 使能**才可再次启动<br>④ **通信断开时急停仍然有效**（本地路径独立） |
| **前置条件** | 设备处于 就绪 / 运行 / 暂停 / 回零中 / 排胶中 任一状态 |
| **异常 / 边界** | 急停期间收到下发的程序 ⇒ **拒绝且不排队**；急停期间上位机崩溃 ⇒ 重启后**仍为急停态**（不得自行恢复为就绪） |
| **依赖** | `REQ-STA-002`（急停可从任意状态进入）· **`B5`**（✅ 已裁 = 独立外部程序 + 经通信） |
| **来源** | `C1`（4/8）· S-1 附录 B 安全策略 · `P-5` |
| **优先级** | **P0**（安全） |
| **可测性** | **单测**（状态迁移）+ **演示**（断连下触发） |
| **状态** | ⏳ 待裁 |

#### `REQ-RUN-005` · 复位 / 使能

| 字段 | 内容 |
|---|---|
| **描述** | 系统应提供复位操作，用于解除急停并恢复可运行状态；且复位后须"使能"才可启动。 |
| **验收判据** | ① 急停态下执行复位 ⇒ 急停标志**清除**<br>② 复位后**未使能**即下达启动 ⇒ **被拒绝**<br>③ 使能成功后状态转为**就绪** |
| **前置条件** | 设备状态 = 急停 |
| **异常 / 边界** | 复位**不等于回零**（两者语义不同，见 `REQ-RUN-006`）—— 复位只清状态，不动坐标 |
| **依赖** | `REQ-RUN-004` · `REQ-STA-003` |
| **来源** | `C1`（5/8）· `03-资料` 方向 1「急停恢复流程：复位→确认安全→使能」 |
| **优先级** | **P0**（安全） |
| **可测性** | **单测** + 演示 |
| **状态** | ⏳ 待裁 |

#### `REQ-RUN-006` · 回零

| 字段 | 内容 |
|---|---|
| **描述** | 系统应能下达回零指令，使各轴回到机械基准位；未完成回零不得自动运行。 |
| **验收判据** | ① 下达回零 ⇒ 状态进入**回零中**，三轴依次归位<br>② 回零完成 ⇒ 坐标显示为基准值（如 `X=0, Y=0, Z=0`）<br>③ **未回零**即下达自动运行 ⇒ **被拒绝**并提示 |
| **前置条件** | 就绪 / 急停已复位 |
| **异常 / 边界** | 回零过程中下达急停 ⇒ 立即转入急停，**回零中断**且不得标记为"已回零" |
| **依赖** | `REQ-RUN-004` · `REQ-STA-001`（含"回零中"态）· **`B5`**（✅ 已裁 = 独立外部程序 + 经通信） |
| **来源** | `C1`（6/8）· `03-资料` 方向 1「开机必须回原点以保证坐标准确」 |
| **优先级** | **P0** |
| **可测性** | **单测** + 演示 |
| **状态** | ⏳ 待裁 |

#### `REQ-RUN-007` · 排胶 / 清洗

| 字段 | 内容 |
|---|---|
| **描述** | 系统应提供手动出胶（排胶）能力，用于换胶、清洗管路、排空气。 |
| **验收判据** | ① 下达排胶 ⇒ 进入**排胶中**态，点胶头按设定出胶<br>② 排胶可**手动停止**<br>③ 排胶期间**不影响**其他设备的运行 |
| **前置条件** | 就绪（✅ **已裁：不要求回零** —— 排胶只用于换胶/清管/排空气，不需精确定位） |
| **异常 / 边界** | 排胶与自动运行**互斥**（同台设备不得同时进行） |
| **依赖** | `REQ-PRM-001`（出胶时间）· `REQ-PRM-002`（气压） |
| **来源** | `C1`（7/8）· `03-资料` 方向 1「排胶是常见必备项」 |
| **优先级** | **P1** |
| **可测性** | 演示 + 人工 |
| **状态** | ⏳ 待裁 |

#### `REQ-RUN-008` · 点动 / 手动移位

| 字段 | 内容 |
|---|---|
| **描述** | 系统应支持按轴低速点动，用于调试、对针与位置微调。 |
| **验收判据** | ① 可选择轴（X/Y/Z）并点动<br>② 有**低速 / 高速**两档切换<br>③ 点动时 3D 中对应部件**同步移动** |
| **前置条件** | 就绪（点动属手动模式） |
| **异常 / 边界** | 点动至**限位** ⇒ 停止并提示（依赖 `REQ-VMC-004` 的限位仿真） |
| **依赖** | `REQ-VMC-004`（限位）· `REQ-3D-003`（位置映射） |
| **来源** | `C1`（8/8）· `03-资料` 方向 1「点动是常见必备项」 |
| **优先级** | **P1** |
| **可测性** | 演示 |
| **状态** | ⏳ 待裁 |

#### `REQ-RUN-009` · **掉线 / 超时即停**

| 字段 | 内容 |
|---|---|
| **描述** | 上位机与设备的通信中断超过阈值时，系统**必须停机**（不得静默继续）。 |
| **验收判据** | ① 断连时间 > 阈值 ⇒ 设备转入**停机/报警**态<br>② 界面**明确提示**哪台设备掉线<br>③ 掉线设备**不影响**其他在线设备的运行 |
| **前置条件** | 设备处于在线且非离线态 |
| **异常 / 边界** | ✅ **阈值已裁（2026-09-19）：3 秒**（1 秒心跳 × 连续 3 次未收到）；恢复连接后**须人工复位**，不得自动恢复运行 |
| **依赖** | `REQ-COM-003`（连接状态可见）· `REQ-COM-004`（掉线检测）· **`B5`**（✅ 已裁 = 独立外部程序 + 经通信） |
| **来源** | S-1 附录 B（`B-015`「掉线 / 超时即停」） |
| **优先级** | **P0**（安全） |
| **可测性** | **单测**（超时逻辑）+ 演示（断连） |
| **状态** | ⏳ 待裁 |

---

### 二、D13 · 设备状态与迁移（4 条）

#### `REQ-STA-001` · 状态集合

| 字段 | 内容 |
|---|---|
| **描述** | 设备状态集合应被明确枚举，且每个状态在界面上有**唯一可辨的表现**。 |
| **验收判据** | ① 状态集合至少含：**离线 / 就绪 / 运行 / 暂停 / 急停 / 报警 / 回零中 / 排胶中**（8 态）<br>② 每个状态在列表与详情中**表现一致**<br>③ `D1` 的 3D 场景能区分**运行 / 暂停 / 急停 / 报警** |
| **前置条件** | — |
| **异常 / 边界** | 若后续需加状态（如"空跑"），**不得改动已有状态语义**（只增不改） |
| **依赖** | `B-020`（**待裁** —— 本条的 8 态取值等它）· `REQ-3D-004` |
| **来源** | `B-017a`（层次 + 表驱动）· `B-020`（待裁）· S-1 附录 A.1 #3 |
| **优先级** | **P0** |
| **可测性** | **单测**（状态集合存在性）；§`REQ-STA-004` 覆盖迁移 |
| **状态** | ⏳ 待裁 |

#### `REQ-STA-002` · **急停可从任意状态进入**

| 字段 | 内容 |
|---|---|
| **描述** | 急停必须能从**任意其他状态**进入 —— 这是"急停覆盖一切"的形式化表达。 |
| **验收判据** | ① 对**每一个**其他状态（离线 / 就绪 / 运行 / 暂停 / 报警 / 回零中 / 排胶中）触发急停 ⇒ **均进入急停态**<br>② 上行路径的**每一条**都能被单测覆盖（≥ 7 条用例） |
| **前置条件** | — |
| **异常 / 边界** | ✅ **已裁（2026-09-19）**：离线态触发急停 ⇒ **仍进入急停态；连接恢复后保持急停，须人工复位** |
| **依赖** | `REQ-RUN-004` · `REQ-STA-001` |
| **来源** | `P-5`（安全优先）· `B-015` · S-1 附录 B |
| **优先级** | **P0**（安全） |
| **可测性** | **单测**（迁移表全覆盖） |
| **状态** | ⏳ 待裁 |

#### `REQ-STA-003` · 非法迁移被拒绝

| 字段 | 内容 |
|---|---|
| **描述** | 系统应拒绝不满足前置条件的操作请求，并给出可读原因。 |
| **验收判据** | ① 未回零时启动 ⇒ **拒绝** + 提示"未回零"<br>② 未复位时启动 ⇒ **拒绝** + 提示"急停未解除"<br>③ 拒绝时设备状态**不发生变化**（不得进入中间错态） |
| **前置条件** | — |
| **异常 / 边界** | 拒绝必须**有提示**，不得静默忽略（静默忽略会让操作者以为指令已下达） |
| **依赖** | `REQ-RUN-005` · `REQ-RUN-006` |
| **来源** | `B-017a` · 可用性要求 |
| **优先级** | **P0** |
| **可测性** | **单测**（逐条非法迁移各一例） |
| **状态** | ⏳ 待裁 |

#### `REQ-STA-004` · **状态迁移表驱动、可单测**

| 字段 | 内容 |
|---|---|
| **描述** | 状态迁移应以**数据驱动**方式实现（迁移表），使迁移规则可被单测逐条覆盖。 |
| **验收判据** | ① 迁移规则以**表 / 数据结构**形式存在，而非散落在分支代码里<br>② 迁移表可被**逐条单测**（合法迁移、非法迁移各覆盖）<br>③ 新增一个状态时**只需改表**，不改既有分支逻辑 |
| **前置条件** | — |
| **异常 / 边界** | 与 `REQ-ARCH-*` 的"可扩展类型"呼应 —— **第 ③ 条本身就是一条架构成分** |
| **依赖** | `B-017a`（**层次 + 表驱动**）· `REQ-ARCH-000` |
| **来源** | `B-017a` · S-1 §6（单测要求） |
| **优先级** | **P0** |
| **可测性** | **单测**（本条的判据 ② 就是"可单测"本身） |
| **状态** | ⏳ 待裁 |

---

#### 三、本批自检（AI 自查，交你复核）

| # | 观察 | 需你确认 |
|---|---|---|
| 1 | ~~**`B5` 出现在 5 条条目的"依赖"里**~~ → **✅ 已裁**：`B5` = **独立外部程序 + 经通信**（你原话「用外部模拟软件进行模拟通信」）；5 条依赖已同步标注 | **无需再裁**（本节保留作留档） |
| 2 | **`REQ-STA-002` 的"离线态急停"我给了建议但没定** | 属语义问题，**需你拍** |
| 3 | **`REQ-RUN-009` 的阈值未定** | 建议 S1 定；本批先标"待定" |
| 4 | **`REQ-RUN-007` 排胶要求"不必回零"** —— 这是我按行业惯例推断的 | **需你确认**是否同意 |

#### 四、统计

| 项 | 值 |
|---|---|
| 本批条数 | **13**（D5 9 条 + D13 4 条） |
| 深度展开（10 字段） | 13 / 13 |
| P0 条数 | **10** |
| 含"待定值"的条目 | 2（`RUN-009` 阈值 · `STA-001` 的 `B-020`） |

---

## 批2 · D8虚拟下位机+D8bIO+D9误差归正

### 一、深度展开（11 条 · 10 字段）

#### `REQ-VMC-002` · 逐点耗时（时序可单测）

| 字段 | 内容 |
|---|---|
| **描述** | 虚拟下位机执行程序时，应按设定的单点耗时推进，使该耗时可被单元测试断言。 |
| **验收判据** | ① 给定单点耗时 `t`，执行 `n` 点 ⇒ 总耗时 ≈ `n×t`（容差内）<br>② **该断言可作为一条单测**（不依赖界面）<br>③ 耗时参数可被外部设置（供 `REQ-VMC-005` 倍速使用） |
| **前置条件** | 已收到并加载程序 |
| **异常 / 边界** | 倍速模式下 `t` 按倍率缩放，**单测须覆盖 1× 与倍速两种情形**；执行被急停中断时**不补算**剩余耗时 |
| **依赖** | `REQ-VMC-001`（接收程序）· `REQ-VMC-005`（倍速）· `REQ-RUN-004`（急停中断） |
| **来源** | `B-012`（虚拟下位机必备）· S-1 §6（点名"虚拟下位机时序"） |
| **优先级** | **P0** |
| **可测性** | **单测** |
| **状态** | ⏳ 待裁 |

#### `REQ-VMC-004` · 限位仿真

| 字段 | 内容 |
|---|---|
| **描述** | 虚拟下位机应模拟各轴限位，坐标到达限位时触发限位信号并使运动停止。 |
| **验收判据** | ① 坐标到达行程边界 ⇒ 产生**限位信号**<br>② 限位触发后继续下达该方向运动 ⇒ **被拒绝**<br>③ 限位信号可通过 `REQ-IO-001` 在界面上看到 |
| **前置条件** | 已加载程序或处于点动模式 |
| **异常 / 边界** | **行程范围取值待定**（建议由 S1 定，或直接取 `02-资料` 的典型值如 600×500mm）；反向运动应能脱离限位（不得锁死） |
| **依赖** | `REQ-IO-001`（I/O 显示）· `REQ-RUN-008`（点动）· `REQ-CHK-001`（越界校验） |
| **来源** | `U8`（01-资料 方向 4「传感器仿真：限位」） |
| **优先级** | **P0**（安全相关） |
| **可测性** | **单测** + 演示 |
| **状态** | ⏳ 待裁 |

#### `REQ-VMC-006` · 可脱离上位机独立运行

| 字段 | 内容 |
|---|---|
| **描述** | 虚拟下位机应作为**独立进程**存在，通过通信与上位机交互（而非上位机内的一个函数）。 |
| **验收判据** | ① 虚拟下位机**可单独启动**（不依赖上位机 UI）<br>② 上位机**关闭后**虚拟下位机仍在运行（或按既定规则退出）<br>③ 二者之间**只通过通信接口**交换数据（不共享内存/全局变量） |
| **前置条件** | — |
| **异常 / 边界** | ✅ **已裁（2026-09-19）**：`B5` = **独立外部程序 + 经通信**（用户确认「用外部模拟软件进行模拟通信」）⇒ 本条成立，无需重写。好处：**将来换真设备时上位机代码不用改** |
| **依赖** | `B5`（✅ 已裁：独立进程经通信）· `REQ-COM-001`（抽象层） |
| **来源** | `B-012` + 用户裁 `B5=独立外部程序 + 经通信`（2026-09-19 ✅） |
| **优先级** | **P0**（决定架构与"本地优先"边界） |
| **可测性** | **单测**（接口隔离）+ 演示（独立启动） |
| **状态** | ⏳ 待裁 |

#### `REQ-IO-001` · I/O 信号可仿真与显示

| 字段 | 内容 |
|---|---|
| **描述** | 虚拟下位机应提供数字量输入 / 输出信号，且上位机可看到每个 I/O 点的当前状态。 |
| **验收判据** | ① 至少含：**限位 ×3 轴**（输入）、**针头开胶**（输出）<br>② 界面能显示每个 I/O 点的**当前状态（0/1）**<br>③ 状态变化时界面**随之更新** |
| **前置条件** | 虚拟下位机已在线 |
| **异常 / 边界** | I/O 点集合**可扩展**（见 `REQ-ARCH-*`）；掉线时 I/O 显示应转为"未知"而非保留旧值 |
| **依赖** | `REQ-VMC-004`（限位）· `REQ-COM-003`（连接状态） |
| **来源** | `U7`（01-资料 方向 4「信号映射与 I/O 仿真是关键环节」） |
| **优先级** | **P1** |
| **可测性** | **单测**（I/O 状态机）+ 演示 |
| **状态** | ⏳ 待裁 |

#### `REQ-IO-002` · I/O 点与 3D / 状态一致

| 字段 | 内容 |
|---|---|
| **描述** | I/O 信号、3D 场景表现与设备状态三者应保持同步，不得出现"信号置位但 3D 无变化"。 |
| **验收判据** | ① 限位信号置位 ⇒ 3D 中对应轴**停在边界**且状态标记变化<br>② 开胶输出置位 ⇒ 3D 中**开始生成胶点/胶线**<br>③ 三者不一致的时长 **≤ 1 个刷新周期** |
| **前置条件** | 虚拟下位机在线且 3D 场景已加载 |
| **异常 / 边界** | 刷新周期按 `AS-001`（**50ms 默认，可配置**）；若 3D 渲染滞后，应以**状态为准**并记录告警 |
| **依赖** | `REQ-IO-001` · `REQ-3D-005`（出胶反馈）· `REQ-3D-003`（位置映射）· `AS-001` |
| **来源** | `U7` + S-1 附录 A.1 #2/#4 |
| **优先级** | **P0**（一致性） |
| **可测性** | 演示 + 人工（时间同步） |
| **状态** | ⏳ 待裁 |

#### `REQ-ERR-001` · 固定幅度误差

| 字段 | 内容 |
|---|---|
| **描述** | 虚拟下位机在定位时叠加一个**固定幅度**的偏差，模拟系统性误差。 |
| **验收判据** | ① 每次定位的实际位置 = 指令位置 + **固定偏移**<br>② 偏移量可**被设置**（数值 = `AS-003`）<br>③ 偏移对三个轴**可分别设置** |
| **前置条件** | 虚拟下位机正在执行程序 |
| **异常 / 边界** | 偏移量若设为 0 ⇒ 等价于无固定误差（不得报错）；偏移应作用于**执行位置**而非**显示值**（否则误差就不真实） |
| **依赖** | `AS-003`（幅度取值，**待定**）· `REQ-VMC-002`（时序） |
| **来源** | S-1 §7（"固定幅度…的误差产生与上报"） |
| **优先级** | **P0**（闭环核心） |
| **可测性** | **单测**（给定偏移断言实际位置） |
| **状态** | ⏳ 待裁 |

#### `REQ-ERR-002` · 随机扰动误差

| 字段 | 内容 |
|---|---|
| **描述** | 在固定误差之上叠加**随机扰动**，模拟随机性误差。 |
| **验收判据** | ① 扰动在设定**分布与幅度**内随机波动<br>② 多次执行**结果不完全相同**（证明随机性生效）<br>③ 分布参数可被设置（数值 = `AS-003`） |
| **前置条件** | 同 `REQ-ERR-001` |
| **异常 / 边界** | **随机种子应可控** —— 否则**单测无法断言**（这是"可测性"的关键设计点） |
| **依赖** | `REQ-ERR-001` · `AS-003` |
| **来源** | S-1 §7（"随机扰动的误差产生与上报"） |
| **优先级** | **P0** |
| **可测性** | **单测**（固定种子 ⇒ 断言可复现） |
| **状态** | ⏳ 待裁 |

#### `REQ-ERR-003` · 误差上报到上位机

| 字段 | 内容 |
|---|---|
| **描述** | 虚拟下位机应把产生的误差上报给上位机，使其可见。 |
| **验收判据** | ① 上位机可收到误差值（含**指令位置 / 实际位置 / 偏差量**）<br>② 界面上**能看到**当前误差<br>③ 误差数据可被记录（供 `N2 日志` / `N3 存储`） |
| **前置条件** | 通信正常 |
| **异常 / 边界** | 通信中断期间产生的误差 ⇒ 恢复后**补报或标记缺失**（不得静默丢弃） |
| **依赖** | `REQ-COM-*` · `REQ-ERR-001/002` · `REQ-LOG-*` |
| **来源** | S-1 §7（"…误差产生与**上报**"） |
| **优先级** | **P0** |
| **可测性** | **单测**（上报数据内容）+ 演示 |
| **状态** | ⏳ 待裁 |

#### `REQ-ERR-004` · 归正（**一维卡尔曼**，可单测）

| 字段 | 内容 |
|---|---|
| **描述** | 系统应采用**一维卡尔曼滤波**对位置偏差做状态估计：把"**系统性偏差**"作为状态量、"**随机扰动**"作为观测噪声，从而在混合观测中**分离出真实偏差**，据此计算修正量下发补偿。 |
| **验收判据** | ① 输入**纯固定偏差**（如恒定 +0.05mm）⇒ 估计值在若干点内**收敛到该偏差**（可单测断言）<br>② 输入**固定 + 随机** ⇒ 估计值**不随单次噪声大幅跳动**（可单测：相邻两次估计值之差 < 噪声幅值）<br>③ 每点更新一次，**复杂度 O(1)**（无迭代、无矩阵运算）——满足指令间隔短的实时性要求<br>④ `Q`/`R` 可配置，默认 **`Q=1e-5`、`R=1e-2`**；改参后行为随之改变（可单测） |
| **前置条件** | 已收到误差上报（`REQ-ERR-003`） |
| **异常 / 边界** | `Q/R` 调参不当导致发散 ⇒ **可降级为 α 指数平滑**（α 默认 0.3）；误差超阈值 ⇒ **拒绝归正并告警**（见 `REQ-ERR-006`），不产生荒谬修正量。**S-1 §7 界定**：不含物理模型、不含预测 ⇒ 本条**只做一维状态估计**，不做多轴耦合 / 动力学建模 |
| **依赖** | `REQ-ERR-003`（上报）· `REQ-ERR-001/002`（造误差） |
| **来源** | S-1 §2 T3 + §6（点名"归正"）· **用户裁（2026-09-20）：选 D 一维卡尔曼**（非 C 平滑） |
| **优先级** | **P0**（闭环最后一环） |
| **可测性** | **单测**（收敛性 / 抗噪性 / O(1)） |
| **状态** | ⏳ 待裁 |

#### `REQ-ERR-005` · 归正后可复测

| 字段 | 内容 |
|---|---|
| **描述** | 归正之后再次执行，误差应显著减小 —— 这是"闭环有效"的证据。 |
| **验收判据** | ① 归正后再次执行 ⇒ **误差绝对值 < 归正前**<br>② 该对比**可作为演示脚本的一步**（`B-018` 闭环第 ⑦ 步） |
| **前置条件** | 已完成一次归正 |
| **异常 / 边界** | 随机扰动存在时"一定变小"不总成立 ⇒ 判据应表述为**"误差均值变小"**（而非单次比较），**待你确认表述** |
| **依赖** | `REQ-ERR-004` · `B-018`（演示脚本） |
| **来源** | S-1 §2 T3（"…误差模拟与归正"） |
| **优先级** | **P0** |
| **可测性** | **演示**（核心） |
| **状态** | ⏳ 待裁 |

---

#### `REQ-ERR-006` · 误差超阈值 → 告警 + 记入日志

| 字段 | 内容 |
|---|---|
| **描述** | 当单点偏差超过设定阈值时，系统应**逐条记录**并**触发告警** —— 对应误差的**用途 2（记录 + 警示）**。核心原则：**提醒可以合并（防刷屏），日志必须逐条（不能漏）**。 |
| **验收判据** | ① 偏差 > 阈值 ⇒ 触发**告警**；**连续多点超阈值时提醒合并为一条**（含首次 / 末次 / 累计次数），**避免刷屏**<br>② 该合并告警**必须可展开查看每一个超阈值点的明细**（滑动容器 / 可滚动列表 / 展开区，实现形式不限）<br>③ **日志逐条记、不合并**：每个超阈值点都写一条完整记录，含**设备 / 时间点 / 偏差量 / 阈值 / 是否已归正**，同时写入事件日志（`REQ-LOG-002`）与误差日志（`REQ-LOG-007`）<br>④ 阈值**可配置**（默认待定，建议由 S1 给） |
| **前置条件** | 已收到误差上报（`REQ-ERR-003`） |
| **异常 / 边界** | ✅ **已裁（2026-09-20）：提醒合并、日志逐条，两者分离**。⚠️ **不得以"提醒已合并"为由减少日志记录**；提醒的合并只影响显示，不影响存储 |
| **依赖** | `REQ-ERR-003` · `REQ-LOG-002` · `REQ-LOG-007` |
| **来源** | 用户裁定：误差用途 2 =「提供记录信息警示」 |
| **优先级** | **P0** |
| **可测性** | **单测**（触发与字段）+ 演示 |
| **状态** | ⏳ 待裁 |

#### `REQ-VMC-005` · 时间推进可调倍速

| 字段 | 内容 |
|---|---|
| **描述** | 虚拟下位机的时间推进速度应可调（1:1 与倍速），以便演示加速、调试保真。 |
| **验收判据** | ① 可切换 1:1 与至少一档倍速<br>② 倍速下总耗时 ≈ 1:1 耗时 ÷ 倍率（容差内，**可单测**）<br>③ 倍速切换**不改变执行结果**（点位序列一致） |
| **前置条件** | 程序已加载（`REQ-VMC-001`） |
| **异常 / 边界** | ⚠️ **倍速档位取值待裁**（`B-024`：实时 1:1 / 实时+可调倍速 / 步进）—— 未定前判据 ②③ 的具体数值为空 |
| **依赖** | `B-024`（**待裁**）· `REQ-VMC-002`（单点耗时） |
| **来源** | `B-012` + `B-024`（待裁） |
| **优先级** | **P1** |
| **可测性** | **单测**（倍速换算） |
| **状态** | ⏳ 待裁（**依赖 `B-024`**） |

### 二、标准展开（2 条 · 7 字段）

| ID | 条目 | 描述 | 验收判据（≥2 条） | 来源 | 优先级 | 可测性 |
|---|---|---|---|---|---|---|
| `REQ-VMC-001` | 接收程序并可执行 | 虚拟下位机应能接收上位机下发的程序并按顺序执行 | ① 收到程序后可确认接收<br>② 按基元顺序逐点推进 | `B-012` | **P0** | 单测 + 演示 |
| `REQ-VMC-003` | 上报实时坐标 | 执行过程中应持续上报当前坐标 | ① 执行中坐标随时间变化<br>② 上位机可收到并显示 | S-1 §2 T1 | **P0** | 单测 + 演示 |

---

#### 三、本批自检（交你复核）

| # | 观察 | 需你确认 |
|---|---|---|
| 1 | ~~**`B5` 再次成为拦路项**~~ → **✅ 已裁**：`REQ-VMC-006`「独立运行、只经通信交互」**成立** | **无需再裁**（本节保留作留档） |
| 2 | **`REQ-ERR-002` 的"随机种子可控"是我加的设计约束** | 它不是需求本身，但**不加就没法单测** ⇒ **需你确认是否接受** |
| 3 | **`REQ-ERR-005` 的表述**："误差变小"在随机扰动下不总成立，我建议改为"**误差均值变小**" | **需你确认表述** |
| 4 | **`REQ-VMC-004` 的行程范围取值**我建议直接取 `02-资料` 典型值（600×500mm） | **需你确认** |

#### 四、统计

| 项 | 值 |
|---|---|
| 本批条数 | **12**（VMC 5 + IO 2 + ERR 5） |
| 深度 / 标准 | 11 / 2（其中 `VMC-005` 因依赖 `B-024` 暂缓） |
| P0 条数 | **10** |
| 全文含"待定"的条目 | 3（`VMC-004` 行程 · `ERR-001/002` 的 `AS-003`） |

---

## 批3 · D4设备管理+D10通信

### 一、深度展开（8 条 · 10 字段）

#### `REQ-DEV-001` · 设备列表可见

| 字段 | 内容 |
|---|---|
| **描述** | 系统应显示全部已配置设备，及其名称与当前状态。 |
| **验收判据** | ① 列表显示**全部**已配置设备<br>② 每台显示**名称 + 当前状态**<br>③ 选择某台 ⇒ 其余视图（3D / 程序 / 详情）**切换到该台** |
| **前置条件** | 至少已配置 1 台设备 |
| **异常 / 边界** | 设备数为 0 ⇒ 显示**空状态提示**（不得白屏）；设备较多时列表可滚动 |
| **依赖** | `REQ-STA-001`（状态集合）· `REQ-UI-001`（2D 主框架） |
| **来源** | S-1 §3（"需设备列表"） |
| **优先级** | **P0** |
| **可测性** | 演示 + 人工 |
| **状态** | ⏳ 待裁 |

#### `REQ-DEV-002` · 设备「在线 / 离线」状态可见

| 字段 | 内容 |
|---|---|
| **描述** | 每台设备的在线 / 离线状态应可见，且状态变化时实时更新。 |
| **验收判据** | ① 每台显示在线 / 离线<br>② 断连后**在阈值内**变为离线（阈值见 `REQ-RUN-009`）<br>③ 恢复连接后状态**自动回到在线** |
| **前置条件** | — |
| **异常 / 边界** | "离线"与"报警"是**不同维度**（离线≠故障）⇒ 界面上二者不得混用同一标识 |
| **依赖** | `REQ-COM-003`（连接状态）· `REQ-RUN-009`（掉线即停） |
| **来源** | 子项 `S1`（状态可见性检查发现）· S-1 §3 |
| **优先级** | **P0** |
| **可测性** | **单测**（状态映射）+ 演示（拔线） |
| **状态** | ⏳ 待裁 |

#### `REQ-DEV-003` · 状态汇聚（单一事实源）

| 字段 | 内容 |
|---|---|
| **描述** | 各设备状态应汇聚到一处，且各处展示（列表 / 详情 / 3D / 监控）**同一时刻取值一致**。 |
| **验收判据** | ① 同一设备在同一时刻，**列表与详情状态一致**<br>② 状态来源**唯一**（不出现两处各自维护）<br>③ 汇聚路径在并发下**不产生错序**（后发生的状态不得被先发生的覆盖） |
| **前置条件** | 多台设备在线 |
| **异常 / 边界** | **本条是并发正确性的核心** ⇒ 与 `B-017b`（每设备一线程 + SPSC 无锁汇聚）**直接相关**；需明确"汇聚点在哪、谁写谁读" |
| **依赖** | `B-017b`（线程架构）· `REQ-NFR-002`（多设备响应）· `REQ-DEV-002` |
| **来源** | S-1 §3（"需…状态汇聚"）· `B-017b` |
| **优先级** | **P0**（并发正确性） |
| **可测性** | **单测**（汇聚一致性）+ 演示 |
| **状态** | ⏳ 待裁 |

#### `REQ-DEV-005` · 批量下发需二次确认

| 字段 | 内容 |
|---|---|
| **描述** | 对多台设备批量下发指令时，必须经二次确认才真正发送。 |
| **验收判据** | ① 选择 ≥2 台并下发 ⇒ **弹出确认**，列明**将影响的设备清单**<br>② 未确认 ⇒ **不发送任何指令**<br>③ 确认后 ⇒ 逐台下发并显示**逐台结果** |
| **前置条件** | 已选择多台设备 |
| **异常 / 边界** | 部分成功部分失败 ⇒ **必须逐台显示结果**（不得只报"成功"）；取消确认 ⇒ 状态**不变** |
| **依赖** | `REQ-DEV-004`（逐台下发）· `REQ-DEV-006`（故障隔离） |
| **来源** | `B-015`（安全边界：批量下发二次确认） |
| **优先级** | **P0**（安全） |
| **可测性** | 演示 + 人工 |
| **状态** | ⏳ 待裁 |

#### `REQ-DEV-006` · 单台故障不扩散

| 字段 | 内容 |
|---|---|
| **描述** | 一台设备掉线或报错时，其余设备应不受影响，可继续运行。 |
| **验收判据** | ① 使 A 台掉线 ⇒ B 台**运行不受影响**<br>② A 台的故障**不产生对 B 台的指令**<br>③ A 台恢复后**不自动补发**此前未完成的指令 |
| **前置条件** | ≥2 台设备，其中 ≥1 台在运行 |
| **异常 / 边界** | 需澄清：故障台**是否应自动停机**？（建议：按 `REQ-RUN-009` 掉线即停，**但不停其他台**）⇒ **待你确认** |
| **依赖** | `REQ-RUN-009` · `REQ-DEV-003`（状态汇聚） |
| **来源** | `B-015`（安全边界：单台故障隔离） |
| **优先级** | **P0**（安全 + 差异化） |
| **可测性** | **单测** + 演示 |
| **状态** | ⏳ 待裁 |

#### `REQ-COM-001` · 通信经抽象层

| 字段 | 内容 |
|---|---|
| **描述** | 业务代码不应直接依赖具体通信协议的实现，而应通过抽象接口访问。 |
| **验收判据** | ① 业务层代码中**不出现**具体协议 API（如 Modbus 库的类）<br>② 替换协议实现时**只改实现层**，业务层不变<br>③ 抽象接口**可被 mock**（供单测使用） |
| **前置条件** | — |
| **异常 / 边界** | 第 ③ 条是**可测性的前提**：接口不可 mock ⇒ 大部分通信相关单测无法编写 |
| **依赖** | `B-010`（抽象层 + 现成库）· `REQ-ARCH-000`（接口预留） |
| **来源** | `B-010`（用户裁：D 抽象层 + 现成库） |
| **优先级** | **P0**（架构亮点） |
| **可测性** | **单测**（用 mock 验证业务逻辑） |
| **状态** | ⏳ 待裁 |

#### `REQ-COM-003` · 连接状态可见

| 字段 | 内容 |
|---|---|
| **描述** | 界面应显示每台设备的通信连接状态。 |
| **验收判据** | ① 每台设备显示"已连接 / 未连接 / 连接中"<br>② 状态变化**即时反映**<br>③ 与 `REQ-DEV-002` 的在线/离线**语义一致**（不得一处在线一处离线） |
| **前置条件** | — |
| **异常 / 边界** | "连接中"是**中间态**，需有超时上限（否则会永久停在"连接中"） |
| **依赖** | `REQ-COM-004`（掉线检测）· `REQ-DEV-002` |
| **来源** | 子项 `S2`（状态可见性检查发现） |
| **优先级** | **P1** |
| **可测性** | 演示 |
| **状态** | ⏳ 待裁 |

#### `REQ-COM-004` · 掉线可检测

| 字段 | 内容 |
|---|---|
| **描述** | 通信中断应在阈值内被检测到，并触发既定处置（见 `REQ-RUN-009`）。 |
| **验收判据** | ① 拔线 / 停进程后，**在阈值内**判定为断连<br>② 判定后**触发** `REQ-RUN-009` 的停机逻辑<br>③ 检测机制**不依赖业务数据**（仅靠心跳或超时即可判定） |
| **前置条件** | 已建立连接 |
| **异常 / 边界** | ✅ **阈值已裁：3 秒**；网络闪断 ⇒ 允许一次重试（避免误判），**重试次数建议 1 次，待你确认** |
| **依赖** | `REQ-RUN-009` · `REQ-COM-003` · **`B5`**（✅ 已裁 = 独立外部程序 + 经通信） |
| **来源** | `B-015`（"掉线 / 超时 ⇒ 停机"） |
| **优先级** | **P0**（安全） |
| **可测性** | **单测**（超时判定）+ 演示 |
| **状态** | ⏳ 待裁 |

---

### 二、标准展开（3 条 · 7 字段）

| ID | 条目 | 描述 | 验收判据（≥2 条） | 来源 | 优先级 | 可测性 |
|---|---|---|---|---|---|---|
| `REQ-DEV-004` | 逐台下发 | 系统应支持只对选中的单台设备下发程序 | ① 选中 1 台可下发<br>② 其余设备**不收到**该程序 | S-1 §3 | **P0** | 演示 |
| `REQ-DEV-007` | 每台设备独立参数集 | 每台设备的工艺参数应相互独立 | ① 改 A 台参数 ⇒ B 台**不变**<br>② 参数随设备持久化 | S-1 §3 · `B1`（归属**待裁**） | **P1** | **单测** |
| `REQ-COM-002` | 对外语义为 Modbus TCP | 抽象层之下的对外协议语义应与 Modbus TCP 一致 | ① 有明确的寄存器 / 功能码映射<br>② 可对照 Modbus 文档解释 | `B-010` | **P1** | 人工（对照文档） |

---

#### 三、本批自检（交你复核）

| # | 观察 | 需你确认 |
|---|---|---|
| 1 | **`REQ-DEV-003`（状态汇聚）是本批最硬的一条** —— 它直接决定并发正确性 | **需你确认"汇聚点在哪、谁写谁读"**（这会进 S1 架构） |
| 2 | **`REQ-DEV-006` 有个未澄清点**：故障台**是否应自动停机**？ | 我建议"按 `REQ-RUN-009` 掉线即停，但不停其他台" ⇒ **需你确认** |
| 3 | **`REQ-COM-004` 的两个数值未定**：超时阈值、重试次数 | 建议 S1 定 |
| 4 | `REQ-COM-001` 的**判据③（可 mock）** 是我加的设计约束 | 不加则通信相关单测写不了 ⇒ **需你确认** |

#### 四、统计

| 项 | 值 |
|---|---|
| 本批条数 | **11**（DEV 7 + COM 4） |
| 深度 / 标准 | 8 / 3 |
| P0 条数 | **6** |
| 含"待定"的条目 | 3（`COM-004` 阈值 · `DEV-007` 的 `B1` · `DEV-006` 的停机语义） |

---

## 批4 · D1三维场景+D2程序+D3校验

### 一、深度展开（5 条 · 10 字段）

#### `REQ-3D-004` · 运行状态在场景内可区分

| 字段 | 内容 |
|---|---|
| **描述** | 3D 场景中应能区分设备的运行 / 暂停 / 急停 / 报警四种状态。 |
| **验收判据** | ① 四种状态**视觉可辨**（颜色或标识）<br>② 状态切换时场景内**随之改变**<br>③ 与 `REQ-STA-001` 的状态集合**语义一致** |
| **前置条件** | 3D 场景已加载 |
| **异常 / 边界** | 仅用颜色区分对**色觉障碍**不友好 ⇒ 建议**颜色 + 标识**双通道（**需你确认是否加此要求**） |
| **依赖** | `REQ-3D-001`（机构加载）· `REQ-STA-001`（状态集合） |
| **来源** | S-1 附录 A.1 #3 |
| **优先级** | **P1** |
| **可测性** | 演示 |
| **状态** | ⏳ 待裁 |

#### `REQ-3D-005` · 出胶状态有可见反馈

| 字段 | 内容 |
|---|---|
| **描述** | 开胶期间场景内应生成胶点 / 胶线；关胶时停止生成。 |
| **验收判据** | ① 开胶 ⇒ **开始生成**胶点/胶线<br>② 关胶 ⇒ **停止生成**<br>③ 胶路位置与实际执行位置**一致** |
| **前置条件** | 虚拟下位机在执行程序且开胶输出已置位（见 `REQ-IO-002`） |
| **异常 / 边界** | ⚠️ **与 `REQ-3D-008`（胶路结果保留）可能有重叠** —— "生成的胶点/胶线"与"保留的胶路"是不是同一个东西？<br>**AI 建议**：`005` 管**实时生成**，`008` 管**结束后仍保留** ⇒ **需你确认这个切分** |
| **依赖** | `REQ-IO-002` · `REQ-3D-008` |
| **来源** | S-1 附录 A.1 #4 |
| **优先级** | **P1** |
| **可测性** | 演示 |
| **状态** | ⏳ 待裁 |

#### `REQ-3D-009` · 多设备同一场景内多实例

| 字段 | 内容 |
|---|---|
| **描述** | 同一 3D 场景内应**同时**出现多个设备实例，而非一次只显示一台。 |
| **验收判据** | ① 场景内同时出现 **≤8 个**设备实例（`AS-002` 已裁为**设计容量**，**上限可配置**；演示默认 3–4 台）<br>② 每个实例**独立**反映各自设备的状态<br>③ 点击某实例 ⇒ 切换到该设备详情 |
| **前置条件** | 已配置 ≥2 台设备 |
| **异常 / 边界** | ✅ **已裁（2026-09-20）：上限 8 台**（`AS-002`，**可配置**）；实例过多时靠 `REQ-3D-010` 的精度分级控制负载 |
| **依赖** | `AS-002`（**8 台，可配置**）· `REQ-3D-010`（精度分级）· `REQ-DEV-001` |
| **来源** | S-1 附录 A.1 #8 · `B-013`（单场景多实例 + 精度分级） |
| **优先级** | **P0**（差异化卖点） |
| **可测性** | 演示 |
| **状态** | ⏳ 待裁 |

#### `REQ-PRG-003` · 轨迹插值（可单测）

| 字段 | 内容 |
|---|---|
| **描述** | 系统应对直线 / 圆弧基元做插值，生成可执行的中间点序列。 |
| **验收判据** | ① 给定直线参数 ⇒ 输出中间点序列，**点间距 ≤ 设定值**<br>② 给定圆弧参数 ⇒ 输出**落在圆弧上**的点（可断言）<br>③ **该断言可作为单测**（不依赖界面） |
| **前置条件** | 程序中已有对应基元 |
| **异常 / 边界** | 零长度直线 / 半径为 0 的圆弧 ⇒ **应被拒绝或退化为点**（不得产生除零）；插值精度参数待定（建议 S1 定） |
| **依赖** | `REQ-PRG-002`（基元）· `REQ-ARCH-005`（基元可扩展） |
| **来源** | S-1 §6（点名"轨迹插值"）· `B-016`（与点级参数关系） |
| **优先级** | **P0** |
| **可测性** | **单测** |
| **状态** | ⏳ 待裁 |

#### `REQ-CHK-004` · 校验可单测

| 字段 | 内容 |
|---|---|
| **描述** | 程序校验规则应可被单元测试逐条覆盖。 |
| **验收判据** | ① 每条校验规则**各有 ≥1 个合法用例与 ≥1 个非法用例**<br>② 校验逻辑**不依赖界面**（可直接调用）<br>③ 校验结果含**可断言的结构**（如 `{规则, 基元序号, 是否通过}`） |
| **前置条件** | — |
| **异常 / 边界** | 多条规则同时失败 ⇒ 应**全部报出**还是**只报第一条**？（**AI 建议**：全部报出，便于一次修完）⇒ **需你确认** |
| **依赖** | `REQ-CHK-001/002/003/005` |
| **来源** | S-1 §6（点名"校验规则"） |
| **优先级** | **P0** |
| **可测性** | **单测** |
| **状态** | ⏳ 待裁 |

---

### 二、标准展开（19 条 · 7 字段）

#### D1 3D 场景（其余 8 条）

| ID | 条目 | 描述 | 验收判据（≥2 条） | 来源 | 优先级 | 可测性 |
|---|---|---|---|---|---|---|
| `REQ-3D-001` | 加载并显示龙门机构 | 启动后 3D 视区应显示由图元拼出的龙门机构 | ① 出现 6 部件：底座/双立柱/横梁/X滑座/Z主轴/针头<br>② 可用鼠标旋转/平移/缩放 | S-1 附录 A.1 #1 | **P0** | 演示 |
| `REQ-3D-002` | 运动归属正确 | 机构的运动应与"龙门沿 Y 移动"一致 | ① 底座不动<br>② 立柱+横梁**整体沿 Y** 动<br>③ X 滑座沿横梁动、Z 沿 Z 动 | S-1 §1 + `CR-003` | **P0** | 演示 |
| `REQ-3D-003` | 三轴位置实时映射 | 3D 中机构位置应随坐标实时改变 | ① 坐标变化后部件位置随之改变<br>② **状态/坐标刷新 ≤ 50ms**（`AS-001` 已裁；**可配置**） | S-1 附录 A.1 #2 | **P0** | 演示 |
| `REQ-3D-006` | 编程轨迹渲染 | 轨迹基元应渲染为线，已执行与未执行可区分 | ① 点/直线/圆弧均渲染为线<br>② 已执行段与未执行段**视觉可区分** | S-1 附录 A.1 #5 | **P1** | 演示 |
| `REQ-3D-007` | 执行进度可见 | 当前位置应有高亮标记 | ① 有高亮标记<br>② 随执行推进而移动 | S-1 附录 A.1 #6 · `B-032` | **P1** | 演示 |
| `REQ-3D-008` | 胶路结果保留 | 已点胶路径应保留在场景中 | ① 程序结束后路径仍在<br>② 切换设备后该台路径可再次看到 | S-1 附录 A.1 #7 · `B-032` | **P1** | 演示 |
| `REQ-3D-010` | 渲染精度分级 | 详情实例高精度、其余实例低精度 | ① 详情实例为**完整图元模型**<br>② 其余为**低精度表示**<br>③ 分级差异 = `AS-005`（**待定**） | S-1 附录 A.1 #8 | **P1** | 演示 |
| `REQ-3D-011` | 3D 视区刷新率达标 | 3D 视区刷新率应达到设定值 | ① **3D 渲染 UI 刷新 ≥ 10Hz（100ms）**<br>② 状态/坐标刷新 ≤ 50ms（`REQ-3D-003`）<br>③ 两者均**可配置**（`REQ-INF-006`） | 第 3 组 `F1` | **P1** | 人工（性能测试） |

#### D2 程序与轨迹（其余 7 条）

| ID | 条目 | 描述 | 验收判据（≥2 条） | 来源 | 优先级 | 可测性 |
|---|---|---|---|---|---|---|
| `REQ-PRG-001` | 程序可新建/打开/保存 | 系统应支持程序的基本文件操作 | ① 新建产生空程序<br>② 保存后重新打开**内容一致** | `B-011`（四类表） | **P0** | **单测**（读回一致） |
| `REQ-PRG-002` | 基元可增删改 | 支持添加/删除/修改点、直线、圆弧 | ① 三种基元均可增删改<br>② 列表随之更新 | S-1 附录 A.1 #5 | **P0** | 演示 |
| `REQ-PRG-004` | 基元顺序可调 | 程序内基元顺序应可调整 | ① 可上移/下移<br>② 执行顺序随之改变 | 编程常规 | **P1** | **单测** |
| `REQ-PRG-005` | 未保存改动有提示 | 存在未保存改动时应提示 | ① 关闭/切换时**有提示**<br>② 提示可取消操作 | 子项 `S3` | **P2** | 演示 |
| `REQ-PRG-006` | 编辑与运行互斥 | 程序被设备执行时不得被编辑 | ① 编辑中的程序被执行 ⇒ 按规则阻止或告警<br>② 规则**明确可述** | 子项 `S4` | **P1** | **单测** |
| `REQ-PRG-007` | 参数修改可撤销 | 工艺参数修改后应可撤销 | ① 可撤销回上一值<br>② 撤销后程序状态一致 | 子项 `S5` | **P2** | **单测** |
| `REQ-PRG-008` | 程序可被调用切换 | 可从列表选择程序并切换为当前程序 | ① 可切换<br>② 切换后显示当前程序编号 | `U1` | **P1** | 演示 |

#### D3 程序校验（其余 4 条）

| ID | 条目 | 描述 | 验收判据（≥2 条） | 来源 | 优先级 | 可测性 |
|---|---|---|---|---|---|---|
| `REQ-CHK-001` | 校验坐标越界 | 程序坐标超出行程应报错 | ① 越界 ⇒ 报错<br>② 指出**第几条** | S-1 §2 T2 | **P0** | **单测** |
| `REQ-CHK-002` | 校验空程序 | 无轨迹的程序应报错 | ① 空程序 ⇒ 报错<br>② 报错可读 | S-1 §2 T2 | **P0** | **单测** |
| `REQ-CHK-003` | 结果可定位到基元 | 报错应含基元序号 | ① 报错含**基元序号**<br>② 界面可跳转到该基元 | 可用性 | **P1** | 演示 |
| `REQ-CHK-005` | 碰撞 / 干涉检查 | 路径与机构相交应报错（B 级新增） | ① 相交 ⇒ 报错<br>② 指出**相交的基元序号** | `U11`（01-资料 方向 4） | **P2** | **单测** |

---

#### 三、本批自检（交你复核）

| # | 观察 | 需你确认 |
|---|---|---|
| 1 | `REQ-3D-005` 与 `REQ-3D-008` 的**重叠** 我给了切分方案（005 管实时生成、008 管结束后保留） | **需你确认这个切分** |
| 2 | `REQ-3D-004` 我建议加**"颜色 + 标识"双通道**（色觉友好） | **需你确认是否加** |
| 3 | `REQ-CHK-004` 的**多规则失败时全部报出 vs 只报第一条** | **需你确认** |
| 4 | ~~3 条含待定值~~ → `3D-003`/`3D-011` **已填**（`AS-001` 已裁）；**`3D-010` 仍等 `AS-005`** | 剩 `AS-005` |

#### 四、统计

| 项 | 值 |
|---|---|
| 本批条数 | **24**（3D 11 + PRG 8 + CHK 5） |
| 深度 / 标准 | 5 / 19 |
| P0 条数 | **10** |
| 明确可单测 | **9** |

---

## 批5 · D6/D7/D11+N系列

### 一、深度展开（5 条 · 10 字段）

#### `REQ-DB-002` · 存盘读回一致（可单测）

| 字段 | 内容 |
|---|---|
| **描述** | 各类数据写入持久化后，读回应与写入**完全一致**。 |
| **验收判据** | ① 写入程序 / 参数 / 记录后读回，**内容完全一致**（可断言）<br>② 该断言**可作为单测**<br>③ 覆盖四类表：设备 / 程序 / 工艺 / 记录 |
| **前置条件** | 持久化层可用 |
| **异常 / 边界** | **浮点数的精度处理**需明确（直接比较可能失败）⇒ 建议按容差比较；写入过程中断电 ⇒ 不得产生**半写**的坏记录 |
| **依赖** | `REQ-DB-001` · `REQ-DB-004`（选型待 `B-021`） |
| **来源** | S-1 §6（点名"存储"）· `B-011`（四类表） |
| **优先级** | **P0** |
| **可测性** | **单测** |
| **状态** | ⏳ 待裁 |

#### `REQ-INF-002` · 核心模块有单元测试

| 字段 | 内容 |
|---|---|
| **描述** | S-1 §6 点名的 5 个核心模块应各有单元测试且全部通过。 |
| **验收判据** | ① 以下 5 处**各 ≥1 条单测**且通过：**轨迹插值**（`PRG-003`）· **校验规则**（`CHK-004`）· **虚拟下位机时序**（`VMC-002`）· **归正**（`ERR-004`）· **存储**（`DB-002`）<br>② 单测可**一条命令运行**<br>③ 单测**不依赖界面** |
| **前置条件** | 工程已可构建（`REQ-INF-001`） |
| **异常 / 边界** | **框架待裁**（`B-022`：建议 GoogleTest）；**测试覆盖率无硬指标**（S-1 §6 只要求"核心模块有"，未要求百分比） |
| **依赖** | `REQ-INF-001` · 上述 5 条 · `B-022`（框架待裁） |
| **来源** | S-1 §5 工程化 ② + §6 |
| **优先级** | **P0**（工程化证据） |
| **可测性** | **单测**（本条就是"有单测"本身） |
| **状态** | ⏳ 待裁 |

#### `REQ-INF-005` · 数值可追溯

| 字段 | 内容 |
|---|---|
| **描述** | 文档中出现的每个数字都应能指出来源（实测值 / 行业数据 / 用户裁决）。 |
| **验收判据** | ① 抽查文档中的数字 ⇒ **能指出出处**<br>② 无法溯源的数字**必须显式标注**"假设 / 推测 / 待验证"<br>③ 不再出现"约""估算"**未标注**的数字 |
| **前置条件** | — |
| **异常 / 边界** | 本条源于**实际教训**：本项目已发生 **4 次**"报未实测数字"（约 108→实 110 等）⇒ 它是**流程性要求写进需求** |
| **依赖** | `REQ-PRC-002`（来源可追溯） |
| **来源** | 用户在 `CR-003` 立的规矩 + 2026-09-19 第 4 次纠正 |
| **优先级** | **P1** |
| **可测性** | 人工（抽查） |
| **状态** | ⏳ 待裁 |

#### `REQ-LOG-002` · 事件日志

| 字段 | 内容 |
|---|---|
| **描述** | 报警与状态变化应留有事件记录。 |
| **验收判据** | ① 每次报警产生一条记录<br>② 每次状态变化（如 运行→暂停）产生一条记录<br>③ 记录含**时间戳 + 设备 + 事件类型** |
| **前置条件** | 日志存储可用 |
| **异常 / 边界** | ✅ **已裁（2026-09-20）：全记**，不做粒度筛选 —— 用户原则「**可以没用，不能没有**」；容量由 `REQ-LOG-009` 保留策略管控 |
| **依赖** | `REQ-LOG-003`（时间戳）· `REQ-DB-001` · `REQ-STA-001` |
| **来源** | `03-资料` 方向 4（"日志分类：生产线/操作/设备状态"） |
| **优先级** | **P1** |
| **可测性** | **单测**（记录生成）+ 演示 |
| **状态** | ⏳ 待裁 |

#### `REQ-LOG-007` · **误差日志**（本项目特色：偏差可追溯）

| 字段 | 内容 |
|---|---|
| **描述** | 每次执行的**每个点**都应记录：**预期坐标 / 实际坐标 / 偏差量 / 是否超阈值 / 本次归正修正量** —— 使每一件活的偏差可追溯。 |
| **验收判据** | ① 每点一条记录，**5 个字段齐全**（可单测断言字段完整性）<br>② 可按**设备 / 程序 / 时间段**查询（`REQ-LOG-004`）<br>③ 可**导出**（CSV） |
| **前置条件** | 已收到误差上报（`REQ-ERR-003`） |
| **异常 / 边界** | 点数可能很多（资料：程序可达数万点）⇒ 记录量大，**由 `REQ-LOG-009` 管控**（滚动 / 归档）；⚠️ **不得因量大而少记字段**；⚠️ **也不得因 `REQ-ERR-006` 的"提醒合并"而少记条目** —— 提醒合并只影响显示 |
| **依赖** | `REQ-ERR-003` · `REQ-ERR-004` · `REQ-LOG-003` |
| **来源** | 用户原则「**信息一定要记录充足，可以没用不能没有**」+ 误差用途 2 |
| **优先级** | **P0**（误差可追溯，面试可讲） |
| **可测性** | **单测**（字段完整）+ 演示（导出查看） |
| **状态** | ⏳ 待裁 |

#### `REQ-MON-001` · 设备状态总览

| 字段 | 内容 |
|---|---|
| **描述** | 应有一屏可同时看到全部设备的状态。 |
| **验收判据** | ① 一屏列出全部设备及其状态<br>② **不需逐台切换**即可看到<br>③ 状态变化时**实时更新** |
| **前置条件** | ≥2 台设备 |
| **异常 / 边界** | 与 `REQ-DEV-001`（设备列表）**边界需明确** —— 建议：`DEV-001` 是"列表 + 可操作"，`MON-001` 是"只读总览"。**需你确认** |
| **依赖** | `REQ-DEV-003`（状态汇聚）· `REQ-UI-001` |
| **来源** | S-1 §3 · `N4`（监控统计） |
| **优先级** | **P1** |
| **可测性** | 演示 |
| **状态** | ⏳ 待裁 |

---

### 二、标准展开（27 条 · 7 字段）

#### D6 工艺参数（6 条）

| ID | 条目 | 描述 | 验收判据（≥2 条） | 来源 | 优先级 | 可测性 |
|---|---|---|---|---|---|---|
| `REQ-PRM-001` | 出胶时间可设 | 可设置点胶出胶时间 | ① 可设<br>② 设置后实际生效 | `C2`(1/5) | **P0** | **单测** |
| `REQ-PRM-002` | 气压可设 | 可设置气压值 | ① 可设<br>② 设置后实际生效 | `C2`(2/5) | **P0** | **单测** |
| `REQ-PRM-003` | 点胶速度可设 | 可设置点胶速度 | ① 可设<br>② 影响执行耗时 | `C2`(3/5) | **P0** | **单测** |
| `REQ-PRM-004` | 提前开胶可设 | 可设置提前开胶量 | ① 可设<br>② 影响开胶时机 | `C2`(4/5) | **P1** | **单测** |
| `REQ-PRM-005` | 延迟关胶可设 | 可设置延迟关胶量 | ① 可设<br>② 影响关胶时机 | `C2`(5/5) | **P1** | **单测** |
| `REQ-PRM-006` | 点级可覆盖程序级 | 点可覆盖程序级参数，未设则回退 | ① 某点设了 ⇒ 用点的<br>② 未设 ⇒ **回退**程序级<br>③ 回退行为可断言 | `B-016` | **P0** | **单测** |

#### D7 视觉简化版（3 条）—— ⚠️ 形式待 `B-030`

| ID | 条目 | 描述 | 验收判据（≥2 条） | 来源 | 优先级 | 可测性 |
|---|---|---|---|---|---|---|
| `REQ-VIS-001` | 针头对位（模拟） | 可执行对位并产出偏移量 | ① 可执行对位<br>② 产出偏移量并被记录 | `B-030` 建议值 | **P1** | 演示 |
| `REQ-VIS-002` | Z 测高（模拟） | 可执行测高并产出高度值 | ① 可执行测高<br>② 产出 Z 高度并被记录 | `B-030` 建议值 | **P1** | 演示 |
| `REQ-VIS-003` | 结果进入误差链路 | 对位/测高结果应作为误差来源之一 | ① 结果上报到误差链路<br>② 参与归正计算 | S-1 §7 | **P1** | **单测** |

#### D11 基础设施（其余 3 条）

| ID | 条目 | 描述 | 验收判据（≥2 条） | 来源 | 优先级 | 可测性 |
|---|---|---|---|---|---|---|
| `REQ-INF-001` | 一键构建 | CMake 配置后一条命令构建出可运行程序 | ① 一条命令完成构建<br>② 产物可直接运行 | S-1 §5 工程化① | **P0** | 演示 |
| `REQ-INF-003` | README 含架构图与亮点 | README 应含架构图与一句话亮点 | ① 有架构图<br>② 有一句话亮点 | S-1 §5 工程化③ | **P0** | 人工 |
| `REQ-INF-004` | 源码树与命名规范可查 | 有规范文件且目录结构一致 | ① 规范文件存在<br>② 目录结构与之一致 | S-1 §5 工程化④ · `B-025` | **P1** | 人工 |
| `REQ-INF-006` | **参数可配置（全局约束）** | 所有阈值 / 上限 / 周期 / 开关都应可配置，**不得硬编码** | ① 需求中出现的数值都能在配置中找到对应项<br>② 配置**集中管理**（一处定义），不得散落各模块分支<br>③ 每项有默认值 + 取值范围，非法值有明确处理<br>④ **安全项不可通过配置关闭**（如急停本地优先） | 用户原则（2026-09-20）+ `00-流程/05-设计原则.md` | **P0**（贯穿） | **单测**（加载/非法值）+ 人工 |

#### N1 报警（4 条）

| ID | 条目 | 描述 | 验收判据（≥2 条） | 来源 | 优先级 | 可测性 |
|---|---|---|---|---|---|---|
| `REQ-ALM-001` | 报警分三级 | 报警应分信息/警告/严重三级 | ① 三级可区分<br>② 仅"严重"触发停机 | `03-资料` 方向 4 | **P1** | 演示 |
| `REQ-ALM-002` | 需两次确认 | 报警须经两次确认才清除 | ① 第一次关声光<br>② 第二次清画面<br>③ 未确认**不清除** | `03-资料` 方向 4 | **P1** | 演示 |
| `REQ-ALM-003` | 覆盖常见类型 | 至少覆盖气压异常/轴超限/通信失败 | ① 三类均可触发<br>② 各有对应提示 | `03-资料` 方向 4 | **P1** | 演示 |
| `REQ-ALM-004` | 严重报警触发停机 | 严重报警应按 `P-5` 触发停机 | ① 严重报警 ⇒ 停机<br>② 与 `RUN-009` 协同 | S-1 附录 B | **P0** | **单测** |

#### N2 日志（其余 3 条）

| ID | 条目 | 描述 | 验收判据（≥2 条） | 来源 | 优先级 | 可测性 |
|---|---|---|---|---|---|---|
| `REQ-LOG-001` | 操作日志 | 参数修改应留记录（谁/何时/改了什么） | ① 每次修改一条记录<br>② 含修改前后值 | `03-资料` 方向 4 | **P1** | **单测** |
| `REQ-LOG-003` | 记录带时间戳 | 每条记录应带时间戳 | ① 有可读时间戳<br>② 排序正确 | `W1` | **P1** | **单测** |
| `REQ-LOG-004` | 可按条件查询 | 日志可按条件查询 | ① 可按时间筛选<br>② 可按类型筛选 | `03-资料` 方向 4 | **P2** | 演示 |
| `REQ-LOG-005` | **通信日志** | 连接的建立/断开/重连/超时/收发失败都应记录 | ① 断连有记录<br>② 重连成功有记录<br>③ 含对端与原因 | `REQ-COM-003/004` | **P1** | **单测** |
| `REQ-LOG-006` | **执行日志** | 每次执行应记录：设备 / 程序 / 起止时间 / 耗时 / 结果（完成·中断·急停） | ① 一次执行一条记录<br>② 含结果与耗时 | `REQ-RUN-*` | **P1** | **单测** |
| `REQ-LOG-008` | **系统日志** | 启动 / 关闭 / 异常退出 / 恢复都应记录 | ① 启动与关闭各有记录<br>② 崩溃恢复后能看到上次退出的记录 | `REQ-NFR-003` | **P2** | 演示 |
| `REQ-LOG-009` | **保留策略** | 日志应有容量上限与滚动归档，并支持导出 | ① 达上限按策略滚动（不得静默丢弃关键字段）<br>② 可导出全部或区间 | 由「全记」推导（配套需求） | **P1** | **单测** |

#### N3 存储（其余 3 条）

| ID | 条目 | 描述 | 验收判据（≥2 条） | 来源 | 优先级 | 可测性 |
|---|---|---|---|---|---|---|
| `REQ-DB-001` | 四类数据可持久化 | 设备/程序/工艺/记录重启后仍在 | ① 四类均持久化<br>② 重启后可读 | `B-011` 四类表 | **P0** | **单测** |
| `REQ-DB-003` | 可追溯查询 | 可按标识查到历史生产参数 | ① 可按 S/N 或时段查<br>② 结果含当时参数 | `U2` | **P2** | 演示 |
| `REQ-DB-004` | 选型落地 | 按 `B-021` 定案实现 | ① 按 `B-021` 选型<br>② 配置与业务数据分离 | `B-021`（**待裁**） | **P1** | 人工 |

#### N4 监控统计（其余 2 条）+ N5 2D 界面（3 条）

| ID | 条目 | 描述 | 验收判据（≥2 条） | 来源 | 优先级 | 可测性 |
|---|---|---|---|---|---|---|
| `REQ-MON-002` | 产量计数 | 可看到产量且与执行结果一致 | ① 显示产量<br>② 与实际执行一致 | `03-资料` 方向 6 | **P2** | 演示 |
| `REQ-MON-003` | 数据可导出 | 数据可导出为 CSV/Excel | ① 可导出<br>② 内容与界面一致 | `U3`+`W2` | **P2** | 演示 |
| `REQ-UI-001` | 主框架存在 | 有主窗口 + 设备区 + 3D 区 + 操作区 | ① 四区齐全<br>② 布局可用 | 3D 之外的必要界面 | **P0** | 演示 |
| `REQ-UI-002` | 参数表单可编辑生效 | 改表单后参数实际改变 | ① 可编辑<br>② 保存后生效 | `REQ-PRM-*` 载体 | **P0** | 演示 |
| `REQ-UI-003` | 操作有反馈 | 关键操作有成功/失败提示 | ① 成功有提示<br>② 失败有原因 | 可用性 | **P1** | 演示 |

---

#### 三、本批自检（交你复核）

| # | 观察 | 需你确认 |
|---|---|---|
| 1 | `REQ-MON-001` 与 `REQ-DEV-001` 的**边界**我给了切分（列表可操作 vs 总览只读） | **需你确认** |
| 2 | `REQ-LOG-002` 的**记录粒度**（每次状态变化都记 vs 只记关键迁移） | **需你确认** |
| 3 | `REQ-DB-002` 的**浮点精度处理**（建议按容差比较） | **需你确认** |
| 4 | `REQ-VIS-001~003` 现按 `B-030` 建议值（针头对位+Z测高）写 | **`B-030` 定了要重写** |

#### 四、统计

| 项 | 值 |
|---|---|
| 本批条数 | **32**（PRM 6 · VIS 3 · INF 5 · ALM 4 · LOG 4 · DB 4 · MON 3 · UI 3） |
| 深度 / 标准 | 5 / 27 |
| P0 条数 | **12** |
| 明确可单测 | **13** |

---

## 批6 · F非功能+X结构+ARCH架构冗余

### 一、深度展开（9 条 · 10 字段）

#### `REQ-NFR-002` · 多设备下响应不劣化到不可用

| 字段 | 内容 |
|---|---|
| **描述** | N 台设备同时在线时，界面应仍可操作，不出现卡死或无响应。 |
| **验收判据** | ① 在 **8 台**（`AS-002`，可配置）下，界面操作**仍有响应**（无卡死）<br>② 状态刷新**不中断**<br>③ 设备数增加时，响应时间**增长可控**（不得指数劣化） |
| **前置条件** | 已配置设备（容量上限 **8 台**，`AS-002`；**可配置**） |
| **异常 / 边界** | **"不可用"的阈值未定**（建议定"任意操作响应 ≤ 2s"之类）⇒ **需你确认**；本条与 `REQ-3D-010`（精度分级）**协同**——精度分级正是为了不劣化 |
| **依赖** | `AS-002`（台数）· `REQ-3D-010` · `REQ-DEV-003` |
| **来源** | 第 3 组 `F1` 性能 |
| **优先级** | **P0** |
| **可测性** | 人工（性能观察）+ 演示 |
| **状态** | ⏳ 待裁 |

#### `REQ-NFR-003` · 上位机崩溃后可恢复现场

| 字段 | 内容 |
|---|---|
| **描述** | 上位机异常退出后重启，应能恢复到最近一次已知状态。 |
| **验收判据** | ① 重启后能读到**最后一次已知的设备与程序状态**<br>② 恢复后设备状态为**急停或就绪**（**不得自动恢复运行**）<br>③ 未保存的程序改动**不丢失**或**明确提示丢失** |
| **前置条件** | 崩溃前已有状态快照 |
| **异常 / 边界** | ⚠️ **"恢复到什么程度"需你定**：仅恢复界面选择（轻）？还是要恢复设备状态（重）？<br>**AI 建议**：只恢复上位机侧（选中设备 / 未保存程序），**设备状态由设备自身维持** |
| **依赖** | `REQ-DB-001`（持久化）· `REQ-RUN-004`（急停态不得自恢复）· `REQ-ARCH-007` |
| **来源** | `W4`（横切项） |
| **优先级** | **P1** |
| **可测性** | 演示（杀进程后重启） |
| **状态** | ⏳ 待裁 |

#### `REQ-PRC-001` · 有"砍范围"的触发条件

| 字段 | 内容 |
|---|---|
| **描述** | 文档应写明：出现何种情况时应砍掉哪一档功能。 |
| **验收判据** | ① 文档中存在**明确的降级触发条件**（如"若 X 未完成则砍 P2"）<br>② 降级对象**可指名**（按优先级 P0/P1/P2）<br>③ 触发条件**可判定**（不是"时间紧就砍"这种） |
| **前置条件** | — |
| **异常 / 边界** | **触发条件的具体内容需你定**（我无法替你判断"什么情况算做不完"）；本条只要求"存在且可判定" |
| **依赖** | 优先级标（`P0/P1/P2`）已在全部条目上标注 ⇒ **本条才有依据** |
| **来源** | `X1`（= `../70-Backlog/` 的 **BL-006**） |
| **优先级** | **P0**（单人项目的安全阀） |
| **可测性** | 人工（查文档） |
| **状态** | ⏳ 待裁 |

#### `REQ-PRC-002` · 每条需求必须能指出「来源」

| 字段 | 内容 |
|---|---|
| **描述** | 需求基线中每条都应标注来源，且来源**真实存在**。 |
| **验收判据** | ① 每条**有来源栏且非空**<br>② 来源指向的对象**确实存在**（可 grep 到）<br>③ 无法溯源的条目**不得进入基线** |
| **前置条件** | — |
| **异常 / 边界** | 本条**已由脚本闸门部分机械化**（`../../00-流程/check-consistency.py` 会检查"来源为空"） |
| **依赖** | `REQ-INF-005`（数值可追溯） |
| **来源** | S-1 §七 `R3` · `X3` |
| **优先级** | **P0** |
| **可测性** | **单测**（脚本闸门） |
| **状态** | ⏳ 待裁 |

#### `REQ-ARCH-000` · "接口预留、实现可替换"成为可验证要求

| 字段 | 内容 |
|---|---|
| **描述** | 每个 C 级预留项都应能指出其扩展点在哪个接口 / 哪层抽象 —— 使"预留"可验证而非口头承诺。 |
| **验收判据** | ① 对 `REQ-ARCH-001~008` **逐条**能指出**具体的接口 / 抽象层位置**<br>② 该位置**在代码中存在**（可指出文件 / 类 / 接口名）<br>③ 新增实现时**不需改既有分支逻辑** |
| **前置条件** | S1 架构已产出 |
| **异常 / 边界** | ⚠️ **本条是"C 级预留"能否兑现的唯一保证**（用户明确要求"C 级预留且给架构冗余"）；若本条不落地，则 8 条 `REQ-ARCH-*` 会**退化为口头承诺** |
| **依赖** | S1 架构基线 · `REQ-ARCH-001~008` |
| **来源** | `X2`（经"C 级要架构冗余"升级为必做） |
| **优先级** | **P0** |
| **可测性** | 人工（对照代码核查） |
| **状态** | ⏳ 待裁 |

#### `REQ-ARCH-002` · 故障注入的预留

| 字段 | 内容 |
|---|---|
| **描述** | 虚拟下位机应预留**故障模式注入点**（本阶段不实现，只留钩子）。 |
| **验收判据** | ① 虚拟下位机存在**可注入故障的扩展点**（接口 / 枚举 / 钩子）<br>② 当前**不实现**具体故障（或只实现 1 个作为示例）<br>③ 将来新增故障模式**不需改既有执行逻辑** |
| **前置条件** | `REQ-VMC-001` 已实现 |
| **异常 / 边界** | ⚠️ **`U10` 故障注入可考虑提到"做"**（它是虚拟仿真的突出价值、极易演示）⇒ **若提到"做"，本条升级为功能条目** |
| **依赖** | `REQ-VMC-001` · `REQ-ARCH-000` |
| **来源** | `X2` + `U10`（01-资料 方向 4「故障注入是虚拟仿真的突出价值」） |
| **优先级** | **P2** |
| **可测性** | 人工（查代码） |
| **状态** | ⏳ 待裁 |

#### `REQ-ARCH-004` · 传感器仿真的预留

| 字段 | 内容 |
|---|---|
| **描述** | 虚拟下位机应预留**传感器抽象接口**（当前只实现限位）。 |
| **验收判据** | ① 存在传感器抽象接口（编码器 / 限位等可挂在其下）<br>② 当前**只实现限位**（`REQ-VMC-004`）<br>③ 新增传感器类型**不需改既有逻辑** |
| **前置条件** | `REQ-VMC-004`（限位）已实现 |
| **异常 / 边界** | 传感器接口的"噪声"能力（01-资料 提到"含噪声"）是否要预留？**建议预留参数位，不实现** ⇒ **需你确认** |
| **依赖** | `REQ-VMC-004` · `REQ-ARCH-000` |
| **来源** | `X2` + `U8` |
| **优先级** | **P2** |
| **可测性** | 人工（查代码） |
| **状态** | ⏳ 待裁 |

#### `REQ-ARCH-007` · 中断恢复的预留

| 字段 | 内容 |
|---|---|
| **描述** | 状态持久化应预留**快照 / 恢复点**字段与接口。 |
| **验收判据** | ① 状态持久化结构中存在**快照 / 恢复点**相关字段<br>② 存在**恢复接口**（当前可不实现完整恢复）<br>③ 与 `REQ-NFR-003` 衔接 |
| **前置条件** | `REQ-DB-001` 已实现 |
| **异常 / 边界** | **快照粒度需定**（设备级 / 全局）⇒ 建议设备级（**需你确认**） |
| **依赖** | `REQ-DB-001` · `REQ-NFR-003` · `REQ-ARCH-000` |
| **来源** | `X2` + `W4` |
| **优先级** | **P2** |
| **可测性** | 人工（查代码） |
| **状态** | ⏳ 待裁 |

#### `REQ-ARCH-008` · 时间预估的预留

| 字段 | 内容 |
|---|---|
| **描述** | 程序模型应预留**时间估算接口**（可由虚拟下位机实现）。 |
| **验收判据** | ① 程序模型存在时间估算接口<br>② 可由虚拟下位机基于单点耗时实现（`REQ-VMC-002`）<br>③ 当前可不实现完整估算 |
| **前置条件** | `REQ-VMC-002`（时序）已实现 |
| **异常 / 边界** | 与 `REQ-VMC-002` 的耗时数据**天然衔接** ⇒ 实现成本可能很低，**可考虑直接做**（**需你判断**） |
| **依赖** | `REQ-VMC-002` · `REQ-ARCH-000` |
| **来源** | `X2` + `U12` |
| **优先级** | **P2** |
| **可测性** | 人工（查代码） |
| **状态** | ⏳ 待裁 |

---

### 二、标准展开（9 条 · 7 字段）

#### F 非功能（其余 6 条）

| ID | 条目 | 描述 | 验收判据（≥2 条） | 来源 | 优先级 | 可测性 |
|---|---|---|---|---|---|---|
| `REQ-NFR-001` | 3D 视区刷新率达标 | 3D 刷新率应达到设定值 | ① **3D 渲染 UI 刷新 ≥ 10Hz（100ms）**<br>② 状态/坐标刷新 ≤ 50ms（`REQ-3D-003`）<br>③ 两者均**可配置**（`REQ-INF-006`） | 第 3 组 `F1` | **P1** | 人工（性能测试） |
| `REQ-NFR-004` | 代码不写平台相关 | 不得硬编码 Windows 路径 / 用 Win32 API / 用 `#ifdef` 塞业务逻辑 | ① 无硬编码路径<br>② 无 Win32 API 直调<br>③ 无业务 `#ifdef` 分支 | `F3`（S-1 §四） | **P1** | 人工（查代码） |
| `REQ-NFR-005` | 命名规范被遵守 | 命名应符合项目规范 | ① 抽查 10 个文件均一致<br>② 规范文件存在 | `F4` · `B-025` | **P2** | 人工 |
| `REQ-NFR-006` | 术语对外合规 | 对外表述不得出现 "L3" | ① README 中无 "L3"<br>② 统一使用固定表述句 | `F6`（S-1 §五） | **P1** | 人工 |
| `REQ-NFR-007` | 错误码可查 | 应有错误码 / 故障代码对照表 | ① 存在对照表<br>② 报错给出错误码 | `W5` | **P2** | 人工 |
| `REQ-NFR-008` | **数据刷新与 UI 刷新分离** | 状态/坐标按 50ms 收取，UI（3D 渲染）按 10Hz 取最新值绘制，两者解耦 | ① 两者频率**独立可配**<br>② UI 掉帧**不丢数据点**<br>③ 界面显示最新值，不堆积历史帧 | `AS-001` 分层方案（B）的配套需求 | **P0** | **单测**（解耦）+ 演示 |

#### REQ-ARCH 其余 4 条（7 字段）

| ID | 条目 | 描述 | 验收判据（≥2 条） | 来源 | 优先级 | 可测性 |
|---|---|---|---|---|---|---|
| `REQ-ARCH-001` | 趋势曲线的预留 | 监控统计数据模型保留时间序列字段 | ① 数据模型含时间序列字段<br>② 界面预留视图位 | `X2`+`U9` | **P2** | 人工（查代码） |
| `REQ-ARCH-003` | 报警洪泛抑制的预留 | 报警数据结构预留优先级 / 抑制标志 | ① 含优先级字段<br>② 含抑制标志字段 | `X2`+`U13` | **P2** | 人工（查代码） |
| `REQ-ARCH-005` | 轨迹基元可扩展 | 基元用可扩展类型表示 | ① 新增类型**不改既有分支**<br>② 类型定义集中 | `X2`+`U6` | **P2** | 人工（查代码） |
| `REQ-ARCH-006` | 开机自检的预留 | 启动流程预留自检阶段钩子 | ① 启动流程有自检钩子<br>② 当前可不实现具体自检项 | `X2`+`W3` | **P2** | 人工（查代码） |

---

#### 三、本批自检（交你复核）

| # | 观察 | 需你确认 |
|---|---|---|
| 1 | **`REQ-ARCH-000` 是 8 条预留的"担保人"** | 若它不落地，8 条 `REQ-ARCH-*` 就是**空话** ⇒ 建议列为**必做** |
| 2 | **`REQ-NFR-003` 我建议"只恢复上位机侧"** | 需你确认恢复到什么程度 |
| 3 | **`REQ-ARCH-008`（时间预估）实现成本可能很低**（与 `VMC-002` 天然衔接） | **可考虑直接做**，需你判断 |
| 4 | ~~`REQ-NFR-001` 判据为空~~ → **已填**（`AS-001` 已裁：UI ≥10Hz） | 但 ⚠️ `REQ-NFR-001` 与 `REQ-3D-011` **内容重复**，待合并 |

#### 四、统计

| 项 | 值 |
|---|---|
| 本批条数 | **18**（NFR 6 + PRC 2 + ARCH 9 + 1 条 `NFR-001` 计入 NFR 共 7？详见下） |
| 深度 / 标准 | 9 / 9 |
| P0 条数 | **3**（`NFR-002` · `PRC-001` · `PRC-002` · `ARCH-000`） |
| P2 条数 | **8**（多为架构冗余） |


---

# 附录 · 速查索引

| ID | 条目 |
|---|---|
| `REQ-3D-001` | 启动后加载并显示龙门机构（图元拼） |
| `REQ-3D-002` | 机构运动归属正确 |
| `REQ-3D-003` | 三轴位置实时映射到 3D |
| `REQ-3D-004` | 运行状态在场景内可区分 |
| `REQ-3D-005` | 出胶状态有可见反馈 |
| `REQ-3D-006` | 编程轨迹渲染 |
| `REQ-3D-007` | 执行进度可见 |
| `REQ-3D-008` | 胶路结果保留 |
| `REQ-3D-009` | 多设备同一场景内多实例 |
| `REQ-3D-010` | 渲染精度分级 |
| `REQ-3D-011` | 3D 视区刷新率达标 |
| `REQ-PRG-001` | 支持新建 / 打开 / 保存程序 |
| `REQ-PRG-002` | 支持三种轨迹基元的增删改 |
| `REQ-PRG-003` | 轨迹插值（可单测） |
| `REQ-PRG-004` | 程序内可调整基元顺序 |
| `REQ-PRG-005` | 未保存改动有提示 |
| `REQ-PRG-006` | "编辑中" 与 "设备运行" 的互斥规则生效 |
| `REQ-PRG-007` | 参数修改可撤销 |
| `REQ-PRG-008` | 程序可被调用切换 |
| `REQ-CHK-001` | 校验坐标越界 |
| `REQ-CHK-002` | 校验空程序 |
| `REQ-CHK-003` | 校验结果可定位到具体基元 |
| `REQ-CHK-004` | 校验可单测 |
| `REQ-CHK-005` | 碰撞 / 干涉检查 |
| `REQ-DEV-001` | 设备列表可见 |
| `REQ-DEV-002` | 设备「在线 / 离线」状态可见 |
| `REQ-DEV-003` | 状态汇聚 |
| `REQ-DEV-004` | 逐台下发 |
| `REQ-DEV-005` | 批量下发需二次确认 |
| `REQ-DEV-006` | 单台故障不扩散 |
| `REQ-DEV-007` | 每台设备持有独立参数集 |
| `REQ-RUN-001` | 启动 / 运行 |
| `REQ-RUN-002` | 暂停 |
| `REQ-RUN-003` | 停止 |
| `REQ-RUN-004` | 急停（本地优先） |
| `REQ-RUN-005` | 复位 |
| `REQ-RUN-006` | 回零 |
| `REQ-RUN-007` | 排胶 / 清洗 |
| `REQ-RUN-008` | 点动 / 手动移位 |
| `REQ-RUN-009` | 掉线 / 超时即停 |
| `REQ-PRM-001` | 出胶时间可设 |
| `REQ-PRM-002` | 气压可设 |
| `REQ-PRM-003` | 点胶速度可设 |
| `REQ-PRM-004` | 提前开胶可设 |
| `REQ-PRM-005` | 延迟关胶可设 |
| `REQ-PRM-006` | 点级可覆盖、缺省回退程序级 |
| `REQ-VIS-001` | 针头对位（模拟） |
| `REQ-VIS-002` | Z 测高（模拟） |
| `REQ-VIS-003` | 对位 / 测高结果进入误差链路 |
| `REQ-VMC-001` | 接收程序并可执行 |
| `REQ-VMC-002` | 逐点耗时（时序可单测） |
| `REQ-VMC-003` | 上报实时坐标 |
| `REQ-VMC-004` | 限位仿真（1 个点即可） |
| `REQ-VMC-005` | 时间推进可调倍速 |
| `REQ-VMC-006` | 可脱离上位机独立进程运行 |
| `REQ-ERR-001` | 固定幅度误差 |
| `REQ-ERR-002` | 随机扰动误差 |
| `REQ-ERR-003` | 误差上报到上位机 |
| `REQ-ERR-004` | 归正（可单测） |
| `REQ-ERR-005` | 归正后可复测 |
| `REQ-IO-001` | I/O 信号可仿真与显示 |
| `REQ-IO-002` | I/O 点与 3D / 状态一致 |
| `REQ-COM-001` | 通信经抽象层 |
| `REQ-COM-002` | 对外语义为 Modbus TCP |
| `REQ-COM-003` | 连接状态可见 |
| `REQ-COM-004` | 掉线可检测 |
| `REQ-INF-001` | 一键构建 |
| `REQ-INF-002` | 核心模块有单元测试 |
| `REQ-INF-003` | README 含架构图与一句话亮点 |
| `REQ-INF-004` | 源码树与命名规范可查 |
| `REQ-INF-005` | 数值可追溯 |
| `REQ-STA-001` | 状态集合明确 |
| `REQ-STA-002` | 急停可从任意状态进入 |
| `REQ-STA-003` | 非法迁移被拒绝 |
| `REQ-STA-004` | 状态迁移表驱动、可单测 |
| `REQ-ALM-001` | 报警分三级（信息/警告/严重） |
| `REQ-ALM-002` | 报警需两次确认 |
| `REQ-ALM-003` | 报警类型覆盖常见项 |
| `REQ-ALM-004` | 报警可触发停机 |
| `REQ-LOG-001` | 操作日志 |
| `REQ-LOG-002` | 事件日志 |
| `REQ-LOG-003` | 每条记录带时间戳 |
| `REQ-LOG-004` | 可按条件查询 |
| `REQ-LOG-005` | 通信日志 |
| `REQ-LOG-006` | 执行日志 |
| `REQ-LOG-008` | 系统日志 |
| `REQ-LOG-009` | 日志保留策略 |
| `REQ-DB-001` | 四类数据可持久化 |
| `REQ-DB-002` | 存盘读回一致（可单测） |
| `REQ-DB-003` | 记录可按 S/N 或时段查询（可追溯性） |
| `REQ-DB-004` | 选型落地 |
| `REQ-MON-001` | 设备状态总览 |
| `REQ-MON-002` | 产量计数 |
| `REQ-MON-003` | 数据可导出 |
| `REQ-UI-001` | 主框架存在 |
| `REQ-UI-002` | 参数表单可编辑并生效 |
| `REQ-UI-003` | 操作有反馈 |
| `REQ-NFR-001` | 3D 视区刷新率达标 |
| `REQ-NFR-002` | 多设备下响应不劣化到不可用 |
| `REQ-NFR-003` | 上位机崩溃后可恢复现场 |
| `REQ-NFR-004` | 代码不写平台相关 |
| `REQ-NFR-005` | 命名规范被遵守 |
| `REQ-NFR-006` | 术语对外合规 |
| `REQ-NFR-007` | 错误码可查 |
| `REQ-PRC-001` | 有"砍范围"的触发条件 |
| `REQ-ARCH-000` | "接口预留、实现可替换"成为可验证要求 |
| `REQ-PRC-002` | 每条需求必须能指出「来源」 |
| `REQ-ARCH-001` | 趋势曲线（`U9`） |
| `REQ-ARCH-002` | 故障注入（`U10`） |
| `REQ-ARCH-003` | 报警洪泛抑制（`U13`） |
| `REQ-ARCH-004` | 传感器仿真（`U8` 完整版） |
| `REQ-ARCH-005` | 轨迹基元扩到 5 种（`U6`） |
| `REQ-ARCH-006` | 开机自检（`W3`） |
| `REQ-ARCH-007` | 中断恢复（`W4`） |
| `REQ-ARCH-008` | 时间预估（`U12`） |
| `REQ-ERR-006` | 误差超阈值→告警+记录 |
| `REQ-LOG-007` | 误差日志（偏差可追溯） |
| `REQ-INF-006` | 参数可配置（全局约束） |
| `REQ-NFR-008` | 数据/UI 刷新分离 |
