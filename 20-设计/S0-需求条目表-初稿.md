# S0 需求条目表（初稿 · A+B 级展开）

> **一句话**：把 **A 级 + B 级**（≈40 项）展开成**可勾选验收判据**的需求条目；**C 级 8 项**只产出**架构冗余要求**（`REQ-ARCH-*`），不产出功能实现条目。
> **状态**：⏳ **草稿，未裁决** —— 按 S-1 §七 `R2`，**未经你逐条裁决前不构成约束**。
> **来源**：`../10-基线/S-1-立项声明.md`（v1.9）· `./S0-需求基线草稿.md`（〇 裁决记录）· `../80-资料/`（已采用条目）
> **编号**：暂用**方案 A**（`REQ-<域>-<序号>`）—— ⚠️ **编号规则你尚未裁**，若改选方案 B，本表需重编
> **规模**：**约 110 条**（推算，非实测）

---

## 一、功能域条目（`D` 系列）

### D1 · 3D 场景
> 已展开，见 `./S0-需求基线草稿.md` §三·样章 → `REQ-3D-001` ~ `REQ-3D-011`（**11 条**，不在此重复）

### D2 · 程序与轨迹

| ID | 需求条目 | 验收判据（可勾选 是/否） | 来源 |
|---|---|---|---|
| `REQ-PRG-001` | 支持新建 / 打开 / 保存程序 | 新建产生空程序；保存后可重新打开且**内容一致** | B-011（四类表） |
| `REQ-PRG-002` | 支持三种轨迹基元的增删改 | 可添加 / 删除 / 修改**点、直线、圆弧**；列表随之更新 | S-1 附录 A.1 #5 |
| `REQ-PRG-003` | **轨迹插值**（可单测） | 给定直线/圆弧参数，输出的中间点序列**可被单测断言**（点间距 ≤ 设定值） | S-1 §6（点名"轨迹插值"） |
| `REQ-PRG-004` | 程序内可调整基元顺序 | 上移 / 下移后执行顺序随之改变 | 编程常规 |
| `REQ-PRG-005` | **未保存改动有提示** | 关闭 / 切换程序时若有未保存改动 ⇒ 弹提示 | 子项 `S3` |
| `REQ-PRG-006` | **"编辑中" 与 "设备运行" 的互斥规则生效** | 某程序正在被设备执行时，编辑该程序 ⇒ 按既定规则阻止或告警 | 子项 `S4` |
| `REQ-PRG-007` | 参数修改可**撤销** | 修改工艺参数后可撤销回上一值 | 子项 `S5` |
| `REQ-PRG-008` | **程序/配方可被调用切换** | 可从列表选择程序并切换为"当前程序"；切换后显示当前程序编号 | `U1`（行业：常见必备） |

### D3 · 程序校验

| ID | 需求条目 | 验收判据（可勾选 是/否） | 来源 |
|---|---|---|---|
| `REQ-CHK-001` | 校验**坐标越界** | 存在超出行程的程序 ⇒ 报错并指出**第几条** | S-1 §2 T2 |
| `REQ-CHK-002` | 校验**空程序** | 程序无轨迹 ⇒ 报错 | S-1 §2 T2 |
| `REQ-CHK-003` | 校验结果可定位到具体基元 | 报错信息含**基元序号** | 可用性 |
| `REQ-CHK-004` | 校验可**单测** | 上述规则各有一条单测（合法 / 非法各一例） | S-1 §6（点名"校验规则"） |
| `REQ-CHK-005` | **碰撞 / 干涉检查** | 程序路径与机构（立柱 / 横梁 / 夹具）相交 ⇒ 报错并指出**相交的基元序号** | `U11`（01-资料 方向 4「提前抓碰撞/干涉」） |

### D4 · 设备管理

| ID | 需求条目 | 验收判据（可勾选 是/否） | 来源 |
|---|---|---|---|
| `REQ-DEV-001` | 设备列表可见 | 界面显示全部已配置设备，**含名称与当前状态** | S-1 §3 |
| `REQ-DEV-002` | **设备「在线 / 离线」状态可见** | 每台设备显示在线 / 离线，且状态变化时**实时更新** | 子项 `S1` |
| `REQ-DEV-003` | 状态汇聚 | 状态变化后，列表与详情**一致**（不出现两处不一致） | S-1 §3 |
| `REQ-DEV-004` | **逐台下发** | 可只对选中的一台下发程序 | S-1 §3 |
| `REQ-DEV-005` | **批量下发需二次确认** | 对多台下发 ⇒ 弹出确认；未确认不发送 | S-1 附录 B（B-015）|
| `REQ-DEV-006` | **单台故障不扩散** | 一台掉线 / 报错时，其余设备**不受影响**（可继续运行） | S-1 附录 B（B-015）|
| `REQ-DEV-007` | **每台设备持有独立参数集** | 修改 A 台的工艺参数 ⇒ B 台参数**不变** | S-1 §3 + `B1`（归属待裁）|

### D5 · 运行控制（8 项 + 安全）

| ID | 需求条目 | 验收判据（可勾选 是/否） | 来源 |
|---|---|---|---|
| `REQ-RUN-001` | 启动 / 运行 | 可下达启动指令，设备状态转为"运行" | `C1`（1/8）|
| `REQ-RUN-002` | 暂停 | 运行中可暂停；再启动可继续 | `C1`（2/8）|
| `REQ-RUN-003` | 停止 | 可停止并回主画面状态，**区别于暂停** | `C1`（3/8）|
| `REQ-RUN-004` | **急停（本地优先）** | 急停**不经通信往返**即可生效；急停后必须复位才可再启动 | `C1`（4/8）+ S-1 附录 B |
| `REQ-RUN-005` | 复位 | 解除急停后需使能才可运行 | `C1`（5/8）|
| `REQ-RUN-006` | 回零 | 可下达回零；回零完成前不允许自动运行 | `C1`（6/8）|
| `REQ-RUN-007` | 排胶 / 清洗 | 可手动出胶（排胶）用于换胶 / 清管 | `C1`（7/8）|
| `REQ-RUN-008` | 点动 / 手动移位 | 可按轴低速点动 | `C1`（8/8）|
| `REQ-RUN-009` | **掉线 / 超时即停** | 通信中断超过阈值 ⇒ 设备转为停机状态 | S-1 附录 B（B-015）|

### D6 · 工艺参数（最小 5 项）

| ID | 需求条目 | 验收判据（可勾选 是/否） | 来源 |
|---|---|---|---|
| `REQ-PRM-001` | 出胶时间可设 | 可设置并生效 | `C2`（1/5）|
| `REQ-PRM-002` | 气压可设 | 同上 | `C2`（2/5）|
| `REQ-PRM-003` | 点胶速度可设 | 同上 | `C2`（3/5）|
| `REQ-PRM-004` | 提前开胶可设 | 同上 | `C2`（4/5）|
| `REQ-PRM-005` | 延迟关胶可设 | 同上 | `C2`（5/5）|
| `REQ-PRM-006` | **点级可覆盖、缺省回退程序级** | 某点设了参数 ⇒ 用点的；未设 ⇒ **回退**用程序级 | `B-016` |

### D7 · 视觉（简化版）
> ⚠️ **具体形式待裁 `B-030`**（建议：针头对位 + Z 测高）；以下为**该建议值下的条目**，若 B-030 改动需重写

| ID | 需求条目 | 验收判据（可勾选 是/否） | 来源 |
|---|---|---|---|
| `REQ-VIS-001` | **针头对位**（模拟） | 可执行对位操作，产出**偏移量**并被记录 | `B-030`（建议值）|
| `REQ-VIS-002` | **Z 测高**（模拟） | 可执行测高，产出 **Z 高度值**并被记录 | `B-030`（建议值）|
| `REQ-VIS-003` | 对位 / 测高结果进入误差链路 | 结果作为误差来源之一上报（与 `D9` 衔接） | S-1 §7 |

### D8 · 虚拟下位机

| ID | 需求条目 | 验收判据（可勾选 是/否） | 来源 |
|---|---|---|---|
| `REQ-VMC-001` | 接收程序并可执行 | 收到下发程序后逐点推进 | `B-012`（必备）|
| `REQ-VMC-002` | **逐点耗时（时序可单测）** | 单点耗时 = 设定值 ± 容差；**可被单测断言** | S-1 §6（点名"虚拟下位机时序"）|
| `REQ-VMC-003` | 上报实时坐标 | 执行过程中持续上报坐标 | S-1 §2 T1 |
| `REQ-VMC-004` | **限位仿真**（1 个点即可） | 坐标到达限位 ⇒ 触发限位信号并停机 | `U8`（最小版）|
| `REQ-VMC-005` | 时间推进可调倍速 | 可切换 1:1 与倍速（倍速值待 `B-024`） | `B-024`（待裁）|
| `REQ-VMC-006` | **可脱离上位机独立进程运行** | 虚拟下位机作为**独立进程**存在，经通信与上位机交互 | ⚠️ 见 `B5`（边界待裁）|

### D9 · 误差模拟与归正

| ID | 需求条目 | 验收判据（可勾选 是/否） | 来源 |
|---|---|---|---|
| `REQ-ERR-001` | **固定幅度误差** | 每次执行产生固定偏移；数值 = `AS-003` 定值 | S-1 §7 |
| `REQ-ERR-002` | **随机扰动误差** | 误差在设定分布内随机波动 | S-1 §7 |
| `REQ-ERR-003` | 误差**上报**到上位机 | 上位机可收到并显示误差值 | S-1 §7 |
| `REQ-ERR-004` | **归正**（可单测） | 依据误差算出修正量并下发；**修正量可被单测断言** | S-1 §6（点名"归正"）|
| `REQ-ERR-005` | 归正后可复测 | 归正再执行 ⇒ 误差绝对值**小于归正前** | S-1 §2 T3 |

### D8b · I/O 信号（A+B 级新增 · B 检查时补漏）

| ID | 需求条目 | 验收判据（可勾选 是/否） | 来源 |
|---|---|---|---|
| `REQ-IO-001` | **I/O 信号可仿真与显示** | 虚拟下位机提供数字量输入 / 输出信号；界面可看到每点的 I/O 状态 | `U7`（01-资料 方向 4「信号映射与 I/O 仿真是关键环节」） |
| `REQ-IO-002` | I/O 点与 3D / 状态一致 | 限位信号置位时，对应的 3D 表现与设备状态**同步变化** | `U7` + `REQ-VMC-004` |

---

### D10 · 通信

| ID | 需求条目 | 验收判据（可勾选 是/否） | 来源 |
|---|---|---|---|
| `REQ-COM-001` | 通信经**抽象层** | 业务代码不直接依赖具体协议实现 | `B-010` |
| `REQ-COM-002` | 对外语义为 Modbus TCP | 功能码 / 寄存器映射可用 | `B-010` |
| `REQ-COM-003` | **连接状态可见** | 界面显示每台设备的连接状态 | 子项 `S2` |
| `REQ-COM-004` | 掉线可检测 | 断连在阈值内被检测到并触发 `REQ-RUN-009` | `B-015` |

### D11 · 基础设施（工程化 4 条）

| ID | 需求条目 | 验收判据（可勾选 是/否） | 来源 |
|---|---|---|---|
| `REQ-INF-001` | **一键构建** | CMake 配置后一条命令构建出可运行程序 | S-1 §5 工程化① |
| `REQ-INF-002` | **核心模块有单元测试** | 5 个点名模块各≥1 条单测且通过 | S-1 §5 工程化② + §6 |
| `REQ-INF-003` | **README 含架构图与一句话亮点** | 文件存在且两要素齐 | S-1 §5 工程化③ |
| `REQ-INF-004` | 源码树与命名规范可查 | 有规范文件；目录结构与之一致 | S-1 §5 工程化④ |
| `REQ-INF-005` | **数值可追溯** | 文档中每个数字都能指出来源 | 用户在 `CR-003` 立的规矩 |

### D13 · 设备状态与迁移

| ID | 需求条目 | 验收判据（可勾选 是/否） | 来源 |
|---|---|---|---|
| `REQ-STA-001` | 状态集合明确 | 至少含：离线 / 就绪 / 运行 / 暂停 / 急停 / 报警 / 回零中 / 排胶中 | `B-020`（待裁）|
| `REQ-STA-002` | **急停可从任意状态进入** | 在任状态下触发急停 ⇒ 均进入急停态 | `B-015` + `P-5` |
| `REQ-STA-003` | 非法迁移被拒绝 | 未复位时尝试启动 ⇒ 被拒绝并给出提示 | `REQ-RUN-005` |
| `REQ-STA-004` | 状态迁移**表驱动、可单测** | 迁移表可被单测逐条覆盖 | `B-017a`（层次+表驱动）|

---

## 二、S-1 未覆盖、经裁决要做的（`N` 系列）

### N1 · 报警

| ID | 需求条目 | 验收判据 | 来源 |
|---|---|---|---|
| `REQ-ALM-001` | 报警分三级（信息/警告/严重） | 三级可区分（颜色或标识） | `03-资料` 方向 4 |
| `REQ-ALM-002` | **报警需两次确认** | 第一次关声光、第二次清画面；**未确认不清除** | `03-资料` 方向 4 |
| `REQ-ALM-003` | 报警类型覆盖常见项 | 至少含：气压异常 / 轴超限 / 通信失败 | `03-资料` 方向 4 |
| `REQ-ALM-004` | 报警可触发停机 | 严重报警 ⇒ 按 `P-5` 触发停机 | S-1 附录 B |

### N2 · 日志

| ID | 需求条目 | 验收判据 | 来源 |
|---|---|---|---|
| `REQ-LOG-001` | 操作日志 | 参数修改留有记录（谁/何时/改了什么） | `03-资料` 方向 4 |
| `REQ-LOG-002` | 事件日志 | 报警与状态变化留有记录 | 同上 |
| `REQ-LOG-003` | **每条记录带时间戳** | 可看到时间戳 | `W1` |
| `REQ-LOG-004` | 可按条件查询 | 可按时段 / 类型筛选 | `03-资料` 方向 4 |

### N3 · 存储（持久化）

| ID | 需求条目 | 验收判据 | 来源 |
|---|---|---|---|
| `REQ-DB-001` | 四类数据可持久化 | 设备 / 程序 / 工艺 / 记录 **重启后仍在** | B-011 四类表 |
| `REQ-DB-002` | **存盘读回一致（可单测）** | 写入后读回，内容**完全一致**（可断言） | S-1 §6（点名"存储"）|
| `REQ-DB-003` | 记录可按 S/N 或时段查询（可追溯性） | 可按标识查到历史生产参数 | `U2` |
| `REQ-DB-004` | 选型落地 | 按 `B-021` 定案实现（SQLite + JSON 配置） | `B-021`（待裁）|

### N4 · 监控统计

| ID | 需求条目 | 验收判据 | 来源 |
|---|---|---|---|
| `REQ-MON-001` | 设备状态总览 | 一屏可见全部设备状态 | S-1 §3 |
| `REQ-MON-002` | 产量计数 | 可看到产量数，且与执行结果一致 | `03-资料` 方向 6 |
| `REQ-MON-003` | 数据可导出 | 导出为 CSV / Excel 且内容与界面一致 | `U3` + `W2` |

### N5 · 2D 界面

| ID | 需求条目 | 验收判据 | 来源 |
|---|---|---|---|
| `REQ-UI-001` | 主框架存在 | 有主窗口 + 设备区 + 3D 区 + 操作区 | 3D 之外的必要界面 |
| `REQ-UI-002` | 参数表单可编辑并生效 | 改表单后参数实际改变 | `REQ-PRM-*` 载体 |
| `REQ-UI-003` | **操作有反馈** | 每次关键操作有成功/失败提示 | 可用性 |

---

## 三、非功能条目（`F` 系列）

| ID | 需求条目 | 验收判据 | 来源 |
|---|---|---|---|
| `REQ-NFR-001` | **3D 视区刷新率达标** | 刷新率 ≥ `AS-001` 定值（**未定前判据为空**） | `F1` |
| `REQ-NFR-002` | 多设备下响应不劣化到不可用 | N 台（`AS-002`）同时在线时界面仍可操作 | `F1` |
| `REQ-NFR-003` | **上位机崩溃后可恢复现场** | 重启后能恢复到最近一次已知状态 | `W4` |
| `REQ-NFR-004` | **代码不写平台相关** | 不硬编码 Windows 路径 / 不用 Win32 API / 不让 `#ifdef` 塞业务逻辑 | `F3` |
| `REQ-NFR-005` | 命名规范被遵守 | 抽查 10 个文件，命名与规范一致 | `F4` |
| `REQ-NFR-006` | **术语对外合规** | README / 简历表述中**不出现 "L3"** | `F6` |
| `REQ-NFR-007` | **错误码可查** | 有错误码 / 故障代码对照表 | `W5` |

---

## 四、结构性条目（`X` 系列，必做）

| ID | 需求条目 | 验收判据 | 来源 |
|---|---|---|---|
| `REQ-PRC-001` | **有"砍范围"的触发条件** | 文档写明：出现何种情况时，砍哪一部分 | `X1`（=BL-006）|
| `REQ-ARCH-000` | **"接口预留、实现可替换"成为可验证要求** | 每个 C 级预留项都能指出**其扩展点在某接口 / 在某抽象层** | `X2`（**经"C 级要架构冗余"升级为必做**）|
| `REQ-PRC-002` | **每条需求必须能指出「来源」** | 本表中每行都有来源栏且非空 | `X3` + S-1 §七 `R3` |

---

## 五、C 级预留项的**架构冗余要求**（`REQ-ARCH-*`）

> **一句话**：C 级 8 项**不做实现**，但**必须留下"将来能做"的位置** —— 这是用户明确要求的「架构冗余」。

| ID | 预留项 | 架构冗余要求（可勾选 是/否） | 来源 |
|---|---|---|---|
| `REQ-ARCH-001` | 趋势曲线（`U9`） | 监控统计的数据模型**保留时间序列字段**；界面预留视图位 | `X2`（用户要求 C 级预留且留架构冗余）+ `U9` |
| `REQ-ARCH-002` | 故障注入（`U10`） | 虚拟下位机预留**故障模式注入点**（不改实现，只留钩子） | `X2` + `U10` |
| `REQ-ARCH-003` | 报警洪泛抑制（`U13`） | 报警数据结构**预留优先级 / 抑制标志**字段 | `X2` + `U13` |
| `REQ-ARCH-004` | 传感器仿真（`U8` 完整版） | 虚拟下位机预留**传感器抽象接口**（当前只实现限位） | `X2` + `U8` |
| `REQ-ARCH-005` | 轨迹基元扩到 5 种（`U6`） | 轨迹基元用**可扩展类型**表示，新增类型不需改既有分支 | `X2` + `U6` |
| `REQ-ARCH-006` | 开机自检（`W3`） | 启动流程预留**自检阶段**钩子 | `X2` + `W3` |
| `REQ-ARCH-007` | 中断恢复（`W4`） | 状态持久化预留**快照 / 恢复点**字段与接口 | `X2` + `W4` |
| `REQ-ARCH-008` | 时间预估（`U12`） | 程序模型预留**时间估算接口**（可由虚拟下位机实现） | `X2` + `U12` |

---

## 六、⚠️ 本表仍需你裁的（不裁则无法冻结）

| # | 事项 | 影响 |
|---|---|---|
| 1 | **编号规则**（A 按域前缀 / B 全局流水） | 本表暂用 A；改 B 需**重编全部 ID** |
| 2 | **`B-018` 演示脚本** | 影响 `REQ-NFR-*` 与整机验收判据 |
| 3 | **`B-030` 简化版视觉形式** | 影响 `REQ-VIS-001~003`（现按"针头对位+Z测高"写） |
| 4 | **`B-020` 状态集合** | 影响 `REQ-STA-001` |
| 5 | **`B-021` 持久化选型** | 影响 `REQ-DB-004` |
| 6 | **`B5` 虚拟下位机是否走通信** | 影响 `REQ-VMC-006`（现按"独立进程"写） |
| 7 | **`B1` 设备参数集归属** | 影响 `REQ-DEV-007` |
| 8 | **D 级 U4/U5** | 现按 AI 建议记为"不做"，**你可推翻** |

---

## 七、统计

| 组 | 条目数 |
|---|---|
| `D` 功能域 | 8+4+7+9+6+3+6+5+4+5+4 = **61**（D1 的 11 条另计 ⇒ 共 **72**） |
| `N` 补充域 | 4+4+4+3+3 = **18** |
| `F` 非功能 | **7** |
| `X` 结构 | **3** |
| `REQ-ARCH` 架构冗余 | **8** |
| **合计** | **约 108 条**（**推算**） |

> ⚠️ 其中 **`REQ-NFR-001` 的验收判据为空**（等 `AS-001`）—— 这暴露 `R3` 缺一句：
> **"判据为空不得进基线"**。建议走变更单补进 S-1 §七。
