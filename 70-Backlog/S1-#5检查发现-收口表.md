# S1 `#5 依赖图` 检查发现 · 收口表

> **一句话**：`#5` 至今跑了 **4 批检查**（方法优化 20 轮 + 双向核对 2 轮 + 循环第 1 批 3 轮 + 循环第 2 批 50 轮），
> 共报出大量条目。**结构层面的问题已全部处理完（0 新环 / 0 新禁边 / 0 阻断 D）**；
> 其余**按归属任务收口**在这里 —— **它们是"还没做到那个任务"，不是"#5 有缺陷"**。
> **性质**：**I 类（改进，不阻塞）**。**建立日期**：2026-09-21

---

## 一、`#5` 自身的硬缺口（**必须做，不是收口**）

| # | 缺口 | 依据 | 阻塞谁 |
|---|---|---|---|
| **1** | **`S1-接口清单.md`**（24 模块 × 输入/输出/错误）—— **完全未产出** | `§一` `#5` 判据的另一半 · 方法 §十 判据⑥ | 🔴 **卡 `S2`** |
| **2** | `S1-依赖图.md`（渲染图）—— 未产出 | 方法 §八 步 7 | 无（可读性） |
| **3** | `S1-模块表.md`（24 行：稳定度 + 责任）—— **未产出，但方法 §四 已引用它** | 方法 §四 = **空引用** | `#18` |
| **4** | `#5` **未纳入 `check-consistency.py` 闸门** | 三路独立实证"边数漂移 75/74/78" | 根因①复发 |
| **5** | `check-deps.py` 三处缺口：§七 源码交叉校验**只有 docstring 无桩函数** · §五 第 6 条禁边**未实现** · `MODULES` 曾硬编码 | 交付评审 + loop-2 | 中 |

## 二、按归属任务收口（**不是 `#5` 的活**）

### 归 `#6 运行时视图`（最大一坨）
| 项 | 来源 |
|---|---|
| **24 模块 × 线程归属**（仅 10 个可定、**14 个未定**，含 `core/device`） | loop2-b CT-3/CT-6 |
| 启动顺序 / 关闭逆序（栈式） | loop2-b CT-7 |
| 跨线程边清单 + 事件通道的线程安全 | loop2-b 表四 |
| 死锁分析（含 `ALLOWED_CYCLES` 豁免环的 **SPSC 机制验证**） | loop2-b CT-2 |
| 资源释放责任链（含 vtk 子资源） | loop2-b CT-8 |
| `#5` 与 `#17` 共用场景清单的交接 | loop-deps-3 |

### 归 `#8 横切机制方案`
| 项 | 来源 |
|---|---|
| `A9` 背压 —— ⚠️ **`§九` 已裁 SLICE-00 即走完整并发 ⇒ 切片 0 即生效、不可递延** | loop2-b CT-4 |
| `A10` 重试超时 × `B8` 幂等（非幂等写边清单） | loop2-b CT-5 |
| `A11` 命令插队（急停插队）—— **至今无决策、无场景覆盖** | loop-deps-3 |
| `A12` 输入验证（三入口只有"配置文件"有校验落点） | loop2-d F39 |
| 报警升级（ISA 18.2 第四要素） | check-strategy-d |
| `comm`/`data` 层**非断连类错误**无上行边（错误静默丢失） | loop2-d F33-1 |
| 机制级 ADR（决策 9/11/17 只给 pattern 名不给机制） | check-strategy-e |

### 归 `#18 构建组织`
| 项 | 来源 |
|---|---|
| **增量编译影响面**（实测：改 `data/log` 头牵连 **13** 模块；`core/device`/`statemachine`/`store`/`concurrency` 各 **11**） | loop2-a NEW-2 |
| PIMPL / 前向声明（高扇入核心优先） | loop2-a NEW-2 |
| 跨进程协议无共享模块（`comm/interface` ↔ `apps/virtual-device` 的线格式无归属） | loop2-a NEW-4 |
| `E2 ≡ apps/virtual-device` **同物异名**（切 target 需人工映射） | loop2-a NEW-1 |
| 构建缓存（ccache / binary cache）缺失 · 构建时间预算 | loop2-a NEW-6 |
| `#5 → #18` 交接是**半截**（边表是必要非充分，还缺模块表 + S4 源码） | loop2-a NEW-5 |

### 归 `#14` / `#15`
| 项 | 来源 |
|---|---|
| 跨进程 IPC + Modbus 延迟计入 50ms 预算 | dep-audit-forward |
| 8 台/50ms/100ms 常量绑定 `AS-001`/`AS-002` | check-strategy-d |

### 归 `#7 部署视图`
| 项 | 来源 |
|---|---|
| 部署与升级数据兼容（配置 / SQLite 位置 / 旧数据迁移） | check-strategy-e |

### 归 `#13 ADR`
| 项 | 来源 |
|---|---|
| 决策 12（异步启动）依据不硬，补冻结背书或降为建议 | check-strategy-f |

## 三、`#5` 表达力缺口（**要不要改表结构，待裁**）

| 项 | 来源 | 说明 |
|---|---|---|
| A 层边不区分**同步调用 vs 异步投递（enqueue）** | loop2-b **CT-1** | 已用「【投递 enqueue】」标注两条环边；**是否上升为正式字段待裁** |
| 边表缺「**技术债 / 例外**」字段 | loop2-c N4 | 破例边与普通边长得一样，易误读 |

## 四、收敛结论

| 批次 | 新根因 | 备注 |
|---|---|---|
| 方法优化 20 轮 | ~9 | — |
| 双向核对 2 轮 | 2 | 已处理 |
| 循环第 1 批（3 轮） | **1** | B 层约定，已修 |
| **循环第 2 批（50 轮）** | **0**（`loop2-e` 一路 10 轮） | 其余各路的"新根因"多为**已知 6 根因的重复投影** |

> 🔑 **`loop2-e` 拿旧项目 GlueTwin 的 6 个根因逐条对照 ⇒ 6 个在 `#5` 全部重犯，0 个新根因。**
> ⇒ **结构层面收敛成立。`#5` 现在缺的不是检查，是上面第一节那 5 个交付物。**
