# S1 §17.2 检查发现 · 收口表

> **一句话**：30 轮对抗性检查（`§17.2` 决策清单）共查出 **30 条候选**；**1 条新 D 已当场修**、**2 条计数/脚注错已当场修**，
> 其余 **25 条按"归属任务"分组**存在这里 —— **它们不是"遗漏"，是"还没做到那个任务"**。
> **性质**：**I 类（改进，不阻塞）**。依 `../00-流程/03-发现分类与变更控制.md`：**不进本轮**；到对应任务时取用。
> **建立日期**：2026-09-20 ｜ **来源**：`../20-设计/S1-§17.2-第21至30 / 31至40 / 41至50轮检查.md` 三份报告

---

## 一、当场已修（3 条，不在本表）

| # | 问题 | 处置 |
|---|---|---|
| D-4 | `§14.6` 四域映射**漏了 `core/alarm` / `core/stats`** ⇒ 与 `§17.5` 业务层枚举不一致 | ✅ 已补（24 模块全部落位） |
| — | `§一` 任务表 #8 写「横切 **13 项**」，`A4` 收口后未回改 | ✅ 已改 **12**（含 `§1.0.1` / `§12.3` / 合计行） |
| — | `§17.1` 主干⑤ 脚注仍写「记在 `A4`」 | ✅ 已改「已收口为**决策 17**」 |

---

## 二、待收口（25 条，按归属任务分组）

### 归 `#8 横切机制方案`（**16 条 —— 最大一坨**）

> 根因：**`§17.2` 全是"选什么"，"怎么转起来"那批还没做**。`#8` 本来就是干这个的。

| # | 内容 | 来自 |
|---|---|---|
| 1 | 决策 9（每设备一线程 + SPSC）的**并发正确性单测路径** | E-N1 |
| 2 | 加轨迹基元时的**跨模块一致性**（`core/program` ↔ `app/scene3d` 锁步） | E-N2 |
| 3 | 加报警类型时的**扩展治理**（跨 ≥3 模块） | E-N3 |
| 4 | **单台故障的"崩溃隔离"** —— 每设备一线程**隔离不了段错误**（`REQ-DEV-006` 的崩溃场景） | E-N4 = F-N3 |
| 5 | **急停带外触发 / 看门狗** —— UI 阻塞时也要发得出 | E-N5 |
| 6 | **配置原子写** —— JSON 半写会损坏文件 | E-N7 |
| 7 | **`data/store` 单写者串行化** + SQLite 连接架构 | E-N8 = F-N6 |
| 8 | **诊断追踪** —— `infra/telemetry` 只给指标，不给因果链 | E-N9 |
| 9 | **报警升级（escalation）** —— ISA 18.2 第四要素，`R5` 列了范围但无决策 | D-N5 |
| 10 | **日志保留量化值** | F-N8 |
| 11 | **线程崩溃隔离 / 虚拟下位机进程生命周期** | F-N3 / F-N4 |
| 12 | **事件总线成本显性化** —— 决策 14① / 16 / 17 三重依赖它 | E-N11 |
| 13 | **机制级 ADR** —— 决策 9 / 11 / 17 只给 pattern 名，不给机制细节 | E-N12 |
| 14 | **操作缺口**：班次交接 · 下发中取消 · 虚拟下位机未起时怎么操作 | F-N7 |
| 15 | `§17.2` 的**排除项枚举**漏了 `§十二` A5 / B6 / C6 / C7 | D-N2 |
| 16 | **决策 16 的分级基线 = `AS-005`** —— 须在 S1 冻结前定稿 | D-N6 |

### 归 `#14 性能预算分配`（**2 条**）

| # | 内容 | 来自 |
|---|---|---|
| 17 | **跨进程 IPC + Modbus 延迟**要计入 50ms 预算（独立进程虚拟下位机引入的） | E-N6 |
| 18 | **`8 台` / `50ms` / `100ms` 常量须绑定 `AS-001` / `AS-002`**，不能裸写 | D-N7 |

### 归 `#15 NFR → 达成手段表`（**1 条**）

| # | 内容 | 来自 |
|---|---|---|
| 19 | **决策 ↔ 演示脚本 `B-018` 映射** —— 约 9 条决策"肉眼不可见"，而"可演示"是 `S-1 §6` 判据 | F-N10 |

### 归 `#5 构建块视图`（**1 条**）

| # | 内容 | 来自 |
|---|---|---|
| 20 | **层间依赖方向规则**（正是 `#5` 的内容，已在队列里） | F-N5 |

### 归 `#18 构建组织`（**1 条**）

| # | 内容 | 来自 |
|---|---|---|
| 21 | **vcpkg 离线构建 / vendor 锁定**风险 | F-N9 |

### 归 `#7 部署视图`（**1 条**）

| # | 内容 | 来自 |
|---|---|---|
| 22 | **部署与升级数据兼容**（配置 / SQLite 磁盘位置、旧数据迁移） | E-N10 |

### 归 `#13 架构决策记录 ADR`（**1 条**）

| # | 内容 | 来自 |
|---|---|---|
| 23 | **决策 12（异步启动）依据不硬** —— 依据"轮 64"是 S1 内部推导，无冻结条目背书 ⇒ 补背书或**降为建议** | F-N1 |

### 归「待裁决队列维护」（**1 条 · 建议当场修**）

| # | 内容 | 来自 |
|---|---|---|
| 24 | `../30-待裁决/待裁决队列.md` **汇总行过期**：仍写"未决 9 条：B-018~B-025"，而其条目已标 ✅ 已裁 | F-N2 |

### 可选（**1 条**）

| # | 内容 | 来自 |
|---|---|---|
| 25 | **ISA-95 层级标注**（对外表述用，非必须） | D-N8 |

---

## 三、收口原则

1. **本表不进本轮** —— 全是 I 类，不阻塞 S1 冻结。
2. **到对应任务时逐条取用** —— 例如做到 `#8` 时，第 1~16 条就是现成的输入清单。
3. **做完一批删一批**，不留悬空引用。
4. ⚠️ **别把它们塞回 `§十二`** —— 那会让"待设计"从 12 项涨到近 40 项，收不了口。
