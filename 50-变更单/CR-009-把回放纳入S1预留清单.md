# CR-009 · 把「历史数据回放」写进 S-1 §四 预留清单（**9 项 → 10 项**）

> **一句话**：回放此前**完全没考虑过**（三层全空白）；用户 2026-09-21 裁定「**只预留接口**」⇒ 按 C3 单一权威源，必须写进 `S-1 §四` 的**预留清单**（它是这两份清单的唯一权威源）。
> **状态**：🟡 **待用户裁决** ｜ **提出日期**：2026-09-21 ｜ **提出人**：AI（依用户裁定提单）
> **裁决依据（用户原话）**：
> ① 「**回放给个预留接口**，而且**回放应该是一个比较独立的功能**」
> ② 「检查一下你的推荐是否合理**如果合理可以进行你的推荐内容**」（= 授权执行不涉冻结层的部分）

---

## 一、为什么改

| # | 依据 |
|---|---|
| 1 | **回放从未被考虑过**：全库搜「回放 / replay / 重放 / 历史重现」，仅 1 处命中且是**比喻**；`S-1 §四` 不做清单 4 项、预留清单 9 项、`S0` 127 条、`S1` 决策 17 条、关注点 32 项 —— **全都没有** |
| 2 | **用户已裁定 = B（只预留接口）** ⇒ 落在 `S-1 §四` 的「⏳ 预留（有接口、无实现）」一类，与原则 `P-3`/`P-4` 完全对应 |
| 3 | **S-1 §四 是唯一权威源** ⇒ 不写进去，回放就没有**追溯链**（面试被问"功能边界在哪"答不出） |
| 4 | 架构师路已确认 **`P-3` 原文把「文件导入」点名列进"只留接口"** ⇒ 回放的"导入数据"正落在 `P-3` 上 |

## 二、改什么（**一处字段：预留项数**）

| # | 文件 | 现状 | 目标 |
|---|---|---|---|
| 1 | `../10-基线/S-1-立项声明.md` **§四** 预留清单 | **9 项**（① ~ ⑨） | **10 项**，新增 **⑩ 历史数据回放** ⇒ 版本 **v1.9 → v1.10** |
| 2 | 同文件 **§8 槽位行** | 「**预留 9 项**见 §四」 | 「**预留 10 项**见 §四」 |
| 3 | 同文件 **§六 #3** | 「`P-3` 重活只预留（**8 项**走预留）」 | **改为与 §四 一致**（10 项）；⚠️ 该处现与 §四 的 9 项**本来就不一致**，属既有缺陷一并修 |
| 4 | `../00-流程/check-consistency.py` | 圆括号枚举正则写死 **`[①②③④⑤⑥⑦⑧⑨]`**（2 处） | 补 **⑩** —— 实测：对 `①②…⑩` 该正则只数出 **9** ⇒ 不改则 §四 声明 10 / 实际数出 9 ⇒ **FAIL** |

> 第 4 项是第 1 项的**必要配套**（先例：`CR-007` 一单内既改冻结物又立闸门）—— 不修脚本，改完当场红。

## 三、影响哪些已冻条目

**不推翻任何条目** —— 仅**新增**一个预留项，并把 3 处**互相矛盾的项数**统一到 10。

## 四、代价 / 复跑范围

- 需重跑：`check-consistency.py`（S-1 层闸门）
- **不需要**重跑 S0 层闸门（本次不碰 S0 条目）

## 五、已先行执行的部分（**不涉冻结层，用户已授权**）

| # | 已做 | 文件 |
|---|---|---|
| 1 | `check-deps.py` 的 `MODULES` 加入 `'app/replay'`（24 → **25**）；并把报错文案里写死的"24 模块"改为**动态**（`len(MODULES)`），防计数再漂移 | `00-流程/check-deps.py` |
| 2 | 依赖边表新增 **9 条回放边**（A 层 7 条 + B 层 2 条）⇒ 权威边数 **78 → 87**（A70/B8/D9），节点 **31 → 32** | `20-设计/S1-依赖边表.txt` |
| 3 | 新增假设 `AS-006`（回放的时间戳口径，`G-7`），最晚验证时点 = **S2 接口基线冻结前** | `40-假设台账/假设台账.md` |

> ⚠️ 这三项均**不在冻结层**（`00-流程/` 用户已授权、`S1` 未冻结、台账 AI 可追加）。

## 六、复跑验证（改前）

```
python 00-流程/check-deps.py        ⇒ 退出码 0（87 条边 A70/B8/D9 · 32 节点）
python 00-流程/check-consistency.py ⇒ 退出码 0（23 项检查，失败 0）
```

## 七、🔴 独立发现（本单附带登记，不属本单改动）

`AS-005`（假设台账）写「**4 档**：L0/L1/L2/L3」，而 `REQ-3D-010`（S0）写「**3 档**：L1/L2/L3…**无更低档**（用户裁：**不要 L0**）」—— **两处都是 2026-09-20 已裁，结论不同**（台账未同步）。
⇒ 已另立 **`CR-010`** 之外的独立跟进项，见 `变更单索引` 备注；**本单不处理**（避免一单多改）。
