# CR-S0-001 · 补 `B-023` 构建环境 + 定义 SLICE-00（第一片）

> **一句话**：S0 冻结后的第一张变更单 —— 补两个**冻结时遗留的漏项**：① 构建环境（`B-023`）② **SLICE-00 从未定义**。
> **状态**：✅ **已批准并执行** ｜ **提出日期**：2026-09-20 ｜ **裁决依据**：用户裁「`B-023` = A（全用 vcpkg）」「SLICE-00 = A」
> **S0 版本**：v1.0 → **v1.1**

---

## 一、为什么这两项是"漏项"而非"新需求"

| 项 | 定性 |
|---|---|
| **`B-023` 构建环境** | 它在队列里**本来就存在**，最晚时点是 **S1 前**。S0 冻结时未裁 ⇒ **不是新需求，是补裁** |
| **SLICE-00** | S-1 附录 B **已定切片方式**（垂直切片 + G1/G2），但**从没定义"第一片是什么"** ⇒ **S0 遗漏**（S0 只定义了 S5 最终验收） |

## 二、变更 1 · `B-023` 定值 = **A（全用 vcpkg）**

### 环境实测（2026-09-20）

| 组件 | 版本 | 位置 |
|---|---|---|
| 官方 Qt | **6.10.3** | `C:\Qt\6.10.3\msvc2022_64` |
| **vcpkg Qt** | **6.11.1** | `D:\vcpkg-2026.04.27\vcpkg-2026.04.27\installed\x64-windows` |
| **vcpkg VTK** | **9.3.20231030** | 同上 |

🔴 **关键证据**：vcpkg 的 VTK **编译时链接了它自带的 Qt 6.11.1**（`vtkGUISupportQt-9.3.lib` / `vtkRenderingQt-9.3.lib`）⇒ **不能改用官方 Qt 6.10.3**（ABI 不保证兼容）。

⇒ **裁定：全用 vcpkg 那一套**（Qt 6.11.1 + VTK 9.3），避免两套 Qt 混用。

### 落地为 `REQ-INF-008`（见 S0 v1.1）

## 三、变更 2 · 定义 **SLICE-00**（第一片）

### 问题

`B-018`（多机 10 步）是 **S5 最终验收**——但它**不是"第一片"**。
10 步涉及：编程序 / 校验 / 批量下发 / 多实例 3D / 误差 / 归正 / 告警 / 复测 / 导出 ⇒ **等于整个项目**，远超一片规模（G1 ≤5 文件 / G2 ≤400 行）。

### 裁定：SLICE-00 = A（单台最小闭环 + **环境验证**）

| 步 | 内容 |
|---|---|
| **第 0 步** | **环境验证**：最小 CMake 工程 → Qt 窗口 → 内嵌 VTK 显示一个球 → **编译运行成功** |
| 第 1~5 步 | 编程序 → 校验 → **单台**下发 → 虚拟下位机执行 → **3D 里机构动起来** |

### 落地为 `REQ-PRC-003`（见 S0 v1.1）

## 四、变更 3 · 同步 5 项队列状态

| 项 | 实际状态 | 队列原状 |
|---|---|---|
| `B-025` 源码树与命名 | 已在 S0（`REQ-INF-004`）+ S-1 | 未同步 |
| 通信协议语义 | 已在 S-1 附录 B「Modbus TCP」+ `REQ-COM-002` | 未同步 |
| 多设备 3D 呈现 | 已在 S-1 附录 B + `REQ-3D-009/010` | 未同步 |
| Y 轴归属 | 已在 S-1 + `REQ-3D-002` | 未同步 |
| `B-023` 构建环境 | 本单裁为 A | 未同步 |

## 五、执行结果

- [x] 新增 `REQ-INF-008`（构建环境：全用 vcpkg）
- [x] 新增 `REQ-PRC-003`（SLICE-00 定义）
- [x] 速查索引同步（124 → **126**）
- [x] 队列 5 项标已裁
- [x] `S0-变更单索引` 登记本单（0 → 1 张）
- [x] S0 版本 **v1.0 → v1.1**
- [x] 闸门复跑（见下）

## 六、复跑验证

```
脚本闸门：见本轮执行输出
```

## ⚠️ 本单暴露的一个流程问题

**S0 冻结时，"冻结四件套"齐了，但"下游前置项"没查。**
⇒ 建议：**每层冻结前，加一项检查——"下游各层的时点在本层之前的前置项，是否都已裁"**。
（本次正是靠"验收者视角"复查才发现的。）
