# CR-012 · 同步 `S-1 §三` 的 `AS-005` 行（精度档数 → **3 档**）

> **一句话**：用户 2026-09-21 裁定**方案 A**（精度档数**以 `REQ-3D-010` 为准 = 3 档，无 L0**）⇒ `40-假设台账` 的 `AS-005` **已改**；但 `S-1 §三` 的同名假设行仍写着旧描述「高低精度**差异未定义**」，**两处表述已分叉**，需同步。
> **状态**：🟡 **待用户裁决** ｜ **提出日期**：2026-09-21 ｜ **提出人**：AI

---

## 一、为什么改

| # | 依据 |
|---|---|
| 1 | **用户 2026-09-21 裁定**：精度档数走**方案 A** —— 以 `REQ-3D-010` 为准 = **3 档（L1/L2/L3），无 L0**（选项 A 的连带动作 = 台账只留值 + 引用，删掉档位副本） |
| 2 | **台账侧已执行**：`AS-005` 已改为「以 `REQ-3D-010` 为准 = 3 档，无 L0」并标注原"4 档含 L0"**作废** |
| 3 | 🔴 **`S-1 §三` 的行仍是旧描述**：`附录 A.1 #8「精度分级」的高低精度**差异未定义**（详情实例高精度、其余低精度缩略 —— 具体差在哪里未定）` ⇒ **该描述在 2026-09-20 被裁时就已过时**（那时已定为 4 档、现为 3 档） |
| 4 | 依 `00-流程/02-文件结构规范.md` **C3**（同类信息只有一个权威源）：档位定义**只在 `REQ-3D-010` 维护**，台账与 S-1 都**只引用** |

## 二、改什么（**一处改动：`AS-005` 行的表述**）

| # | 文件 | 改动 |
|---|---|---|
| 1 | `../10-基线/S-1-立项声明.md` **§三** `AS-005` 行 | 假设内容列 → 改为指向 `REQ-3D-010`（3 档、无 L0）；**「最晚验证时点」保持 `S1 架构基线冻结前` 不变**（`check-consistency.py` 逐字比对两处时点） |
| 2 | 版本 v1.11 → **v1.12** | — |

## 三、影响哪些已冻条目

**不推翻任何条目** —— 仅把已过时的表述对齐到已裁结论。

## 四、代价 / 复跑范围

- 需重跑 `check-consistency.py`（**假设集合一致性 + 时点逐条一致性**——本单**不改集合、不改时点**，故应保持绿）

## 五、⚠️ 为什么没并入 `CR-011`

`CR-011` 是"**新增** `AS-006`"；本单是"**修改** `AS-005` 的表述" ⇒ 依 `变更单索引` **纪律 3**（一张单只对应一处改动）**必须拆单**。
