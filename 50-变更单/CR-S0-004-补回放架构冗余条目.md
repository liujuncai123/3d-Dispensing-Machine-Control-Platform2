# CR-S0-004 · S0 补 `REQ-ARCH-009`（**历史数据回放的接口预留**）

> **一句话**：`S1` 要给回放留接口，但 **S0 里没有任何条目可以回指** ⇒ 依 `S-1 §七 R1`（正向范围由 S0 提），必须在 S0 的 `REQ-ARCH-*` 组补一条，否则 S1 的回放接口**无源**。
> **状态**：🟡 **待用户裁决** ｜ **提出日期**：2026-09-21 ｜ **提出人**：AI
> **裁决依据（用户原话）**：「**回放给个预留接口**，而且**回放应该是一个比较独立的功能**」+「检查一下你的推荐是否合理**如果合理可以进行你的推荐内容**」

---

## 一、为什么改

| # | 依据 |
|---|---|
| 1 | **回放在 S0 是空白**：全库搜「回放 / replay / 重放 / 历史重现」仅 1 处命中且是**比喻** ⇒ S0 的 127 条里**没有任何一条**与回放有关 |
| 2 | **S-1 §七 R1**：S0 是**正面范围**的提出层 ⇒ S1 的接口设计必须能回指到 S0 条目 |
| 3 | **同型缺陷有先例**：`D-10` / `D-11`（S1 有设计但 S0 无源）曾被 **`CR-005`** 处理掉 ⇒ 不补就是重犯 |
| 4 | **`REQ-ARCH-000` 已立原则**：「**"接口预留、实现可替换"成为可验证要求**」⇒ 回放正落在这条原则下，应进同一组 |
| 5 | **编号可用（实测）**：S0 现有 `REQ-ARCH-000`（原则）+ `REQ-ARCH-001` ~ `REQ-ARCH-008`（8 条预留）⇒ **`REQ-ARCH-009` 是空号** |

## 二、改什么（**一处改动：新增 1 条 S0 条目**）

| # | 改动 |
|---|---|
| 1 | `../10-基线/S0-需求基线.md` 的 `REQ-ARCH-*` 组**新增** `REQ-ARCH-009`（**净增 1 条**）；版本 **v1.3 → v1.4** |

> 🔴 **2026-09-21 自审修正**：本单原写「条目总数 **127 → 128**」—— 该 **127 已撤回**，理由见 **§七**（S0 自身的条目数**不自洽**，写绝对数会把这个缺陷复制进变更单）。⇒ 本单**只声明净增 1 条**，不引绝对总数。

**拟写内容（写法对齐同组 `REQ-ARCH-006`）**：

| 字段 | 内容 |
|---|---|
| **描述** | 历史数据回放的接口预留：以独立模块承载"按时间轴重演历史时段"，**只留接口、不做实现** |
| **验收判据** | ① 存在回放的 `I*` 抽象接口（**数据源** / **按时间轴逐帧取数** / **时钟** / **播放**）<br>② 有**一个轻量实现**（空数据源 + 只走状态的播放器）<br>③ **换实现不改调用方**<br>④ **不实现**真正的数据回放 |
| **来源** | 用户 2026-09-21 裁定「回放给个预留接口，而且回放应该是一个比较独立的功能」 |
| **优先级** | **P2**（与同组 8 条一致） |
| **可测性** | **人工**（查代码：接口存在 + 轻量实现存在） |
| **依赖** | `REQ-ARCH-000`（接口预留成为可验证要求） |

> ⚠️ **不改** `S-1 §四` 预留清单 —— 那是 **`CR-009`** 的事（一张单只对应一处改动，见 `变更单索引` 纪律 3）。

## 三、影响哪些已冻条目

**不推翻任何条目** —— 仅**新增** 1 条（净增 1 条；**不引绝对总数**，理由见 §七）。

## 七、🔴 自审发现：`S0` 的**条目数不自洽**（不属本单改动，另立跟进）

**2026-09-21 自审时实测**：`S0-需求基线.md` 自己对「条目数」给了 **4 个不同的数**，且**都不等于实测值**：

| 出处 | 写的数 |
|---|---|
| **文件页眉** `> **条目数**：` | **127**（括号内却写「深度卡片 **54** + 标准条目 **16**」= **70**） |
| **冻结四件套**「已冻条目清单」行 | **124**（且声称"与速查索引一致"） |
| `# 条目展开（全部 **111** 条）` 标题 | **111** |
| **实测**：全文出现的**唯一 `REQ-*` 编号** | **114** |

⇒ 🔴 **三点影响**：
1. **`S0` 已冻结**（v1.3），而"已冻条目清单"是**冻结四件套**之一 ⇒ **冻结依据本身不自洽**；
2. 「与速查索引一致」这句**未被验证**（速查索引 §一 只有 52 个 `| REQ-` 行，格式/口径与页眉不同）；
3. `00-流程/check-consistency.py` **根本不查 `S0` 的条目数**（实测：脚本内仅一处提到 `S0`，且是"仅 S-1 这类含槽位表的层检查；S0/S1 无槽位概念"）⇒ **无人拦得住这个漂移**。

> ⚠️ **诚实声明**：114 是"全文出现过的唯一编号数"，**不等于**"定义条目数"——因为 **S0 从未定义过"条目"的计数口径**（这正是问题本身）。
> ⇒ 本单**不处理**此项（一张单只对应一处改动）；建议**另立一张 `CR-S0-005`** 或先登记 `70-Backlog/`。

## 四、代价 / 复跑范围

- 需重跑：`check-consistency.py`（S0 层闸门，含"条目总数"一致性检查）
- ⚠️ **连带**：S0 冻结四件套里写的条目数需同步（另有独立跟进项：现有一处写 **124 条**、与页眉 **127** 不符，属**既存缺陷**，不在本单范围）

## 五、顺带修正（索引维护，**非** S0 条目改动）

`50-变更单/S0-变更单索引.md` 版头现写「当前 S0 版本 **v1.2** · 已登记变更单 **2**」，
但 `CR-S0-003` 已批准执行、**v1.2 → v1.3**，且它的行**游离在主表之外**（落在"备注"段之后）。
⇒ 本单登记时一并修正为 **v1.3 / 3 张**，并把 `CR-S0-003` 行收进主表。

## 六、复跑验证（改前）

```bash
python 00-流程/check-consistency.py   # ⇒ 退出码 0（23 项检查，失败 0）
```
