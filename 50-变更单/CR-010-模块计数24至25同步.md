# CR-010 · 模块计数 **24 → 25**（把 `app/replay` 计入）

> **一句话**：`app/replay` 是**真实模块**（架构师已给出它的文件清单）；按 `S1 §十六` 已定口径「**数全部路径，含独立部署单元**」，模块计数必须从 **24 → 25**。
> **状态**：🟡 **待用户裁决**（24 是用户 2026-09-20 **亲口裁决**的数，改动须用户点头）
> **提出日期**：2026-09-21 ｜ **提出人**：AI
> **裁决依据（用户原话）**：「检查一下你的推荐是否合理**如果合理可以进行你的推荐内容**」

---

## 一、为什么改

| # | 依据 |
|---|---|
| 1 | `S1 §十六` 明文：**模块总数 = 24，口径 = 数全部路径（含独立部署单元）**；且 **「🔑 本节是模块计数的唯一权威源」** |
| 2 | 同节立了一条**硬纪律**：「**24 是唯一能由命令复现的数**」（`22` 用任何命令都产不出来） |
| 3 | 该命令**数的是 `S1` 正文里出现的路径**，不是代码仓库 → 只要 `app/replay` 进了 S1 正文，命令输出就从 **24 变 25**（**实测**：当前输出 24，且 `app/replay` 在 S1 正文出现 **0** 次） |
| 4 | `app/replay` **不是空壳**：架构师路已给出它的真实文件清单（6 个接口头 + 2 个轻量实现 + `CMakeLists.txt` + 1 个测试） ⇒ **代码里会有这个目录** |
| 5 | 不加 ⇒ **模块表与代码对不上**（§十六 自己写了"已同步到 §一/§十一/§18/§17.5/6 份报告"，那一整套都会与代码脱节） |

## 二、改什么（**一处字段：模块计数**）

| # | 文件 / 位置 | 现状 | 目标 |
|---|---|---|---|
| 1 | `../20-设计/S1-架构基线草稿.md` **§16.1 分层枚举** | UI 层 **4** 项（`app/main_window`·`app/scene3d`·`app/dashboard`·`app/dialogs`） | UI 层 **5** 项（+ `app/replay`）；小计 23 → **24**；合计 24 → **25** |
| 2 | 同文件 **§十六 标题与正文** | 「模块计数 = **24**」 | **25**（并保留"唯一权威源 + 命令可复现"的写法） |
| 3 | 同文件 **§一 · §十一 `A3` · §18 · §17.5** 及全文其它提到 24 处 | 24 | 25 |
| 4 | **6 份模块划分检查报告** | 顶部已挂"22 作废"声明 | 同步 25（或补挂"24 → 25"更新声明） |

## 三、影响哪些已冻条目

- **不推翻任何 S-1 / S0 条目**（模块计数属 **S1** 内部结论，S1 未冻结）
- ⚠️ 但 **24 是用户 2026-09-20 的已裁结论** ⇒ 本单**必须用户批**

## 四、代价 / 复跑范围

- 需重跑：`check-deps.py`（模块白名单已含 `app/replay`，已绿）· `check-consistency.py`（若它校验模块数）
- **多文件同步**是本项目历史高发缺陷（"改完不同步引用点"）⇒ 建议**一次性做全**，不留尾巴

## 五、复跑验证（改前，实测）

```bash
# §十六 自己给的可复现命令，当前输出 24
grep -ohE '\b(apps|app|core|comm|data|infra)/[a-z0-9_-]+' 20-设计/S1-架构基线草稿.md | sort -u | wc -l
# => 24

# app/replay 目前在 S1 正文出现次数
grep -c "app/replay" 20-设计/S1-架构基线草稿.md
# => 0    （⇒ 加入后命令输出应为 25）
```
