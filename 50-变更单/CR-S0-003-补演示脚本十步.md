# CR-S0-003 · 补 `B-018` 演示脚本「多机 10 步」的**内容**

> **一句话**：`B-018` 早已裁为「**方案 C（多机 10 步）**」，但**这 10 步的内容从未落文** —— 本单把内容补上。
> **状态**：✅ **已批准并执行** ｜ **提出日期**：2026-09-21 ｜ **裁决依据**：用户裁 `A` = 先补 `B-018` 十步
> **S0 版本**：v1.2 → **v1.3**

---

## 一、为什么补（**这是第四例「决策已裁、内容未落」**）

**问题**：`B-018` 的裁决结论到处都是（`待裁决队列.md` · `决策记录.md` · `假设台账 AS-004`），
**但"10 步"到底是哪 10 步，全库找不到。**

| 查过的地方 | 结果 |
|---|---|
| `../30-待裁决/裁决包-S1-S2预告项.md` 的 `B-018` 节 | 只有 **3 个选项**（①最小闭环 7 步 / ②+多设备 / ③+报警日志）—— **没有"方案 C"** |
| `../30-待裁决/待裁决队列.md` · `../60-决策记录/决策记录.md` | 只有结论「= 方案 C（多机 10 步）」 |
| `../40-假设台账/假设台账.md` `AS-004` | 只有「随 `B-018` = 方案 C（多机 10 步）」 |
| 全库仅有两处旁证 | ① `S0` 一句概括「10 步含**批量下发 / 多实例 / 归正 / 告警 / 复测**」<br>② `REQ-ERR-004`「该对比可作为演示脚本的一步（**`B-018` 闭环第 ⑦ 步**）」 |

⚠️ **而且 `AS-004` 已逾期**：其「最晚验证时点 = **S0 冻结前**」，而 **S0 已冻结**。
依 `../10-基线/S-1-立项声明.md` §三：「**到期未验证 ⇒ 自动升级为待裁决的阻塞条目，不得无声跳过**」。
⇒ 本单即该逾期项的兑现。

> 🔁 **同族前例 3 例**：`B-021`（持久化裁决原文不在 `决策记录`）· `B-025`（同）· 模块数 `24/22`（计数未同步）。
> **根因推测**：**裁决发生在对话里，落库只落了"结论"，没落"内容"** ⇒ 建议后续每条 `B-xx` 裁决必须附内容载体（已记入 Backlog）。

## 二、补的内容（**用户已圈定**）

| 步 | 动作 | 覆盖需求 |
|---|---|---|
| **1** | 启动 → 3D 场景加载龙门机构 | `REQ-3D-001` |
| **2** | 连多台虚拟下位机 → 设备列表显示在线 | `REQ-DEV-001/002` · `REQ-COM-003` |
| **3** | 新建 / 编辑程序（三种轨迹基元）→ 3D 渲染编程轨迹 | `REQ-PRG-001/002` · `REQ-3D-006` |
| **4** | 校验（坐标越界 / 空程序 / 碰撞干涉） | `REQ-CHK-001~005` |
| **5** | 作业前点检确认 → **多台批量下发 + 二次确认** | `REQ-RUN-010` · `REQ-DEV-005` |
| **6** | 虚拟下位机逐点执行 → 3D 实时映射 + 出胶反馈 + 进度 | `REQ-3D-003/005/007` |
| **7** | **误差上报 → 归正下发 → 复测对比**（误差均值下降） | `REQ-ERR-003/004/005` |
| **8** | 触发**严重报警** → 停机 + 声光 + 两次确认 | `REQ-ALM-001~004` |
| **9** | 日志与统计查询（执行 / 误差 / 产量） | `REQ-LOG-*` · `REQ-MON-002` |
| **10** | **多设备同场景呈现**（多实例 + 精度分级 L0~L3）+ 切换详情 | `REQ-3D-009/010` |

**✅ 与 `S0` 那句概括逐项对上**：批量下发（步 5）· 多实例（步 10）· 归正（步 7）· 告警（步 8）· 复测（步 7）。
**✅ 与 `REQ-ERR-004` 旁证对上**：复测对比 = **第 7 步**。

## 三、执行结果

- [x] 本单落文（十步内容）
- [x] `../40-假设台账/假设台账.md` `AS-004` 状态 ⏳ → **✅ 已验证**（附结论 + 逾期说明）
- [x] `S0-变更单索引` 登记（2 → **3 张**）
- [x] `../10-基线/S0-需求基线.md` 的 `B-018` 行加本单指针；版本 **v1.2 → v1.3**
- [x] 派生 S1 用途：`../20-设计/S1-关键场景清单.md`（`#5` 双向核对 + `#17` 走查共用）
