# CR-011 · 假设台账新增 `AS-006`（回放的时间戳口径）**+ S-1 §三 同步**

> **一句话**：回放的**时间戳口径**（分辨率 / 单位 / 单调性 / 跨设备时钟是否统一）在 S0/S-1 里**都没有承诺**，属 **Q 类未知** ⇒ 应进假设台账；但台账与 `S-1 §三` 的假设集合**必须一致**（`check-consistency.py` 逐条校验）⇒ 两处必须**同一次**改。
> **状态**：🟡 **待用户裁决** ｜ **提出日期**：2026-09-21 ｜ **提出人**：AI
> **裁决依据（用户原话）**：「检查一下你的推荐是否合理**如果合理可以进行你的推荐内容**」

---

## 一、为什么改

| # | 依据 |
|---|---|
| 1 | 回放的**时间轴**要落地，必须有统一的时基；但 `REQ-LOG-003` 只承诺「① 有**可读**时间戳 ② **排序正确**」—— **未承诺**分辨率 / 单位 / 单调性 / 跨设备时钟统一 |
| 2 | 架构师路把它列为回放缺口 **`G-7`**，并建议**转假设台账**（原话："需转假设台账的：G-7 … 属 **Q 类未知**，最晚时点建议 **S2 接口基线冻结前**"） |
| 3 | 假设台账的**谁能写** = 「AI **可追加**；用户裁决」⇒ 追加本身合流程 |
| 4 | 🔴 **但 `S-1 §三` 必须同步** —— `check-consistency.py` 有一条**假设集合一致性**检查（逐条比对 S-1 与台账的 `AS-*` 集合，并比对「最晚验证时点」），**只改一边 ⇒ 脚本必红**（见 §四 实测） |
| 5 | `S-1` **已冻结**（v1.9；`CR-009` 已提待裁）⇒ 改 §三 **必须走本单** |

## 二、改什么（**一处改动：新增 1 条假设**，两处同步面）

| # | 文件 | 改动 |
|---|---|---|
| 1 | `../40-假设台账/假设台账.md` | **新增** `AS-006` 一行 |
| 2 | `../10-基线/S-1-立项声明.md` **§三** | **新增** `AS-006` 一行（**「最晚验证时点」必须与台账逐字一致**），版本 **v1.9 → v1.10** |

> ⚠️ 与 `CR-009` 的关系：`CR-009` 会一并把 `S-1` 升到 **v1.10**。若两单同批裁决 ⇒ **版本号取一次即可**（由先执行者升版，后执行者不再重复升）。

**拟写内容**：

| 字段 | 内容 |
|---|---|
| 编号 | `AS-006` |
| 假设内容 | **回放所需的时间戳口径未定义**：`REQ-LOG-003` 只承诺「有可读时间戳 + 排序正确」，**未承诺**分辨率 / 单位 / 单调性 / **跨设备时钟是否统一**（回放缺口 `G-7`） |
| 若不成立的影响 | 回放的**倍速**与**多机对齐**无法实现；`app/replay` 的 `IReplayClock` 时基无法定档；S2 接口基线中「回放取帧时间轴」一栏写不出 |
| 最晚验证时点 | **S2 接口基线冻结前** |
| 验证方式 | 询问用户；或由 S1/S2 定「统一时基 = 单调时钟 + 执行起止对齐」 |
| 状态 | ⏳ 待验证 |

## 三、影响哪些已冻条目

**不推翻任何条目** —— `S-1 §三` 仅**新增**一条假设（5 条 → 6 条）。

## 四、代价 / 复跑范围

- 需重跑：`check-consistency.py`（含假设集合一致性 + 时点逐条一致性）
- 🔴 **实测证据（本单提出的直接原因）**：只改台账、不改 `S-1 §三` 时，脚本报
  `假设集合不一致：S-1 [AS-001..AS-005] / 台账 [AS-001..AS-006]` ⇒ **退出码 1**
  ⇒ AI 已把该改动**回退**，不留红状态（详见 `60-决策记录/决策记录.md` 2026-09-21）

## 五、复跑验证（回退后，实测）

```
python 00-流程/check-deps.py        ⇒ 退出码 0（87 条边 A70/B8/D9 · 32 节点）
python 00-流程/check-consistency.py ⇒ 退出码 0（23 项检查，失败 0）
```
